# 다국어 확장 구현 로드맵

**대상**: `finetune/` adaptation toolkit
**범위**: 일본어(1차) → 한국어(2차)를 **일회성 커스텀 훅이 아니라 툴킷의 기능**으로 지원
**작성일**: 2026-08-30 (최종 갱신 2026-09-04)
**상태**: M1·M5·C6 코드 완료. **M0/G0 통과(2026-09-04, RTX 5090 머신).** 타깃 화자·배포 방침 결정됨(§8 Q1–Q3).
**JA 경로는 하이브리드로 확정** — Micro→arona 직행을 먼저 측정하고, 실패 판정에서만 JSUT 단일화자 stage-1로,
그마저 실패할 때만 C7 다화자로 내려간다(§3.0). G1(c)는 사용자 결정으로 **자동 스크린으로 대체**한다.

이 문서는 [CONTRACT.md](../CONTRACT.md)의 공개 계약과 [SCOPE.md](SCOPE.md)의 지원 범위를
전제로 한다. 두 문서와 충돌하는 항목은 이 로드맵이 아니라 그쪽이 우선한다.

---

## 0. 왜 "확장"인가

현재 툴킷으로도 일본어 파인튜닝은 **오늘 가능하다**. `--frontend custom
--frontend-hook ./ja.py:create_frontend`로 끝난다. 그러나 그 경로는 언어가 늘어날수록
비용이 선형으로 늘어난다:

- 훅 `.py` 파일을 사용자가 직접 보관하고 `export` 때 다시 넘겨야 한다
  ([CUSTOM_G2P.md](CUSTOM_G2P.md) "Deployment requirements").
- 언어별 정규화·G2P 회귀 테스트가 툴킷 CI에 들어오지 못한다.
- 2단계 학습(언어 베이스 → 목표 화자)에 필요한 다화자 준비를 지원하지 않는다.
- 평가가 신호 진단(peak/RMS/무음/클리핑)뿐이라 언어 품질을 판정할 수 없다.

따라서 확장의 목표는 **"일본어를 되게 하는 것"이 아니라 "언어를 추가하는 비용을 상수로
만드는 것"** 이다. 한국어는 그 구조가 실제로 상수인지 검증하는 두 번째 사례다.

---

## 1. 검증된 기준선 (2026-08-30 실측)

아래는 추정이 아니라 이 저장소에서 실행해 확인한 결과다. 재현 명령은 §6.3에 있다.

> 코드 인용은 **심볼 이름이 정본**이다. 줄 번호는 편집으로 즉시 어긋나므로 쓰지 않는다.

| # | 사실 | 근거 | 함의 |
|---|---|---|---|
| V1 | **일본어 음소 전체가 릴리스 178심볼 인벤토리 안에 있다.** OpenJTalk 음소 집합(모음5·무성화5·`N`·`cl`·`pau` + 자음 30여)을 IPA로 매핑했을 때 신규 심볼 **0개** | `symbols.BASE_SYMBOLS` 대조 | 임베딩 마이그레이션이 전 행 복사. 랜덤 초기화 행 없음 |
| V2 | **한국어도 신규 심볼 0개.** espeak-ng `ko` 출력 중 base 밖 문자는 `-`(U+002D) 하나뿐이며, 이는 경음 표지이므로 `ʼ`(U+02BC, base에 존재)로 매핑하면 해소 | 동일 | JA/KO 모두 178 계약 유지 가능 |
| V3 | **eSpeak `ja`는 사용 불가.** 한자마다 영어 단어 "chinese letter"(`tʃˈaɪniːzlˈe̞tə`)를 리터럴로 방출. 카나 입력에서도 base 밖 문자 `ä`·`̞`(U+031E)·`ᵝ`(U+1D5D) 발생 | espeak-ng 1.52 실행 | 일본어는 커스텀 프론트엔드 **필수** |
| V4 | **eSpeak `ko`는 사용 불가.** 음운 규칙이 빠질 뿐 아니라(`옵니다`→`ˈopnidˌɐ`, `신라면`→`sˈinɾɐmjˌʌn`) **후두 대립을 붕괴시킨다** — 최소대립쌍 13개 중 6개(살/쌀·자다/짜다·불/뿔·방/빵·정/쩡·사/싸)가 같은 심볼열이 된다 | espeak-ng 1.52 실행 | 한국어도 커스텀 프론트엔드 **필수**. 2026-08-30 정정 |
| V5 | **`pyopenjtalk-plus` 0.4.1이 prebuilt wheel로 설치된다.** 본가 `pyopenjtalk`는 py3.10 wheel 없음(소스 빌드 필요) | `pip install --only-binary=:all:` | 일본어 의존성이 컴파일러 없이 해결됨 |
| V6 | ~~`g2pkk` + espeak `ko` 조합~~ → **폐기(2026-08-30).** 음운 규칙은 전달되지만 espeak가 후두 대립을 지운다(V4). **대체: `g2pkk` → 발음 한글 → 자모 직접 매핑.** 최소대립쌍 13/13 보존, base 밖 문자 0개, espeak 의존성 제거 | 실행 | 한국어 2단 구조(음운→자모) 확정 |
| V7 | **pyopenjtalk가 supertonic이 어휘사전으로 고쳐야 했던 항목을 그냥 맞게 읽는다.** `抗うつ剤`→コーウツザイ, `対策`→タイサク, `痛み止め薬`→イタミドメヤク, `2026年8月30日`→ニセンニジューロクネンハチガツサンジューニチ | 실행 | supertonic의 `jf-surgical-v1~v6` 아크는 이식 대상이 아님(§5.1) |
| V8 | **2단계 체이닝이 현재 코드로 동작한다.** `export`가 `config.json`+`model.pth`+`runtime/`을 쓰고 `resolve_base_model()`이 로컬 디렉터리를 받는다 | `exporting.export_checkpoint()`, `modeling.resolve_base_model()` | 언어 베이스 → 화자 적응 가능 |
| V9 | ~~심볼 수가 정확히 178이 아니면 체이닝이 깨진다~~ → **해소(2026-08-30, C6).** `load_runtime_components()`가 이제 `>= 178` + 릴리스 접두 일치를 본다 | `modeling.validate_release_compatible_symbols()` | 단일 실패점 제거. JA/KO는 애초에 178이라 체이닝은 이전에도 동작했다 |
| V10 | **다화자 준비가 하드 블록이다.** speaker 값이 2개 이상이면 `prepare`/`audit`이 즉시 실패 | `prepare_dataset()` · `audit_dataset()`의 speaker 가드 | 언어 베이스 단계에 우회 필요 |

### 확인하지 못한 것 — **전부 해소(2026-09-04, G0)**

작성 당시의 세 공백은 모두 메워졌다. 기록은 `inflect-work/env/G0.md`가 정본이다.

- ~~`M:` 미마운트~~ → 마운트 확인, JA/KO/JSUT를 로컬 디스크로 복제하고 검증했다.
  JA는 `metadata/checksums.sha256` **2019/2019 일치**, KO는 `wave` 헤더 전수
  **(1ch, 24-bit, 48 kHz) × 3401**·전사와 1:1, JSUT는 `repeat500` 제외 **7,196** 발화.
  NAS는 9p로 ≈53 files/s이므로 학습·prepare는 복제본에서만 한다.
- ~~`mel_fmin`/`mel_fmax` 미확인~~ → **`mel_fmin 0.0` / `mel_fmax 12000.0`**
  (`sampling_rate 24000`, `filter_length 1024`, `hop_length 256`, `win_length 1024`,
  `n_mel_channels 80`, `segment_size 16384`, `n_speakers 0`). 24 kHz의 Nyquist이므로 mel 손실이
  고음역을 자르지 않는다 — **§7 R4 해소**, 학습 전 결정 사항 없음.
- ~~CUDA 없음~~ → RTX 5090, capability **(12, 0) = sm_120**, torch **2.8.0+cu128**,
  `torch.cuda.is_available() == True`, `pytest` **111 passed / 1 skipped**,
  `INFLECT_TEST_BASE_MODEL=micro pytest -k inventory` **21 passed**(실물 체이닝 실증).
  HF `owensong/Inflect-Micro-v2`는 공개이며 토큰 없이 받아진다.

---

## 2. 설계

### 2.1 F1 — 언어 프론트엔드 레지스트리

새 패키지 `inflect_finetune/frontends/`를 만들고, 언어 프론트엔드를 **기존 훅 계약과
동일한 인터페이스**(`normalize` / `phonemize` / `symbols` / `metadata`)로 등록한다.
계약을 바꾸지 않는 것이 핵심이다 — `frontend.py`의 검증 경로(결정성 2회 호출, 미선언
심볼 거부, 소스 해시, 제어문자 거부)를 그대로 재사용한다.

```
inflect_finetune/frontends/
  __init__.py          # REGISTRY + resolve() + registry_record() + hook_path_for_record()
  ja_openjtalk.py      # pyopenjtalk-plus                                    [구현됨]
  ko_g2pkk.py          # g2pkk + espeak(ko) IPA 단계                          [M5]
```

**구현된 형태(2026-08-30)**: 레지스트리 항목은 새 mode가 아니라 **동봉된 custom
프론트엔드 파일에 대한 이름 별칭**이다. `resolve()`가 이름을
`FrontendOptions(mode="custom", hook="<pkg>/frontends/ja_openjtalk.py:create_frontend")`
로 바꾼다. 그 결과 `exporting.py`와 `frontend.py`를 **한 줄도 바꾸지 않고** 기존 custom
경로의 검증·패키징을 전부 재사용한다. `espeak.py`·`ipa.py`는 불필요해 만들지 않았다 —
espeak은 이미 `frontend.py`가 소유하고, 매핑 헬퍼는 언어 모듈 안에 있으면 충분하다.

CLI 변화:

```bash
# 지금
--frontend custom --frontend-hook ./ja.py:create_frontend
# 확장 후
--frontend ja-openjtalk
```

`custom`/`prephonemized`/`espeak`은 그대로 둔다. `dataset.json`의 `frontend` 블록은
`type: "custom"` + 기존 `hook` 레코드(소스 해시·metadata 해시)를 유지하고, 그 옆에
`registry` 블록으로 이름·언어·**필요한 extra**·툴킷 버전을 기록한다.

> 의존성 선언이 필요한 이유: [CUSTOM_G2P.md](CUSTOM_G2P.md)가 "외부 아티팩트를 요구하면
> self-contained라고 부르지 말 것"을 명시한다. pyopenjtalk 사전과 mecab-ko-dic은 정확히
> 그 외부 아티팩트다. 패키지가 스스로 그 사실을 기록하게 만든다.

### 2.2 F2 — zero-extension 심볼 정책과 178 제약

V1/V2로 JA·KO 모두 신규 심볼 0개가 가능하다. 이걸 **우연이 아니라 계약**으로 만든다.

1. `audit`에 `--require-no-new-symbols` 추가. 프론트엔드 수정이 조용히 심볼을 늘리는
   회귀를 잡는다. **(M1에서 완료)**
2. `modeling.load_runtime_components()`의 `!= 178` 검사를 **`>= 178` + base prefix 일치**로
   완화한다. **(C6에서 완료)** 준비 데이터셋과 런타임 인벤토리가 이제 같은 규칙
   (`validate_release_compatible_symbols()`)을 쓴다.

이 두 개는 서로를 보완한다. (1)은 "심볼을 늘리지 마라", (2)는 "늘려야만 하는 언어가
나왔을 때 막다른 길이 아니게 하라"이다.

**C6가 무엇을 풀었고 무엇을 안 풀었는지**(2026-08-30): JA·KO 둘 다 신규 심볼이 0개라
**체이닝은 C6 이전에도 동작했다**(178 == 178). C6가 없앤 것은 잠재 실패 지점이고,
같이 메운 것이 더 크다 — `warm_start_from_release()`와 `load_runtime_components()`에
테스트가 하나도 없었고, CONTRACT.md가 최소 게이트로 요구하는 "embedding migration by
symbol identity"가 미검증이었다. 일본어 악센트는 `↑`/`↓` 그대로 두었다(D1 불변).

### 2.3 F3 — 2단계 학습 (language-base → voice)

```
stage 1  다화자 일본어 코퍼스 ──> ja-base 체크포인트 (언어·음운·타이밍)
stage 2  단일 화자 (고 F0 여성) ──> 제품 체크포인트 (음색·음역)   [--base = stage1 export]
```

영어 남성 베이스에서 일본어 고 F0 여성으로 **한 번에** 가는 것은 SCOPE.md가 경고하는
"모든 부분을 동시에 움직이라"는 요구다. 언어 이동과 화자 이동을 분리한다.

구현: `prepare`에 `--corpus-role {voice,language-base}` 추가.
- `voice`(기본): 현재 단일 화자 가드 유지.
- `language-base`: 다화자 허용. `dataset.json`에 `corpus_role`과 화자 목록을 기록하고,
  `PREPARATION_REPORT.txt`에 "이 데이터셋은 화자 정체성 학습에 쓸 수 없다"를 명시.

`audit_dataset()`의 동일 가드도 `corpus_role`을 읽도록 한다. 기본 동작은 바뀌지 않는다.

### 2.4 F4 — 평가 확장

현재 `evaluate`는 duration/silence/clipping/peak/RMS/DC/non-finite만 본다. 언어 품질도
음성 정체성도 판정하지 못한다. supertonic 프로젝트의 가장 값비싼 교훈이 여기 적용된다 —
**모든 자동 지표는 스크린이고, 판정자는 청취다** (§5.2).

추가할 것:
1. **언어별 ASR/CER 평가기.** `examples/transcript_evaluator_plugin.py` 훅이 이미 비어
   있다. JA는 kana 정규화 CER, KO는 자모 정규화 CER. 툴킷이 ASR을 자동 다운로드하지
   않는 현재 정책은 유지한다(플러그인으로만).
2. **F0 진단.** `_signal_metrics()`에 f0 median/IQR/유성 프레임 비율 추가. 고 F0 여성
   타깃에서 레지스터 붕괴와 피치 평탄화를 조기에 잡는 유일한 싼 관측치다.
3. **블라인드 A/B 청취 페이지 생성기.** 랜덤 라벨, 실물 앵커 1개 강제 포함, 판정
   JSON export. 고음역 행을 반드시 페이지에 올린다.

### 2.5 F5 — 배포 패키징

`exporting._write_deployment_runtime()`이 레지스트리 프론트엔드를 인식하고,
생성 런타임에 (a) 프론트엔드 모듈, (b) `requirements-frontend.txt`, (c) 사전/의존성이
없을 때 **영어로 조용히 폴백하지 않고 실패**하는 경로를 쓰게 한다. 마지막 항목은
CONTRACT.md의 검증 게이트에 이미 있는 요구사항이다.

---

## 3. 단계별 로드맵

각 단계는 **수락 게이트를 통과해야** 다음으로 간다. 게이트는 착수 전에 고정하고,
데이터를 본 뒤에 옮기지 않는다.

### 3.0 JA 경로 결정 — 하이브리드 (2026-09-04)

M3의 F3(2단계)는 "다화자 코퍼스로 언어 베이스"를 전제하지만 그 전제는 C7 미완으로 오늘 실행할 수
없고(§4 C7, `docs/TRAINING.md`), R1은 **측정된 사실이 아니라 사전 우려**다. 한편 arona JA는 1–12 s 창에서
1,282클립/99.7분으로, supertonic이 "~116발화면 암기한다"고 경고한 규모의 약 10배다. 그래서 직행 1런이
R1을 가장 싸게 측정한다. 되돌아갈 때의 stage-1도 **C7이 필요 없는 단일 화자 JSUT**(7,196발화, 로컬
복제 완료)를 먼저 쓴다.

전환 규칙은 **착수 전에 고정**한다.

| 판정점 | 조건 | 행동 |
|---|---|---|
| **T1** JA-A step 3000 (decoder 해제 직전) | step-3000 체크포인트로 OOD 10행을 렌더해 들었을 때 "일본어로 들리지 않음"(정체성·품질 불문 — G3 문구 재사용) | JA-A 중단 → JSUT stage-1(JA-B1) 즉시 착수 |
| **T2** JA-A G4 라운드 | (i) 자동 스크린 통과 후보가 0개, 또는 (ii) 블라인드 페이지의 **절대 바닥(floor, paired와 분리 기록)**에서 "언어 불명/붕괴"가 결정 블록 항목의 과반 | JA-B1 → JA-B2(arona stage-2) 착수. JA-A 선택본은 JA-B2의 paired baseline으로 보존 |
| **T3** JA-B2 G4 라운드 | JA-B2도 T2 조건 실패 **그리고** 자유기술 결함이 "음운/타이밍" 축에 몰림 | **C7 구현** → JVS 여성 화자(+JSUT) 다화자 stage-1. 결함이 "음역/균열" 축이면 C7이 아니라 데이터(고음역 행)를 다시 본다 |
| **T-KO** | KO는 어떤 시나리오에서도 직행(4.825 h 단일화자). KO G4 실패 시에만 KSS stage-1 검토 | 그때 `Arona_KSS` 952/998행이 KSS 텍스트와 동일하므로 stage-2 **검증셋에서 KSS 중복 텍스트를 제거**해야 비교가 성립한다 |
| **T-Nano** | Micro JA G4 통과 전 Nano 착수 금지 | §5 LANGUAGES.md "Use Micro for the first adaptation attempt" |

**직행 경로의 G4 baseline 정의**: 로드맵 G4는 "baseline 대비 우세"인데 직행에는 stage-1 export가 없다.
직행 라운드의 baseline은 **자동 스크린을 통과한 가장 이른 후보 체크포인트**로 한다(학습 진행 효과를 측정).

### M0 — 환경 확정 (CUDA 머신) — **통과 (2026-09-04)**

`inflect-work/env/G0.md`가 기록 정본이다. 요약: sm_120 / torch 2.8.0+cu128 / cuda True /
`pytest` 111 passed·1 skipped / 실물 인벤토리 체이닝 21 passed / Micro 공개 다운로드 성공 /
`mel_fmax 12000` → R4 해소 / JA·KO·JSUT 로컬 복제 및 검증 완료.

G0에서 **공개 툴킷 결함 2건**이 드러나 함께 고쳤다(둘 다 이 문서의 인계 절차를 따라가다 나왔다).

1. `finetune/`에서 `python -m venv .venv`(§6.1의 지시)를 하면 `tests/test_public_safety.py`가
   트리 전체를 `rglob`하며 `.venv/`의 비 UTF-8 파일을 읽어 `UnicodeDecodeError`로 실패했다.
   → 발행 표면이 아닌 트리(dot 디렉터리·`build`·`dist`·`*.egg-info`)를 스캔에서 제외.
2. `pyproject.toml`이 `unidecode`/`num2words`를 선언하지 않았다. 그런데 웜스타트는 릴리스의
   `runtime/text/cleaners.py`를 임포트하고 그 파일이 `unidecode`를 임포트한다 → 깨끗한 설치에서
   `train --base micro`가 사용자 코드와 무관하게 `ModuleNotFoundError`로 죽는다.
   → 릴리스 `requirements.txt`와 같은 하한으로 두 패키지를 선언.

부수로 `[ko]` 주석을 사실에 맞게 고쳤다: g2pkk는 python-mecab-ko를 **선언하지 않고 첫 임포트에서
런타임 pip install**을 한다(관측). 임포트가 네트워크와 쓰기 권한을 요구하는 것은 재현성 위험이라
분석기를 엑스트라에 명시했다.

- [x] CUDA torch 동작, `nvidia-smi`, VRAM 확인
- [x] `M:` 마운트 및 일본어 데이터셋 실물 확인 (+ 로컬 복제·체크섬 검증)
- [x] `finetune/` editable 설치 + `pytest` 전체 통과
- [x] 릴리스 `config.json`의 `mel_fmin`/`mel_fmax`/`filter_length`/`hop_length` 기록

**게이트 G0**: `inflect-adapt --help`, 기존 테스트 스위트 green, Micro 릴리스 다운로드 성공.
→ **통과.** 세 항목 모두 확인. 단, 두 번째 항목은 위 결함 2건을 고친 뒤에야 green이 됐다.

### M1 — 프론트엔드 레지스트리 + 일본어 프론트엔드 — **코드 완료 (2026-08-30)**

> 구현·테스트·문서는 끝났다. **G1은 아직 열려 있다** — 200문장 화자 검수가 남았고
> 그것은 CUDA 머신 인계 후에 마무리한다.

- F1 골격(`frontends/` 패키지, 레지스트리, CLI 배선)
- `ja_openjtalk.py` 구현: 텍스트 정규화 → `extract_fullcontext` → 음소+악센트 → IPA
- 사용자 사전 슬롯(고유명사 오독 대응). supertonic의 어휘사전 **형식**은 참고하되
  내용은 이식하지 않는다(V7).
- 회귀 테스트: 결정성, 심볼 선언 일치, 신규 심볼 0개, 고정 입력 → 고정 출력 스냅샷

**게이트 G1**: 대표 문장 200개에 대해 (a) 신규 심볼 0, (b) 2회 호출 결과 동일,
(c) 일본어 화자가 정규화 텍스트와 카나 표기를 검수해 오독률 기록. **오독은 0을 요구하지
않는다 — 측정하고 기록하는 것이 게이트다.**

**(a)(b) 통과 (2026-08-30)**: `examples/japanese_review_suite.txt` 207문장에 대해
실패 0 · 릴리스 인벤토리 밖 문자 0. 재현:

```bash
python examples/frontend_review_dump.py \
  --sentences examples/japanese_review_suite.txt \
  --frontend ja-openjtalk --language ja --output review/ja.tsv
```

**(c) 미완**: 화자 검수는 CUDA 머신 인계 후. 커버리지 스위트는 프론트엔드 동작을 덮지
코퍼스를 대표하지 않으므로, 실제 전사 무작위 표본에 대해서도 같은 덤프를 돌린다.

**검수·리뷰가 잡아낸 결함 3건(전부 수정 + 테스트 잠금)** — 스위트가 그냥 통과했다면
발견하지 못했다:
1. 소수점이 문장 끝으로 처리돼 `1.5キロ`가 「イチ。ゴキロ」로 읽혔다. 숫자 사이 `.`는
   분할 대상에서 제외했다.
2. `3,000円`이 「サン ゼロゼロゼロ」로 읽혔다. Open JTalk은 자릿수 구분 쉼표를 모른다 —
   정규화에서 제거한다.
3. **(2)의 첫 수정이 과잉이었다.** `(?<=\d),(?=\d)`가 `1,2,3`을 `123`으로 합쳤다 —
   고치려던 것보다 나쁜 조용한 데이터 손상. 자릿수 쉼표는 **정확히 세 자리**가 뒤따를
   때만 제거하고, 살아남은 쉼표는 열거 구분자로 분할한다.

### M2 — 데이터 준비 — **통과 (2026-09-04)**

준비한 데이터셋 3개. 모두 `added_symbol_count == 0` · `audit.valid true`(strict +
no-new-symbols) · 검증셋 음소 ⊆ 학습셋 음소 · 언어별 필수 심볼이 검증셋에 존재.

| 데이터셋 | 역할 | 규모 | 비고 |
|---|---|---|---|
| `ja-arona-v2` | JA stage-2 (목표 음성) | 1,245행 / 96.96분 / 소스파일 1,188 | `↑` 4,346 · `↓` 4,465 (검증셋 289/278) |
| `ja-jsut-v1` | JA stage-1 (언어 베이스) | 7,101행 / 9.09 h | 단일 여성 화자라 C7 불필요 |
| `ko-arona-v1b` | KO 단독 트랙 | 3,348행 / 4.637 h | 균일 −3 dB 후 재준비(아래) |

- `group_id`는 전부 **소스 녹음 파일**이다. arona JA는 한 소스에서 최대 26개 VAD 세그먼트가
  나오므로 발화 단위 split은 같은 세션을 양쪽에 걸친다. 배포된 `metadata/splits/*`는
  텍스트 20개·소스 14개가 교차하므로 쓰지 않았다.
- **`--min/--max-duration-seconds`는 필터가 아니라 단언이다.** 길이·텍스트 필터는 매니페스트
  빌더가 전부 적용하고, 플래그에는 같은 값을 넘겨 단언으로만 쓴다.
- **`A_CO026` 서브코퍼스 전체 제외**(사용자 결정). 자동 스크린에서 이 조수사 세기 드릴만
  녹음 스크립트 읽기와 p50 0.091 · p90 0.250이었고(다른 12개는 p50 ≤0.045), `九時`를 성우는
  キュウジ로 읽었는데 프론트엔드는 クジ로 읽는 식으로 **텍스트–오디오 정렬이 어긋난다**.
  같은 실패 계열이라 JSUT의 `countersuffix26`도 제외했다.
- `C_AronaChan` 170행은 **포함 + `text_source` 보존**(사용자 결정). ASR 전용 전사라
  R10으로 남고, JA G4 통과 후 제외 ablation으로 영향을 재본다.
- **클리핑 정책은 예상과 다른 곳에서 발동했다.** anime 코퍼스는 애초에 쓰지 않으므로
  §5.2의 교훈 70·71은 해당이 없었고, 문제는 **우리 준비 과정**이었다 — arona KR 원본은
  −0.1 dBFS로 리미팅돼 소스 클리핑이 사실상 없는데, 48→24 kHz 리샘플이 최대 +2.1% 링잉을
  만들고 `prepare`가 그 위를 잘라 **3,348행 중 331행(9.89%)의 상단이 사라졌다**.
  사전 선언대로 로컬 사본 전체에 균일 −3 dB(`sox gain -3`)를 적용해 재준비했다.
  서브코퍼스별 게인과 행 필터는 쓰지 않았다 — 레벨 관계는 행의 성질이 아니라 코퍼스의
  성질이다(교훈 71의 요지). 기록은 `inflect-work/prepared/KO-G2-RECORD.md`.
- 이 과정에서 **공개 툴킷 결함 2건**을 더 고쳤다: `inspect_wav`가 sox·ffmpeg가 24-bit에
  쓰는 WAVE_FORMAT_EXTENSIBLE을 거부했고, `prepare`가 **자기가 만든 클리핑을 보고하지
  않았다**. 이제 `dataset.json.diagnostics.output_clipped_files`가 소스 측 수치와 나란히 나온다.

**게이트 G2**: `phoneme_coverage.json`의 `added_symbol_count == 0`,
group/텍스트 누출 0, 검증셋 음소가 학습셋에 전부 존재. → **세 데이터셋 모두 통과.**

> **운영 교훈 — 프론트엔드를 고치면 그 전에 준비한 데이터셋은 export가 거부한다.**
> `dataset.json`이 훅 소스 해시를 기록하고 `export`가 다시 해시하기 때문이다(설계된 동작,
> `docs/TROUBLESHOOTING.md`에 있다). `fy` 추가로 `ja-arona-v1`/`v2`가 그 상태가 됐고
> `v2`는 재준비했다. `v1`은 T1 기록용으로만 남기고 그 이후 체크포인트는 export할 수 없다.
> **프론트엔드 수정은 prepare 앞에 온다.**

### M3 — F3 다단계 학습 배선 + 일본어 stage 1

- ~~`modeling.py` 178 제약 완화(F2-2)~~ **완료(C6)**
- ~~`--corpus-role`(C7)~~ → **조건부 보류.** stage-1을 단일화자 JSUT로 잡으면 필요가 없다.
  §3.0 T3에서만 착수한다.
- ja-base 학습 (Micro) — **진행 중**: `runs/ja-jsut-stage1-20260904`, 프리셋 20,000 step,
  batch 8 / accum 1, 실측 ≈3.0 step/s. 스테이지 기본값 유지(posterior_warmup 500,
  decoder_unfreeze 3000).
- export → 그 디렉터리를 `--base`로 재로드하는 **체이닝 스모크**
  → 실행 스크립트 `inflect-work/scripts/run_g3.sh`에 게이트 조건까지 고정해 뒀다.

> **JSUT 언어 베이스의 한계를 미리 기록한다.** JSUT는 낭독 평서문이라 학습셋 전체에
> `!`가 1회, `?`가 4회뿐이다(`G2-extra.txt`). 즉 **의문·감탄 프로소디와 고음역은 언어
> 베이스가 가르쳐주지 않고 stage-2의 arona 데이터가 전부 감당해야 한다.** G4에서 고음역
> 결함이 나오면 이 사실을 먼저 본다 — 모델 용량 문제로 오인하기 쉬운 자리다.

**게이트 G3**: stage-1 export를 stage-2의 `--base`로 로드해 1 step 학습이 돈다.
held-out 합성이 일본어로 들린다(정체성·품질 불문).

### M4 — 평가 확장 + 일본어 stage 2

- ~~F4(CER 플러그인, F0 진단, 블라인드 페이지)~~ **완료(C8·C9·C10, 2026-09-04)**.
  청취 파이프라인은 실물 렌더로 예행 검증했다(`listening/rehearsal`, page_key
  `rehearsal-not-a-verdict`) — 행별 라벨 재섞임·실물 앵커 강제·catch 행 동작 확인.
- 고 F0 여성 화자 적응 — stage-2 (`scripts/run_stage2.sh`)
- 체크포인트 선택 규칙을 **데이터 보기 전에** 선언 → `scripts/run_g4.sh` 머리에 고정했다:
  후보 {4000,5000,6000,7000,final} · 스크린은 탈락 전용 · **baseline = 스크린을 통과한
  가장 이른 후보** · 선택은 `latest_within_noise`.

> **직행 arm은 존재하지 않는다.** 로드맵 G4는 "baseline 대비 우세"를 요구하고 원래 계획은
> JA-A 선택본을 paired baseline으로 쓰려 했는데, JA-A는 T1에서 멈췄고 그 체크포인트들은
> `fy` 수정으로 데이터셋 해시가 어긋나 export도 안 된다. 그래서 baseline을 위와 같이
> 재정의했다 — **결과를 보기 전에** 정한 것이고, 이 라운드가 측정하는 것은 "2단계가 직행보다
> 낫다"가 아니라 "학습 진행이 무엇을 벌었나"임을 분명히 해 둔다.
> 실물 앵커는 `prepared/<ds>/val-real.jsonl`로 **렌더와 같은 행 id·같은 evaluate 경로**를
> 지나게 만들었다(교훈 79의 렌더 관례 규칙).

**게이트 G4**: 사전 선언한 규칙으로 고른 체크포인트가 블라인드 청취에서 baseline 대비
우세. 고음역 행이 페이지에 포함되어 있을 것. 여기서 처음으로 "일본어 파인튜닝 성공"을
말할 수 있다.


**G4 결과 (2026-09-04 → 05)**: JA(JSUT stage-1 → arona stage-2 8,000 step)·KO(직행 10,000 step) 두 라운드
모두 스크린은 통과했으나 **사용자 청취 판정 "쓸 수 없음"** — 전 렌더에 심한 링잉(정적 격자 톤). 순서
JA > KO > JSUT. 원인은 §7 R16, 전체 진단은 `inflect-work/evals/diag/RINGING-DIAGNOSIS.md`. **G4 미통과.**
"일본어 파인튜닝 성공"은 아직 말할 수 없다. 이 결함은 언어·화자가 아니라 **적응 레시피(학습 코어)**의
문제이므로, 다음 단계는 M-level이 아니라 툴킷 학습 절차의 수정이다.

### 3.1 링잉 대응 1차 라운드 (2026-09-05) — **처방 기각, 계측 존속**

사용자 승인으로 학습 코어를 열고(개선안 b, C13–C16) KO 직행에서 2 arm을 10,000 step 돌렸다.
`ko-arona-v1b`, batch 8, 대조군 `ko-arona-micro-direct-20260904`와 동일 조건.

| arm | 추가 플래그 |
|---|---|
| K-A | `--adversarial-gating --adversarial-ramp-steps 1000 --decoder-lr-warmup-steps 300 --generator-ema-decay 0.999` |
| K-B | K-A + `--decoder-polish-mode recon --stft-loss-weight 1.0 --decoder-proximal-weight 0.1` |

종점 평가(같은 라운드 매니페스트 40행, 같은 evaluate 경로, p50):

| | 실물 | 대조군 | **K-A** | **K-B** |
|---|---:|---:|---:|---:|
| `grid_tone_excess_db` | −0.13 | 8.15 | **8.27** | **11.53** |
| `steady_tone_artifact_score` | 0.00 | 29.86 | 27.09 | 7.37 |
| `fold_periodic_excess_db` | −0.16 | 4.17 | 3.85 | 19.49 |
| `f0_median_hz` | 363.9 | 362.7 | 376.9 | **93.76** |
| `clips_grid_tone_flagged` /40 | 0 | 40 | 40 | 40 |
| `clips_f0_locked_to_frame_grid` /40 | 0 | 0 | 0 | **31** |

**K-A는 대조군과 구별되지 않는다.** 음역·IQR은 건강했으므로 언어·화자 적응 자체는 대조군만큼
됐고, 콤만 그대로다.

**K-B는 훨씬 나쁘다.** 추적 음고 중앙값이 화자의 364 Hz가 아니라 콤 주파수 93.76 Hz로 붕괴했다.
그런데 recon 폴리시 동안 자기 손실은 계속 좋아졌다(mel 0.936 → 0.726). **재구성 손실은
MR-STFT를 포함해 이 아티팩트에 민감하지 않다** — 더 열심히 최적화하는 것은 탈출구가 아니다.

**진단의 인과 사슬이 기각됐다.** 추론경로 `z_dc_rms`(릴리스 0.737):

| step | 대조군 | K-A |
|---:|---:|---:|
| 1000 | 1.459 | 1.453 |
| 3000 | 1.421 | 1.750 |
| 10000 | **1.190** | **1.528** |

대조군은 학습이 진행되며 드리프트가 줄어든다 — 적대 항이 z를 되당기고 있었다. 게이팅은 그 힘을
없애 드리프트를 **늘렸다**. 그리고 결정적으로 **드리프트 1.190과 1.528이 같은 콤(8.15 대 8.27 dB)을
낸다** — 종점에서 드리프트는 콤을 지배하는 변수가 아니다. 학습경로(포스테리어 z)와 추론경로 둘 다
울리므로 두 경로의 불일치도 아니다.

**남은 것은 계측이다.** 실물 40행 0/40, 두 렌더 40/40, 사이드카가 콤이 켜지는 구간(step 500 → 1000)을
짚었다.

**단, 이 라운드의 결론은 2026-09-05 감사로 한 단계 내려갔다(§3.2).** K-A·K-B를 포함한 실패 런
전부가 **정답과 생성음에 다른 스펙트럼 바닥값을 쓰는 mel 손실**로 학습됐다. 따라서 위 수치는
"이 처방이 무효"가 아니라 "깨진 목적함수 아래에서 이 처방을 재본 결과"다. 스왑 테스트로
디코더를 배제했다는 서술도 과하다 — 반대 방향(릴리스 z → 적응 디코더)은 시행되지 않았다.

다음 후보(수정): (i) z의 프레임 간 구조·채널 공분산과 디코더의 상호작용, (ii) 디코더의
anti-imaging 부재 자체(업샘플러 커널·필터, 단 kernel `[16,16,4,4]`이 stride `[8,8,2,2]`의 정수배라
단순 uneven-overlap 설명은 부정확하다), (iii) **0–12 kHz 안** 좁은 톤에 대한 손실의 해상도·민감도와
중간 업샘플 단계의 이미징. ~~`mel_fmax 12000`이 12 kHz 위를 제외하는가~~ 는 **삭제** — 24 kHz의
Nyquist가 12 kHz이므로 제외되는 것이 없다.
상세 측정은 `inflect-work/runs/REMEDY-B-VERIFICATION.md`.

### 3.2 외부 감사와 확정 결함 5건 (2026-09-05) — **수정 완료, 대조 실험 사용성 탈락**

두 번째 외부 감사가 저장소 함수를 실제로 실행해 결함을 재현했고, 나도 전부 재확인했다.
**전부 초기 커밋 `1da708c`부터 있던 것이고 이번 브랜치가 만든 것이 아니다.**

| ID | 결함 | 확인 방법 | 상태 |
|---|---|---|---|
| D1 | mel 손실이 정답 `sqrt(\|X\|²+1e-6)`(바닥 1e-3)과 생성 `clamp_min(1e-5)`를 비교 — 같은 파형이 `ln(100)=4.605`, 실물 음성끼리 0.07–0.52. 조용한 셀의 기울기가 잡음을 **보상**(RMS 1e-6에서 −840750) | 저장소 함수 CPU 실행 | ✅ C17 |
| D2 | 단계 경계 resume 시 새 그룹 lr이 0으로 남음. decoder는 종단 단계라 영구 | 실제 `train_adaptation` 재현 | ✅ C18 |
| D3 | `evaluate`가 원음 매니페스트에서 모델을 열지 않는데 리포트에 `model_dir`이 남음. 혼합 매니페스트는 `audio`를 조용히 무시 | 코드 분기 + 리포트 29건 검사 | ✅ C19 |
| D4 | 배포 splitter가 `1.5초`를 `1.` + `5초`로 분할(220 ms 무음·시드 재추첨 삽입) | shipped source 13입력 실행 | ✅ C20 |
| D5 | `_validate`의 `manual_seed`가 학습 RNG를 오염 → `validation_interval`이 학습 결과를 바꿈 | 코드 확인 | ✅ C18 |

**기존 판정에 미치는 영향**: D2는 어떤 런도 밟지 않았고(resume한 두 런 모두 단계 중간),
D3은 eval 매니페스트 19개 전부 text-only라 G4·K-A/K-B 판정이 유효하다. **D1만이 실패 런 전부에
영향을 준다.**

콤 주입 실험(실물 6클립): 주입 레벨 1e-4에서 legacy mel이 0.2190 → **0.2124로 내려가고**,
통일 mel과 MR-STFT는 전 범위 단조 증가한다. 즉 "재구성 손실은 이 아티팩트에 민감하지 않다"는
이전 서술은 틀렸다. 다만 **격자 점수가 같다고 콤 진폭이 같은 것은 아니며**, K-B 악화에 대한
D1의 기여도는 대조 학습 전까지 미확정이다.

**대조 실험 결과 (2026-09-05, 완료)** — 상세: `inflect-work/runs/MEL-AB-2026-09-05.md`

같은 커밋 `0727f7d`·같은 시드에서 `--mel-loss-legacy-floor` 하나만 다른 두 arm을 1,500 step 돌렸다.
개입 범위는 손실 값이 아니라 옵션·identity·입력 일치로 검증했다(`training-options.json`이 한 필드만
다르고, step 500까지 검증 렌더가 비트 동일 — warm-up 동안 추론이 `enc_q`를 쓰지 않기 때문이다).

**사전 선언 규칙의 판정은 "악화"다**(격자 톤 초과 문장별 차이 중앙값 step 1000 +3.73, final +1.63).
규칙을 사후에 바꾸지 않는다. 그러나 종점에서 세 스크린 중 둘은 개선을 가리켰고(steady-tone −4.62,
fold −1.78), 계기를 검사한 결과 **격자 톤 초과가 이 비교에 유효하지 않았다.**

`grid_tone_excess_db`는 on-grid/off-grid **비율**이고 분모는 그 렌더 자신의 광대역 바닥이다.
각 렌더의 자기 레벨 기준으로 40행을 다시 재면(실물 척도 병기):

| 관측치 p50 | 실물 | legacy | matched | 차이 | matched가 낮은 문장 |
|---|---:|---:|---:|---:|---:|
| 콤, 신호 대비 dB | −52.3 | −35.9 | **−39.0** | **−3.16** | 37/40 |
| 광대역 바닥, 신호 대비 dB | −52.0 | −42.2 | **−46.7** | **−4.64** | 40/40 |
| **콤, 절대 평균 PSD** | — | — | — | **+0.99** | **9/40** |
| 광대역 바닥, 절대 평균 PSD | — | — | — | −0.62 | 27/40 |
| 레벨 dBFS | −20.0 | −36.4 | −32.5 | **+3.97** | 1/40 |
| on/off 비율(스크린) | −0.1 | 6.1 | 7.7 | +1.63 | 3/40 |

**"신호 대비"와 "절대"는 같은 데이터의 다른 질문이고, 둘 다 옳다.** 신호 대비 지표는 순수
게인에 불변이므로 "콤이 얼마나 있는가"에 답하지 않는다. 절대 평균 PSD는 `레벨 + rms_dbfs`로
파생되며, 그 값에서 **matched의 콤은 줄지 않았다**(중앙값 +0.99 dB, 40문장 중 9개에서만 감소).
줄어든 것처럼 보인 이유는 전체 전력이 3.97 dB 커졌기 때문이고, 그중 일부는 matched 렌더가 더
짧고(40/40) 무음 프레임 비율이 더 낮다는(38/40) 사실이 설명한다.

따라서 **4단 규칙을 사후 관측치에 적용해 "부분 개선"이라고 쓴 앞선 문장은 철회한다.** 판정
등급은 **"개선 불확실"**이다. 바닥이 상대 기준으로 4.6 dB 내려간 것은 예측대로지만, 절대 기준
차이는 −0.62 dB에 그친다. 이 값들은 선택된 격자 bin의 평균 PSD이지 분리된 링잉 성분이 아니며,
길이·무음 비율·스펙트럼 구성 차이는 어떤 정규화로도 제거되지 않는다.

**정정된 문구**: 앞선 초안의 "mel 결함은 지배 원인이 아니다"는 이 데이터가 지지하는 진술이
아니다. 지지되는 것은 **"이번 초기 실험에서 mel 수정만으로는 링잉이 해결되지 않았다"**이며,
콤이 실물까지 16.4 dB 격차 중 3.2 dB만 좁혔다는 사실이 그 근거다. 1,500 step · 단일 seed ·
폴리시 미진입 · 청취 없음으로 지배 원인의 유무는 판별할 수 없다.

**사전 선언과 사후 탐색의 구분**: 사전 선언은 4단 규칙과 그것이 걸린 `grid_tone_excess_db`,
그리고 그 판정("악화")뿐이다. 사후 탐색은 **둘**이고 서로 다른 답을 냈다 — 상대 레벨
(`grid_tone_level_db`·`off_grid_level_db`, 2026-09-05, "개선")과 절대 평균 PSD 재측정
(2026-09-06, "감소 아님"). 어느 쪽도 사전 판정을 덮지 않는다. 계기 세 개가 세 답을 낸 것이
이 라운드에서 얻은 가장 이전 가능한 교훈이다. 문장별 원시값 120행(3체크포인트 × 40문장,
`legacy.*`·`matched.*`·`diff.*`, 파생 절대 PSD·길이·무음 비율 포함)을
`inflect-work/evals/M-mel-ab-per-sentence.csv`에 보존했고
`scripts/verdict_mel_ab.py --csv`로 재생성된다. 재현 지점은 태그 `mel-ab-2026-09-05`(임시 옵션이
살아 있는 마지막 커밋)이다.

**청취 판정 (2026-09-06)** — `inflect-work/listening/mel-ab-20260906/`, 8행, 실물 앵커·탈락 control 포함,
봉인 mapping. **두 arm 모두 품질 1("사람 목소리로 들리지 않는다") 8/8, 언어 인식 "아니오" 8/8.**
실물은 품질 5가 8/8이고 강제 선택 `most_natural`은 실물 7회 + 미응답 1회, catch 행 소음 바닥 0.
강제 선택 `most_blurred`는 M-legacy 7 대 M-matched 1. 자유기술 8행 전부: 1.5k arm은 **"발음 템포에
음성 대신 ringing 기계음만"**, 10k control은 "발음도 들리지만 ringing이 심하다".

**결론: 두 arm 모두 사용 불가.** 이것은 승격 보류가 아니라 **사용성 기준 탈락**이다. 절대 척도
(품질·언어)에서는 두 arm이 구분되지 않았고, 강제 선택에서만 7:1로 legacy가 더 흐렸다 — "귀로
구분 불가"는 과한 표현이므로 철회한다. 그 차이의 크기는 확정하지 않는다. 이 페이지는 클립별로
−24 dBFS를 목표하고 피크 가드를 걸었기 때문에 33트랙 중 9개가 목표에 못 미쳤고 한 행에서 2.33 dB
차가 났다(빌더는 이후 페이지 공통 목표로 수정, C22).

**mel 수정(D1)은 유지한다.** 결함 수정 자체는 타당하고, 이 실험이 보인 것은 "이번 초기 실험에서
mel 수정만으로는 해결되지 않았다"이다. 또한 **1,500 step은 초기 아티팩트 진단 지점이지 최종
음질 비교 지점이 아니다.** control이 발음을 들려준 것은 legacy가 우수하다는 증거가 아니고,
matched의 후반 효과도 배제되지 않았다. mel 수정을 근거로 장기 학습을 늘릴 근거는 없다.

### 3.3 잠재 표현 × 디코더 완전 교차 (2026-09-06) — **잠재가 지배, 디코더는 미해명**

상세: `inflect-work/evals/diag/cross-latent-decoder/FINDING.md`

**1,500 step arm으로는 이 실험을 할 수 없다.** 두 arm 모두 디코더가 전 구간 lr 0이고 231개
텐서가 base Micro와 비트 동일하므로, 둘끼리 교환하면 같은 텐서를 자신과 비교한다. 디코더는
디코더가 7,000 step 학습된 10,000 step 런에서 가져왔다. KO 심볼이 릴리스 178과 완전히 동일해
같은 12문장의 같은 토큰열을 두 사전 경로에 통과시킬 수 있고, 모든 셀의 내용이 일치한다.

on/off 비율 dB (실물 −0.1 근처, 탈락 렌더 +6 이상):

| | D-release | D-control | D-KB |
|---|---:|---:|---:|
| **L-release** | **−0.20** | **1.53** | **−0.24** |
| L-adapted | 6.33 | 8.01 | 9.89 |
| L-posterior | 6.25 | 7.48 | 9.29 |

**릴리스 잠재는 7,000 step 학습된 적응 디코더 두 개를 포함한 세 디코더 전부에서 깨끗하고,
적응 잠재는 릴리스 디코더를 포함한 세 디코더 전부에서 울린다. 12문장 전부, 세 디코더 전부.**

- **적응 디코더 가중치는 링잉의 필수조건이 아니다.** 릴리스 디코더만으로도 적응 잠재는
  울린다. 다만 이것을 "디코더를 사실상 면제한다"로 쓴 앞선 문장은 철회한다 — **같은 디코더
  구조가 특정 잠재 분포에 취약할 가능성은 이 격자로 배제되지 않으며**, 릴리스 디코더에는
  anti-imaging 필터가 없다. §3.1의 유보는 "한 방향은 시행됐다"까지만 해소됐다.
- **"디코더 학습이 악화시켰다"는 지표를 명시해야 성립한다.** 적응 잠재에서 **비율 지표**는
  D-release 6.33 → D-control 8.01 → D-KB 9.89로 악화하지만, **상대 레벨 지표**는 −44.48 →
  −46.47 → −47.66으로 **반대 방향**이다. 어느 쪽이 실제로 더 나쁜 소리인지는 청취하지 않았다.
- **사후 탐색은 수렴하지 않았다.** 채널별 DC 제거는 비율 지표에서 크게 개선(6.25 → 3.35)하지만
  콤 레벨 지표에서 D-control을 6.4 dB 악화시킨다. RMS 정합은 레벨 지표에서 일관되게 개선하고
  비율에서는 거의 무효. **두 지표가 동의하는 것은 "시간 축 평활은 도움이 안 된다" 하나뿐이다.**
  사전 선언한 개입 실험이 필요하다.

계기 수정은 C21로 등재했다. **콤 발생 시점도 좁혔다**: 50 step 간격 사이드카에서 추론 경로가
step 500 −0.24 → step 550 **+4.76**(M-legacy) / **+6.63**(M-matched), 즉 linguistic 단계 시작 후
50 step 안이다. (앞선 판에 "K-A 사이드카"로 적은 것은 오기다. K-A/K-B는 500 step 간격이라 550
지점이 없다.)

한계: 단일 seed · 1,500 step · 폴리시 미진입 · 청취 없음. **재구성 경로에는 양성 대조가 없다** —
릴리스 체크포인트에 `enc_q`가 없어(`FRESH_PREFIXES`) 두 런 모두 posterior를 새로 초기화했으므로,
"이 디코더로 실물을 콤 없이 재구성하는 posterior가 존재한다"는 증거는 아직 없다.

### 3.4 경로 진단 준비 (2026-09-06) — **진행 중**

청취 판정이 문제를 다시 규정했다. 1,500 step 두 arm은 링잉이 얹힌 음성이 아니라 **음성이 없는
링잉**이고, 10k control만 "어색하지만 말"이었다. 즉 지금 실패하고 있는 것은 잔여 잡음 제거가
아니라 **말소리 형성**이다. 다음 학습을 정하기 전에, 기존 체크포인트만으로 같은 문장에서 두
경로를 갈라 본다.

1. **재구성**: 실물 → `enc_q` → `dec` (학습이 디코더에 주는 sampled `z`)
2. **추론**: 음소 → `enc_p`/`dp`/`flow` → `dec`

두 축을 분리해 읽는다. **콤은 측정**(스크린 세 값 + 절대 PSD), **말소리 성립 여부는 청취**로
판정하며, 품질 척도의 1("사람 목소리로 들리지 않는다")과 2("말이지만 어색하다") 사이가 그
경계다. 자연성 점수 차이는 보조 정보이고 두 단계 차이를 필수조건으로 삼지 않는다.

**측정 완료 (2026-09-06)** — 상세 `inflect-work/evals/diag/path/FINDING.md`,
원시값 `path.json`·`path.csv`(760행), 청취 페이지 `listening/path-20260906/`.

재현이 먼저 통과했다. `prior_latent`+`dec`는 배포 경로 `model.infer`와 **비트 동일**(0.00e+00)이고,
control@10k 재구성은 2×2의 L-posterior × D-control 셀을 **0.0000 dB**로 재현한다(12/12).

격자 톤 초과, 40문장 중앙값(실물 −0.13):

| 체크포인트 | recon-sampled | recon-mean | infer |
|---|---:|---:|---:|
| matched@500 | **+4.54** | +9.45 | **−0.20** |
| legacy@500 | **+3.78** | +5.73 | **−0.20** |
| matched@1000 | +11.15 | +11.64 | +10.36 |
| matched@1500 | +8.55 | +8.82 | +7.68 |
| legacy@1500 | +8.96 | +9.38 | +6.03 |
| control@10000 | +8.11 | +8.38 | +8.12 |

- **P1 성립**: 추론@500은 −0.20 dB, +1 dB 초과 1/40. 예측대로 릴리스 사전 수준이다.
- **P2: 재구성@500은 이미 울린다.** 같은 체크포인트·같은 문장·릴리스 그대로의 디코더인데
  추론은 깨끗하고(0/40이 +4 초과) 재구성은 울린다(matched 33/40, mean 40/40).
  **기록은 "warm-up 종료 시점에 콤이 재구성 경로에 이미 존재한다"까지다.** 릴리스에 `enc_q`가
  없어 양성 대조가 없으므로 500 step 미수렴과 설계 결함을 이 실험은 구분하지 못한다.
- **P6**: `recon-mean`이 `recon-sampled`보다 나쁘고 그 격차는 step 500에서만 크다(matched +4.68 dB,
  38/40). 표기는 "step 500 출력은 샘플링에 민감하다"까지이며 **분산·KL 처방으로 쓰지 않는다.**
- **사후 관측**: f0가 93.75 Hz에 잠기는 것은 1,500 step arm에만 나타난다(recon 34/40, infer 39/40).
  청취가 "말로 들린다"고 한 10k control은 실물과 같은 360 Hz대이고 잠김 0/40인데 콤 초과는
  +8.1로 비슷하다. **초과 크기는 말소리 성립과 무관해 보이고 f0 잠김이 그것과 정합한다**는
  가설이 서지만, 계기를 청취 대체물로 쓰지 않는다.

**청취 판정 (2026-09-06)** — `listening/path-20260906/`, 0038 한 문장 8트랙, −24.00 dBFS 공통
목표(편차 0.0000 dB), catch 0 옵션 단계, 미응답 0.

| 시스템 · 경로 | 초과 dB | f0 잠김 | 품질 | 언어 | **말소리** |
|---|---:|---:|:--:|---|:--:|
| 실물 녹음 | −0.13 | 0/40 | 5 | 예 | **O** |
| matched@500 **추론** | −0.20 | 0/40 | **2**(결함 0) | 판단 어려움 | **O** |
| matched@500 재구성 | +4.54 | 0/40 | 1 | 아니오 | X |
| matched@1500 추론 | +7.68 | **39/40** | 1 | 아니오 | X |
| matched@1500 재구성 | +8.55 | **34/40** | 1 | 아니오 | X |
| control@10k 추론(기채점) | +8.12 | 0/40 | **2** | **예** | **O** |
| control@10k **재구성** | +8.11 | 0/40 | **2** | **예** | **O** |

**결과는 전제를 바꾼다. 네 가지.**

1. **콤의 크기는 말소리 성립을 예측하지 않는다.** control@10k(+8.1)는 말로 들리고 한국어로
   인식됐는데, matched@1500(+7.7 / +8.6)은 같은 크기의 콤으로 말소리가 아니다.
   **"콤을 줄이는 문제"와 "말소리를 만드는 문제"는 분리해서 다뤄야 한다.**
2. **재구성 경로의 양성 대조가 생겼다.** control@10k 재구성이 말소리로 판정되면서, 이 디코더가
   이 목소리의 posterior 잠재를 알아들을 수 있는 말로 렌더할 수 있음이 확인됐다(콤은 심한 채로).
   **디코더 구조가 말소리 형성을 막는다는 가설은 약해진다.** 다만 "콤 없는 재구성"의 대조는 없다.
3. **step 500의 추론은 콤 없는 말소리였고(품질 2·결함 0, "영어권 남성이 읽는 느낌"),
   1,500 step에서 그것을 잃었다**(품질 1, 가장 뭉개진 트랙). **linguistic adaptation이 작동하던
   말소리 경로를 망가뜨렸다.**
4. **f0 잠김은 말소리 실패와 강하게 붙지만 필요조건은 아니다.** 잠긴 2/2가 말소리 아님이고,
   잠기지 않은 5 중 4가 말소리다. 예외 matched@500 재구성은 "'에'가 연속으로 울린다"로,
   무작위 초기화 500 step posterior의 미수렴으로 읽히며 1,500 arm의 실패와 다른 현상이다.

사전 선언 분기 중 **4번**에 해당한다. 다음 조사는 **step 500과 10,000 사이에서 무엇이 성립하고
1,500에서 무엇이 무너졌는가**이며, 두 경로가 동시에 f0를 93.75 Hz에 내주는 것이 그 표지다.
**두 문장 확인 (2026-09-06) — 분기 재현.** `listening/path-confirm-20260906/`, 0004(최고음역
472 Hz) · 0015(휴지 최다), 핵심 후보 5트랙 + catch, 공통 목표 −27.73 dBFS, catch 0 옵션 단계.
세 문장 전부 같은 구조다: matched@500 추론은 **결함 0의 말소리 3/3**(자연성 2·3·2). 언어 축은
"판단 어려움"·"판단 어려움"·"아니오"로 **한국어로 인식되지는 않았다** — 0004의 자유기술 "매우 어색한
한국어 발음을 하는 남성"은 그 한 문장의 기술이고, 0038에는 "한국어처럼 들리지는 않았다"가 함께 있다.
matched@1500 두 경로는 말소리 아님(품질 1,
`most_blurred` 2/2), control@10k 재구성은 콤이 심한 말소리(품질 2, 한국어 예).

후속 설계에 쓸 수 있는 결론(3/3 문장): (1) 콤의 크기와 말소리 성립은 별개다. (2) **릴리스 사전은
한국어 음소에서 이미 결함 없는 말소리를 만들고, linguistic adaptation의 첫 1,000 step이 그것을
없앤다.** 한국어 발화·내용 이해는 별개 축이며 기준선에서 확인되지 않았다. (3) 이 디코더는 이 목소리의
posterior 잠재를 말로 렌더한다.

**저장된 체크포인트 궤적 (2026-09-06, 새 학습 없음)** — `evals/diag/path/trajectory.csv`(3,120행).
5개 런의 모든 체크포인트에서 두 경로의 f0 잠김(격자 편차 < 1.5 Hz, 40문장)을 잤다. 셀 = 잠김 recon/infer.

| step | M-matched | control | K-A(adv 3000까지 0) | K-B(adv 계속 0, recon 폴리시) | E2(디코더 영구 동결) |
|---:|:---:|:---:|:---:|:---:|:---:|
| 500 | 0/0 | · | · | · | · |
| 1000 | 39/40 | 40/40 | **0/0** | 40/40 | 40/40 |
| 2000 | · | 13/38 | 35/40 | 37/40 | 34/40 |
| 3000 | · | 27/38 | 36/40 | 39/40 | 37/40 |
| **4000** | · | **1/0** | **2/0** | 40/40 | 35/39 |
| 6000 | · | 0/0 | 1/0 | 40/40 | 34/39 |
| 10000 | · | 0/0 | 0/0 | 5/32 | · |

세 가지가 읽힌다. **(1) 붕괴는 모든 런에서 linguistic 단계 첫 500 step 안에, 디코더가 동결된 채
일어난다.** adversarial을 3000까지 차단한 K-A만 1000에서 미잠김이고 2000에는 잠긴다. 다만 **같은
차단 조건의 K-B는 1000에서 40/40 잠김**이므로(K-B는 `stft_loss_weight`도 다르다) "차단이 ~1,000 step
지연시킨다"는 **단일 관측이고 재현되지 않았다.** 확인되는 것은 adversarial이 붕괴의 **필요조건이 아니라는**
것뿐이다. **(2) 즉시·완전한 회복은 "학습 가능한 디코더 × adversarial ON"에서만
일어났다.** control·K-A(디코더 학습 + adv ON)는 4000에서 즉시 해제, K-B(디코더 학습 + adv OFF)는
6000까지 40/40 잠김이고 7,000 step 학습 뒤에도 infer 32/40, E2(디코더 동결 + adv ON)는 끝까지 잠김.
다만 JA control은 해제 전에 부분 회복했고 E1은 3,000 step에 걸쳐 풀렸으므로 이 둘을 회복의
**필요조건**이라고는 쓰지 않는다. **(3) 콤 초과는 내내 +6~+12로 남는다.** 회복 뒤에도 +8이다.
pitch 회복과 콤 제거는 별개 현상이고 후자는 어느 런에서도 일어나지 않았다.

f0 잠김은 대리지표다. 표의 결정 지점(control@4000·K-A@1000·K-B@10000·E2@6000·control@3000)은 청취된
적이 없어 `listening/path-unlock-20260906/`(0038, 7트랙, 사전 선언 T1–T5)를 만들었다. **판정 전에는
표의 "해제"를 "말소리 회복"으로 읽지 않는다.** **JA E1(디코더 500 해제, adv ON)은 학습 가능한 디코더로도 1000에서 40/40 잠긴다**(다른 화자,
측정만, `trajectory-ja.csv`). 동결 디코더는 붕괴의 원인이 아니고, K-A로 adversarial도 아니다.
**붕괴는 시도된 모든 구성에서 linguistic 단계 첫 500 step 안에 일어나는 그 단계 자체의 성질**이며,
모든 런이 공유하고 어느 런도 바꾸지 않은 조건은 `posterior_init=fresh` · `posterior_warmup_steps=500` ·
linguistic lr 0.5 × 1e-4 · `kl_loss_weight` 1.0이다. 후속 제한 런은 이 중 하나만 바꾼다(별도 계획).
**새 장기 학습은 그 뒤 별도로 승인받는다.**

### 3.5 W1500 — posterior warm-up 스케줄 짧은 탐색 런 (2026-09-06) — **실패(막지 못함) · 준비도 조작 불성립** — *재해석: 관측한 초반 구간의 말소리 미성립*

상세: `inflect-work/runs/W1500-2026-09-06.md`. 2,000 step 한 arm, GPU 10분 47초. 시험하는 것은
**"ADAPT 진입을 늦추는 warm-up 스케줄이 ADAPT+500까지 말소리를 보존하는가"**이며 결론은 그 범위 안에 둔다.
warm-up 하나를 바꿔도 판별기 학습 이력·ADAPT 첫 배치(epoch 406 step)·옵티마이저 상태가 함께 달라지므로
"posterior 준비도" 자체의 시험이 아니다. **기전을 확정하지 않고 다른 개선책을 제외하지 않는다.**

무결성 전부 통과: 옵션은 허용된 세 키만 다름, `loss_g` 전 구간 유한, **W1500@1500 추론이 matched@500 추론과
40문장 max\|diff\| 0.000e+00**(동결 경로 무손상 실증), 비결정성 3.5e-2(기존 control↔E2 쌍 4.8e-2와 동급),
기존 `trajectory.csv`·`trajectory-ja.csv` sha 불변(새 tag `-warmup`).

**계기 둘이 서로 다른 답을 낸다.** 손실 창은 posterior가 더 나아졌다고 하고(`loss_mel` 1400–1500 **0.8774** 대
matched 400–500 1.0825, `loss_kl` 7.33 대 7.95), f0 잠김은 **warm-up 도중 step 1000에 재구성이 이미 콤에
잠겼다**고 한다(39/40, f0 93.7 Hz) — linguistic·decoder가 **전부 동결**인 구간이다. **fresh posterior가 그
자체로, 다른 어떤 것도 학습하지 않는 동안 콤에 수렴한다**는 관측은 이번이 처음이다.

ADAPT 오프셋 비교(유일하게 유효):

| | W1500 | matched |
|---|---|---|
| ADAPT 진입 재구성 | @1500 +5.20 dB / 93.8 Hz / **32/40 잠김** | @500 +4.54 dB / 245 Hz / **0/40** |
| ADAPT+500 추론 | @2000 +5.27 dB / **40/40** | @1000 +10.36 dB / 40/40 |
| ADAPT+500 재구성 | @2000 +6.99 dB / 33/40 | @1000 +11.15 dB / 39/40 |

진입 시점 상태가 **뒤바뀌었다**. 잠김은 계기이므로 판정은 청취가 했다.

**청취 판정** — `listening/warmup-20260906/`, 3문장 × 5시스템 + catch(0 옵션 단계), 공통 −27.03 dBFS. 문장별
ⓐ 진입 직전 재구성 · ⓑ W1500 추론 · ⓒ 비교군 추론이 **세 문장 전부 자연성 1**(말소리 아님), 결함 2, 언어 아니오.
`most_blurred`는 matched@1000 infer 3/3("ringing만"). 사전 선언 규칙: **ⓐ 0/3 → G1 실패**("준비도 조작이 성공하지 않아
가설을 시험하지 못했다; 추가 1,000 step은 준비도를 확보하지 못했다"), **ⓑ=X ∧ ⓒ=X 3/3 → "시험한 warm-up 1,500 스케줄은
이 실행에서 ADAPT+500의 말소리 실패를 막지 못했다"**. (*2026-09-06 재해석: 이 '실패'는 **관측한 초반 구간(ADAPT+500)의 말소리
미성립**이며 계속 학습 시 실패를 뜻하지 않는다 — JSUT도 1k에서 자연성 1이었다가 20k에서 4가 됐다.*) matched@1000 infer는 처음 청취돼
말소리 아님으로 확인. 자유기술은
W1500 트랙에 "일부 음소가 템포에 맞추어 들림"이라는 음소적 흔적을 적었으나 분류를 바꾸지 않는다. 시험하지 않은 길이·
다른 축·콤에는 결론이 없고, gating·polish·proximal·lr·KL 어느 것도 제외되지 않는다.

**후속 설계의 입력이 되는 새 사실**: fresh posterior는 linguistic 단계 없이, posterior-only 학습만으로 1,000 step 안에 콤에
잠긴다(계기 39/40 + 1500 재구성 청취 말소리 아님). matched의 500 step posterior가 잠기지 않은 것은 더 일찍 멈췄기 때문일 수
있다. 다음 후보(posterior-only 단계의 adv/fm 차단, posterior lr, inherit, 현 코드 W500 재현·추가 seed)는 **별도 계획**.

### 3.6 JSUT 기준점과 posterior 계승 (2026-09-06) — **기준점 확립 · 1,000은 JSUT도 말소리 아님 · 계승 처방은 관측 구간의 미성립을 막지 못함**

상세: `inflect-work/runs/JSUT-BASELINE-2026-09-06.md`(A·C), `runs/S2-INHERIT-2026-09-06.md`(B).
짧은 런 3개(각 2,000 step), 학습 합계 ≈ 32분. **기전 확정도, 다른 개선책 배제도 없다.**

최근 진단이 실패한 설정 내부만 봤다는 지적을 받아, **잘 됐던 JSUT stage 1을 분석의 중심으로** 되돌렸다.
JSUT는 규약 청취를 거친 적이 없어(판정 파일 없음) 기준점부터 고정한다.

**A1 — JSUT는 20,000 step 어디서도 콤에 잠기지 않는다.** 40문장 양 경로에서 잠김 0–5/40, 초과 +1~+4 dB,
f0 204–222 Hz. 같은 계기로 step 1000을 나란히 놓으면:

| step 1000 | recon 초과 / f0 / 잠김 | infer 초과 / f0 / 잠김 |
|---|---|---|
| **JSUT** | +2.78 / 205 Hz / **0/40** | +3.24 / 208 Hz / **0/40** |
| KO control | +8.48 / 93.8 / **40/40** | +6.97 / 93.7 / **40/40** |
| KO M-matched | +11.15 / 93.8 / 39/40 | +10.36 / 93.7 / 40/40 |

**A3 — stage 2(JSUT export → Arona, fresh posterior)는 세 번째 양상**이다. 1,000에서 13–16/40 부분 잠김 뒤
7,000에 회복(0–4/40)하고 f0가 254 → 341 Hz로 Arona 실물(354 Hz)을 향해 오른다. 세 조건은 데이터량·음역·
시작점이 함께 다르므로 어느 축의 효과인지 말하지 않는다.

**C — 현행 코드로 JSUT 초반 재현.** 옵션은 허용된 두 키만 다르고 나머지 9개는 원본 시대에 없던 옵트인 키가
전부 기본값. replay@500 추론은 릴리스 사전 렌더와 **0.000e+00**. replay는 750–1250에서 f0가 480–543 Hz로
치솟았다가 1,500부터 내려와 **2,000에서 원본 2,000과 거의 같아진다**(초과 +2.28/+1.83 대 +1.80/+1.66,
f0 220/217 대 204/207, 잠김 0/40). 계기 관측이며 **말소리·품질은 청취가 정한다.**

**B — posterior 계승 여부만.** 같은 체크포인트를 `--model-name`까지 맞춰 재-export해 `model.pth`가
**바이트 동일**(`231efbfc…`), sidecar 100텐서가 학습된 `enc_q`와 **max\|Δ\| 0**, 옵션 diff는 `posterior_init`
한 키, 두 arm의 동결 구간 추론이 **0.000e+00**. 계기에서 계승 arm이 일관되게 낮다:

| | fresh | inherit |
|---|---|---|
| 진입 직전(500) 재구성 초과 / 잠김 | +12.06 / 9/40 | **+7.56 / 7/40** |
| ADAPT+500(1000) 추론 초과 / 잠김 | +8.35 / **19/40** | **+6.51 / 9/40** |
| ADAPT+1500(2000) 추론 초과 / 잠김 | +8.14 / **19/40** | **+6.54 / 10/40** |

동결 구간(250·500)의 추론은 두 arm이 동일하고 **+1.36 dB·f0 221 Hz로 깨끗하다** — JSUT가 학습한 사전이
Arona의 일본어 텍스트를 콤 없이 렌더한다. 두 arm 모두 linguistic 단계가 시작된 750부터 +6~+8.6으로 오른다.

**청취 판정 (2026-09-06, 페이지 2장, 화자별, catch 0 옵션 단계, 미응답 0)**

*페이지 1 (JSUT 화자)*: **JSUT@20000은 추론·재구성 모두 3/3에서 자연성 4·결함 1·언어 예** → **규약상 기준점 확립**,
기준 품질 4·1(비공식 "링잉 약간만"과 일치). **JSUT@1000은 추론·재구성 모두 3/3 자연성 1** → "1,000은 JSUT도 말소리
아님". Context의 전제("JSUT는 1,000에서 이미 말소리")는 **철회**한다. 계기에서 JSUT@1000은 잠김 0/40·f0 205 Hz로
실패 런과 범주적으로 달랐지만 청취는 말소리가 아니었다 — **"잠기지 않음"은 말소리의 충분조건이 아니다**(두 번째
반례; 첫째는 KO matched@500 재구성). 실패 런과 JSUT의 1,000 차이는 "말소리 대 기계음"이 아니라 **"잠기지 않은
비-말소리 대 잠긴 비-말소리"**다. replay@1000도 3/3 자연성 1 → **C는 판단 불가**(원본도 X). 같은 등급 안에서
청취자는 replay를 더 흐리다고 기술했다(`most_blurred` 3/3, "남성 목소리로 웅얼거리는 발음만" 대 원본 "의도한 음소가
약간 느껴짐") — **초반 replay가 더 나쁘게 들렸다는 탐색적 신호**. 코드 퇴보 확정 근거는 아니고, replay@2000의 계기 회복이 청취
회복을 뜻하지도 않는다. **1k의 말소리 미성립이 최종 실패를 예측하지 못한 사례가 있으므로(JSUT)**, 최근 1.5–2k 런은 "관측한 초반
구간에서 말소리 미성립"으로 서술한다. JSUT@1000의 F0 차이(205 Hz 대 93.75 Hz)는 **음향 궤적의 차이**로만 읽는다.

*페이지 2 (JA Arona 화자)*: 세 행(재구성@500·추론@1000·추론@2000) 모두 fresh X ∧ inherit X가 ≥ 2/3 →
**"posterior 계승만으로 관측 구간(≤ 2,000)의 말소리 미성립을 막지 못함."** 이것은 **계승이라는 처방의 실패**이며 **초기화가
원인으로 기여했을 가능성은 남는다** — 계승 직후에 괜찮았는지, warm-up 중 나빠졌는지, 처음부터 Arona 음성을 처리하지 못했는지는
미구분(§3.7이 묻는다). 예외는 최고음역 문장 0036 하나에서 계승 arm만 1,000·2,000 모두 자연성 2·"판단 어려움" — 전체 성공 기준에는
미달하지만 **효과가 전혀 없다는 결과는 아니다**. 계기의 일관된 차이(진입 직전 재구성 초과 −4.5 dB, ADAPT+500 잠김 19 → 9/40)는
**2/3 문장의 말소리 차이로 이어지지 않았다** — "계기 개선 ≠ 청취 개선"의 또 한 사례. 판별기·옵티마이저 계승·데이터량·음역·더 긴
학습은 미시험이고 배제되지 않는다.

### 3.7 말소리 성립 시점과 전환에서 잃은 것 (2026-09-07) — **JSUT 말소리 형태 2k(언어 인식 4k) · 적응 직전(JSUT 목소리)의 추론 출력이 관측 구간에서 저하**

상세: `inflect-work/runs/JSUT-BASELINE-2026-09-06.md`(페이지 A), `runs/S2-INHERIT-2026-09-06.md`(페이지 B).
**새 학습 없음** — 기존 궤적 음원 재사용 + 학습 없는 측정 하나(JSUT 최종 체크포인트로 Arona 재구성).

**페이지 A(JSUT 화자, catch 복제쌍 불일치 0)** — 문장별 자연성·결함:

| 트랙 | 0004 | 0025 | 0038 | O(≥2) |
|---|:--:|:--:|:--:|:--:|
| @2000 infer | 1·2 | 2·2 | 2·2 | 2/3 |
| @2000 recon | 2·2 | 3·1 | 3·1 | **3/3** |
| @3000 infer(해제 직전) | 2·2 | 2·2 | 2·1 | 3/3 |
| @4000 infer(해제 직후) | 4·2 | 3·2 | 3·2 | 3/3 |
| @8000 infer | 4·1 | 4·2 | 4·2 | 3/3 |
| @20000 infer | 5·1 | 4·1 | 5·1 | 3/3 |

문장별 지속 성립 N_s = 3,000(0004) · **2,000**(0025) · **2,000**(0038) → **N = 2,000**, 그리고 2,000은 이 페이지의 가장 이른
관측점이므로 정확히는 "**step 2,000 또는 그 이전**"이다. 0004의 2k 미성립은 그대로 기록한다(미성립으로 재분류하지 않는다).
**품질은 그 뒤로 계속 오른다**(자연성 중앙값 2 → 2 → 3 → 4 → 5, 결함 2 → 1): **말소리 성립과 품질 완성은 다른 시점이다.**
**같은 업데이트 수(2,000 step)에서 서로 다른 양상** — JSUT는 말소리, KO·Arona 런은 미성립. 그러나 2,000은 JSUT replay에서 epoch 3, S2에서
epoch 14이고 시작 가중치도 다르므로 **학습 길이 가설은 배제되지 않는다.** 데이터량·목표 음역도 함께 다르므로 어느 축의 효과인지 말하지 않는다.
**언어 축은 별개**: "일본어 예" 3/3의 첫 관측점은 4,000(3k는 3/3 판단 어려움, 3k–4k 미관측). 디코더 해제(3,000) **전에 이미 말소리**였고
해제 직후 자연성이 뛰는 관측이 있으나 **디코더 인과는 확정하지 않는다.** 원본@2000이 말소리로 확인됐으므로 **다음 라운드에서
replay@2000 비교가 처음으로 유효해진다.**

**페이지 B(JA Arona 화자, catch 복제쌍 불일치 1등급; 라운드 간 재청취에서 공통 파일 9개 중 3개가 말소리 경계를 넘어 바뀜 — 1 대 2 단일 판정은 불안정)**:

| 트랙 | 0029 | 0036 | 0040 | O |
|---|:--:|:--:|:--:|:--:|
| **infer@500**(동결 JSUT 사전 → Arona 텍스트) | 3·1 | 4·1 | 4·1 | **3/3** |
| inherit@2000 infer(적응 후) | 2·2 | 1·2 | 1·2 | 1/3 |
| step-0 recon(학습 0) | 2·2 | 1·2 | 1·2 | 1/3 |
| inherit@250 / @500 recon | 3·2 | 1·2 | 1·2 | 1/3 |
| fresh@500 recon | 1·2 | 1·2 | 1·2 | 0/3 |

- **B-loss 2/3 → "ADAPT 직전에는 말소리였고 같은 문장에서 적응 후 사라졌다."** 동결 사전은 Arona 텍스트를 자연성 3~4·결함 1의
  말소리로 읽는다(3/3). 2,000 step 적응 뒤 같은 문장이 자연성 1이 된다. 0029는 말소리 유지되나 품질 악화(3·1 → 2·2).
  **적응 직전(JSUT 목소리로 Arona 텍스트를 읽은) 추론 출력이 관측 구간에서 저하됐다** — 같은 문장·같은 라운드에서 확인된 첫 사례.
  Arona 음색·감정을 획득했다가 잃은 것이 아니며, 영구 망각·회복 불가는 확인되지 않았다.
- **B-step0 2/3이 `X→X→X` → "계승 posterior의 재구성 실패는 warm-up 이전부터 있었다."** 바닥 점수라 warm-up의 추가 악화 여부는
  미판정이고, 재구성은 posterior+디코더 결합 시험이라 posterior 단독 귀속 불가. 계기의 "step 0이 250·500보다 2.7 dB 깨끗"은 청취로
  이어지지 않았다. 화자 불일치인지 일반화 한계인지는 정하지 않는다.
- 0029에서 계승 재구성 3·2 대 fresh 1·2 — 단일 문장·재청취 불일치가 있는 라운드의 관측, 독립 재현 아님.

**다음에 볼 것(후보 추가, 좁힘 아님)**: linguistic 단계의 무엇이 있던 능력을 지우는가; 대상 데이터와 posterior의 관계.
판별기·옵티마이저 계승·데이터량·음역·학습 길이는 그대로 남는다. 기전 확정 없음.

**청취 부담**: 세 라운드가 비슷한 경향을 보여 평가가 반복됐다. 이후 라운드는 **세트 수와 세트당 트랙 수를 줄여** 총 청취 수를
낮춘다(다음 라운드부터: 페이지 1장, 문장 2개, 트랙 ≤ 5 + catch).

### 3.8 모듈 복원 — 어느 추론 모듈의 변화가 출력 저하에 기여했는가 (2026-09-07~08) — **검사한 두 문장군 모두에서 저하 확인, 모듈 귀속 미결**

상세: `inflect-work/runs/S2-INHERIT-2026-09-06.md`. **새 학습 없음** — 적응된 체크포인트에서 추론이 쓰는 모듈만 적응 전 텐서로 되돌려
렌더했다(`scripts/module_restore.py`). 변형마다 @2000을 새로 로드해 복원 그룹은 @500과·나머지는 @2000과 `torch.equal`로 검사하고,
무복원 대조는 기존 @2000 추론과, 전체 복원은 적응 전 추론과 각각 **max\|diff\| 0**이었다.

**페이지 1(JSUT 화자, 원본 텍스트, catch는 세 축 일치·교체해도 분류 불변)**:

| 조건 | 0025 | 0038 |
|---|:--:|:--:|
| 적응 전(JSUT@20k = S2@500) | 5·1·예 | 5·1·예 |
| 적응 후 @2000 | **2·2·아니오** | **2·2·아니오** |
| `flow←500` | 3·2·예 | 3·2·판단 어려움 |
| `encp+dp←500` | 2·2·판단 어려움 | 3·2·판단 어려움 |

- **손상 확인 성립**(−3, −3): **원본 JSUT 텍스트에서도 관측 구간에 큰 자연성 저하**. Arona 텍스트만 어려워진 것이 아니다. 이 페이지에서는
  언어 응답도 예 → 아니오로 실제 악화돼 언어 인식 저하가 함께 성립한다. **자연성 2만으로 "말소리 완전 소실"이라고 쓰지 않는다.**
- **모듈 귀속은 둘 다 미결**: flow 이득 **+1, +1**, enc_p+dp **0, +1**(기준 두 문장 모두 ≥ 2). **미결을 "효과 없음"으로 읽지 않는다** — flow는
  두 문장에서 양수이고 0025에서 언어가 아니오 → 예로 바뀌었으며 자유기술도 복원이 적응 후보다 낫다. 복원 후보 모두 **결함 2가 남아**
  적응 전(1)으로 돌아오지 못했다. 따라서 "flow가 원인이니 동결"로 넘어갈 근거는 부족하다.
- 계기(양쪽 텍스트): `none` +6.5/+6.0, `flow` +3.3/+2.5, `encp+dp` +5.4/+4.9, `all` +1.4/+1.0 dB — 순서는 같으나 계기가 청취 회복을
  뜻하지 않는다. 자유기술의 공통 관측은 "음색 피치가 올라가고 음소 명료도가 나빠졌다"이며 계기의 f0 상승(220 → 265~281 Hz)과 방향이 같다.
- **이 실험이 구분하지 못하는 것**: ADAPT 구간에서 posterior·판별기도 학습했다. 어느 **추론 모듈**의 변화가 기여했는지는 좁힐 수 있으나
  **KL이 유발했는지 판별기가 유발했는지는 구분하지 못한다.** 복원은 함께 학습된 적 없는 혼합 상태다.

**페이지 2(Arona 화자, 목표 텍스트, 11클립) 판정 완료 (2026-09-08)** — `listening/restore-arona-20260907/`. 같은 라운드 안에 적응 전·후·복원
둘을 넣어 라운드 밖 참조를 쓰지 않았다. **catch(적응 후 복제, 0036행)는 일치하지 않았다**: 원본 1·2·아니오, 복제 2·2·판단 어려움 —
자연성 1등급(말소리 경계 1↔2) 불일치. 이 불일치는 **그 복제쌍의 것**이며 라운드 잡음 바닥이 아니다. 아래는 원본 점수이고 괄호는 복제 점수다.

| 조건 | 0036 | 0040 |
|---|:--:|:--:|
| 적응 전(infer@500, 동결 JSUT 사전이 Arona 텍스트를 읽음) | 4·1·예 | 5·1·예 |
| 적응 후 @2000 | **1·2·아니오** (복제 2·2·판단 어려움) | **1·2·아니오** |
| `flow←500` | 3·2·예 | 3·2·예 |
| `encp+dp←500` | 2·2·예 | 2·2·판단 어려움 |

- **손상 확인 성립**(−3(복제 −2), −4; 복제 점수로도 불변): **목표 텍스트에서도 관측 구간에 큰 자연성 저하**. 두 문장 모두 언어 응답이 실제로
  악화돼(예 → 아니오; 0036은 복제로도 예 → 판단 어려움) 이 페이지에서도 언어 인식 저하가 함께 성립한다. **적응 후의 말소리 성립 여부는 이
  페이지로 정하지 않는다** — 0036이 원본 1·복제 2로 경계를 넘어 갈렸고 0040의 1은 단일 판정이다.
- **flow 복원: 미결 — catch 의존.** 이득 **+2(복제 +1), +2**. **원본 점수로는 두 문장 모두 기준(≥ 2) 도달**이나 0036에서 복제 점수를 쓰면 분류가
  바뀌므로 그 비교는 미결이다. 서술: 두 문장 모두 양수이고 **페이지 1(+1, +1)보다 크며**(합산 아님, 나란히 적는다) 언어가 두 문장 모두
  아니오 → 예로 바뀌었다. 다만 자유기술은 적응 전 대비 "어조가 달라지고 ringing이 심해졌다"고 적었다. **"효과 없음"도 "flow가 기여했다"도 아니다.**
- **enc_p+dp 복원: 미결**(**+1(복제 0), +1**; 복제 교체로도 미결 불변). 복원 후보 **둘 다 결함 2 잔존**(페이지 1과 같다).
- **영역 비교**: 두 페이지 모두 손상 확인 → "**검사한 두 문장군 모두에서 관측 구간의 큰 자연성 저하**"(범위는 그 문장군; 언어 축은 각 페이지에서
  실제 악화가 있었으므로 각각 적는다). 복원 이득 방향은 같으나 **페이지마다 두 문장 모두 ≥ 2가 기준이며 합산해 통과로 쓰지 않는다.**
- 사전 선언 분기상 **"큰 회복 확인"은 성립하지 않는다** → 모듈 귀속을 미결로 남기고 **JSUT 동일 데이터 재시작 대조**(대상 데이터를 바꾸지 않아도
  같은 재시작 조건에서 나빠지는가)를 별도 계획으로 검토한다. flow 복원 신호가 지금까지 가장 크고 그 미결이 catch 한 쌍의 1등급 불일치에 걸려
  있으므로, 새 문장 2개로 flow 복원만 다시 듣는 소형 확인 페이지가 **사후 탐색 후보**로 남는다(사전 판정과 분리, 별도 승인).

**청취 페이지 검증 기준 정정**: 빌더는 float32로 읽어 공통 RMS 게인을 걸고 PCM16으로 다시 쓰며 그 변환이 절단이라, "원본 × 게인"과의 절대
오차는 최대 1 LSB(3.05e-05)까지 정상이다. 출처 검증은 **원본 sha 불변 + 동일 변환 경로 재현 시 바이트 일치**로 한다.

### 3.9 동일 데이터 재시작 대조 — 데이터를 바꾸지 않아도 저하되는가 (2026-09-08~09) — **이 조건에서는 저하가 관측되지 않았다**

상세: `inflect-work/runs/C1-JSUT-RESTART-2026-09-08.md`. **2,000 step 짧은 런 1개**(≈10분). S2-inherit이 한 번에 바꾼 두 가지 중
**대상 데이터 변경만 제거**했다 — 같은 base export, 같은 `--posterior-init inherit`, 같은 스케줄로 **JSUT 데이터에** 다시 적응한다.
`--dataset` 한 개만 다르며 코드 시대도 같다. 묻는 것은 "**대상 데이터를 바꾸지 않아도 재시작·추가 학습 조건에서 출력이 나빠지는가**"
하나이고, **재시작 자체를 독립 원인으로 분리하지 않는다**(재시작과 그 뒤 2,000 step이 한 묶음).

무결성은 범용 검사의 임계가 아니라 **엄격 검사**(`scripts/exact_match.py`)로 잡았고 전부 통과했다: 두 `training-options.json`이
**모든 키·값 일치**, `prepared_dataset` 3개 sha가 대상 데이터셋과 일치하며 baseline과는 전부 다름, base 체크포인트 sha 동일, 계승 리포트
3항(`sidecar`·100텐서·`verified_equal_after_copy`), **@500 추론 40/40 비트 동일**(warm-up 동안 추론 경로가 lr 0 + `requires_grad_(False)`이므로
성립해야 하는 등식), 손실 2,000행 전부 유한.

계기(서술): 같은 JSUT 텍스트 40문장에서 이 런의 추론은 2,000 step 내내 콤 초과 **+1 dB 대**·f0 **220 Hz**·잠김 **0~1/40**에 머물고,
S2-inherit@2000은 같은 문장에서 **+6.00 dB**·**280.9 Hz**·**10/40**이었다. **이 차이는 계기이며 말소리 성립도 품질도 뜻하지 않는다** —
계기 선호가 청취로 이어지지 않은 사례가 여러 번, 미잠김이 말소리가 아니었던 반례가 두 번 있었다. **판정은 청취로만 한다.**

**청취 판정 (2026-09-09)** — `listening/restart-jsut-20260908/`, JSUT 화자·JSUT 텍스트, **처음 쓰는 문장 0012·0031**, 9클립,
축 응답 미기입 0(자유기술 두 칸은 비어 있다). catch(주질문 트랙 `jsutdata` 복제)는 자연성 **1등급** 갈렸으나(5 대 4) **어떤 분류도 바꾸지 않았다.**

| 조건 | 0012 | 0031 |
|---|:--:|:--:|
| 적응 전(base export 추론) | 4·1·예 | 5·1·예 |
| **JSUT 데이터 적응 후 @2000** | **5·1·예** (복제 4·1·예) | **4·1·예** |
| Arona 데이터 적응 후 @2000 | **1·2·아니오** | **1·2·아니오** |

- **행 ① 미결 — 저하 관측되지 않음**: 이득 **−1**(복제 0) · **+1**, 두 문장 모두 1등급 이내. 자연성이 적응 전과 같은 등급대(4~5)에 있고
  **결함도 1로 유지**되며 일본어도 두 문장 모두 예다. 기록은 "**이 조건에서는 저하가 관측되지 않았다**"까지 — **"재시작은 무해하다"로 넓히지
  않는다**(이 대조는 자기 학습 데이터를 다시 도는 조건이고 계승 posterior가 그 데이터에 맞아 있다). 재시작과 추가 학습은 분리하지 않았다.
- **행 ② 성립(관측만)**: 이득 **+4**(복제 +3) · **+3**. "이 두 런에서, 같은 base·같은 계승·같은 스케줄로 **Arona 데이터에 적응한 쪽이 두 문장
  모두 자연성 ≥ 2등급 낮았다**." **"데이터 변경이 기여했다"로 적지 않는다** — 데이터량·화자·음역·정렬과 계승 posterior 정합이 한 묶음이다.
- **행 ③ 성립**: 적응 전 − Arona 데이터 = **+3 · +4** → 페이지 1의 손상 확인을 **새 문장에서 재확인**. 같은 라운드 안이라 행 ①의 저하 부재는
  청취자 표류로 설명되지 않는다. 언어 축은 그 arm에서만 예 → 아니오로 실제 악화(이번 라운드에서 `예` → `판단 어려움`은 미결로 못박았다).
- **여전히 미결**: **"데이터가 원인"도 "데이터는 무관"도 아니다.** 기전 확정 금지(KL·판별기 미구분). 2,000 step은 관측 창이고 최종 상태가 아니다.
  청취자 1인·문장 2개. **모듈 귀속은 미결, flow는 우선 검토 후보 유지.**
- 이번에는 계기와 청취가 같은 방향이었다. 그 동시발생이 계기를 판정 근거로 승격시키지는 않는다.
- *각주 2026-09-12: 이 행은 **서로 다른 두 런의 학습된 가중**을 비교한다. 같은 명목 설정으로 재실행해도 학습된 부분이 달라진다는 것이
  2026-09-12에 확인됐고(W2 대 S2-inherit의 step 500에서 `enc_q` 0/100 동일·max|Δ| 9.4e-03, 판별기 0/111·5.0e-02; 동결 부분은 410/410
  비트 동일), **그 변동의 크기는 측정되지 않았고 원인도 분리되지 않았다.** 판정 라벨은 관측 점수의 산술이라 그대로지만, 이 행의 이득을
  읽을 때 그 변동이 함께 들어 있다는 사실을 적어 둔다. 자세한 것은 `docs/TROUBLESHOOTING.md`.*

다음 후보(별도 계획): ① flow 복원 재확인(새 문장 2개, 학습 없음) — **2026-09-09에 사용자 결정으로 ①을 먼저 진행**(§3.10),
② 데이터 변경 묶음을 쪼개는 대조(데이터량·음역·화자·정렬·계승 정합).

### 3.10 라운드 E — flow 복원 재확인, 새 문장 2개 (2026-09-09) — **다시 미결(+1, +2) · flow 축 반복 청취 종료**

상세: `inflect-work/runs/S2-INHERIT-2026-09-06.md`(라운드 E 절). 페이지 2에서 `flow←500`의 이득은 원본 점수로 +2·+2로 기준에 도달했으나
`adapted2k` 복제쌍이 **1 대 2**로 갈려 분류가 바뀌었고, 그래서 미결이었다. 설계가 아니라 **복제쌍 하나의 1등급 불일치**에 걸린 미결이므로 같은 렌더
조건에서 **문장만 바꿔 한 번 더** 듣는다. **사용자 결정: 재확인은 한 번뿐이며, 다시 미결이면 반복 청취를 멈추고 데이터 전환 조건 분해로 넘어간다.
이번 결과만으로 장기 학습이나 모듈 동결을 선택하지 않는다.**

문장은 **계기 항이 없는 규칙**으로 청취 전에 고정했다: 렌더 범위 0001–0040, JA Arona 앵커 판정 페이지의 `mapping.json` rows 전부 제외,
실물 길이 3–6 s, 감탄·의성어만인 문장 제외, 인덱스가 가장 앞선 둘 → **0002·0003**(0001은 6.403 s로 탈락). 파이프라인 예행 페이지
(`listening/rehearsal`, `page_key rehearsal-not-a-verdict`, 판정 파일 없음)는 판정 라운드가 아니므로 제외 집합에 넣지 않았다.

렌더는 `module_restore.py --variants none flow all --out-subdir restore-e`로 새 디렉터리에 썼고 무결성 두 줄 모두 `max|diff| 0`,
함께 렌더한 0036이 봉인본과 **바이트 동일**, 봉인 sha 7개 불변이다. 계기는 서술로만 적는다(0002 `none` +6.16 / `flow` +3.19,
0003 +7.67 / +4.57 dB).

페이지 `listening/restore-flow-20260909/`는 실물 / 적응 후 / `flow←500` + **`adapted2k` 복제(두 행 모두)** = **8클립**이다.
적응 전 트랙은 없으므로 **이 페이지는 0002·0003의 "적응 전 대비 저하"를 확립하지 않는다** — 판정문에서 "저하에 기여"·"원래 품질로 돌아왔다"를
쓰지 않는다. 복제를 `adapted2k`에 둔 이유는 기대 이득 3 − 1이 문턱(2) 위에 정확히 놓여 분류가 그 트랙의 1↔2 판독에 걸리고, 그 트랙이 바로
여러 번 갈린 트랙이기 때문이다. 사전 규칙: 이득 ≥ 2가 두 문장 모두일 때만 "**flow의 변화가 적응 후 출력의 낮은 자연성에 기여했다**",
`adapted2k` 원본·복제 모두 ≥ 4이면 **미시험**(한쪽만 ≥ 4이면 미결), 언어 축은 `판단 어려움` 포함 전이를 먼저 미결로 빼고 읽으며, 복제 교체로
**분류가 바뀔 때만** 미결이다. **`flowback`은 단일 평가이므로 반복 평가 변동을 확인하지 못하고, catch가 확인하는 안정성은 `adapted2k`의
원본·복제 교체에 한정된다** — 이 꼬리표를 판정문에 싣는다.

**청취 판정 (2026-09-09)** — 축 응답 미기입 0(자유기술 두 칸은 비어 있다).

| 조건 | 0002 | 0003 |
|---|:--:|:--:|
| 적응 후(`adapted2k`) | **1·2·아니오** (복제 1·2·아니오) | **1·2·아니오** (복제 1·2·아니오) |
| `flow←500`(`flowback`) | **2·2·판단 어려움** | **3·2·예** |
| 이득 | **+1**(복제로도 +1) | **+2**(복제로도 +2) |

- **flow 복원: 미결.** 기준(두 문장 모두 ≥ 2) 미달이다. 부호는 두 문장 모두 양수이고 0003은 단독으로 기준을 넘었다.
  **이번 미결은 catch에 걸린 것이 아니다** — 복제쌍이 두 문장에서 **세 축 모두 정확히 일치**(불일치 0단계)했고 복제 점수로 계산해도 이득이
  같다. 0002의 실제 이득이 +1인 것이 미결의 이유다. **미결을 "효과 없음"으로 쓰지 않는다.**
- `adapted2k`가 두 문장 모두 1이므로 **미시험이 아니다** — flow 축을 시험했고 결과가 미결이다. `flowback`은 두 문장 모두 자연성 ≥ 2이지만
  0002의 차이는 1↔2 한 단계라 대비로 읽지 않는다. 결함은 `flowback`도 **2·2**로 남고, 적응 전 트랙이 없으므로 "원래 품질로 돌아왔다"는 쓰지 않는다.
- 언어 축: 0002 `아니오` → `판단 어려움` = 미결, 0003 `아니오` → `예` = 회복. 두 문장 모두가 아니므로 페이지 수준 언어 회복은 쓰지 않는다.
- **페이지 2(+2(복제 +1)·+2)와 합산하지 않는다.** 네 문장에서 부호는 모두 양수지만, 복제 유지 조건에서 두 문장 모두 ≥ 2를 넘긴 페이지는 없다.
- **규칙이 덮지 못한 것**: `flowback`은 단일 평가이므로 반복 평가 변동을 확인하지 못했고, catch가 확인한 안정성은 `adapted2k` 원본·복제 교체에
  한정된다. 0002의 +1과 0003의 +2 차이가 문장 차이인지 평가 변동인지는 이 라운드가 구분하지 못한다.
- **사전 분기 적용**: "다시 미결" → 사용자 결정에 따라 **flow 축의 반복 청취를 종료**하고 **데이터 전환 조건 분해 대조**로 넘어간다.
  모듈 귀속은 미결로 남기며, **flow 갱신 제한 학습 대조는 지금 선택하지 않는다**(기여 확인 분기 불성립). 장기 학습·모듈 동결도 선택하지 않는다.
  다음 후보(설계는 별도 계획): 계승 posterior 정합 · 데이터량(JSUT를 Arona 규모로 줄인 대조) · 음역 · 화자 · 정렬.

**모듈 귀속은 미결 그대로이고 flow는 우선 검토 후보로 유지한다** — 페이지 2에서 flow 복원은 두 문장 모두 자연성 3·일본어 "예"였다.

### 3.11 라운드 F — 친숙한 JSUT를 Arona 규모로 줄여 재시작 (2026-09-09) — **축소해도 큰 저하가 관측되지 않았다**

상세: `inflect-work/runs/F1-JSUT-SMALL-2026-09-09.md`. 라운드 E에서 flow 복원이 다시 미결이어서 **flow 축의 반복 청취를 끝내고**
데이터 전환 조건 분해로 넘어왔다. 첫 후보가 이 대조다 — 전체 JSUT에서 품질이 유지된 **C1이 비교군으로 이미 있고**, 같은 화자·언어·계승
posterior를 유지한 채 규모만 Arona에 맞출 수 있다. **2,000 step 짧은 런 1개.**

**질문은 "데이터량만이 원인인가"가 아니다** — step을 고정하면 자료량과 함께 같은 자료를 다시 보는 횟수, 부분집합 구성이 함께 바뀐다
(`max_steps`는 step 기준, `training.py:1117`). 그래서 묻는 것은 "**친숙한 JSUT도 적은 자료를 반복 학습하면 저하되는가**"이고
**"자료량이 원인"이라고 쓰지 않는다.**

| arm | 자료 | 통과 수 |
|---|---|---|
| C1 | 전체 JSUT 6,959행 / 8.901 h | 2.3 (epoch 3) |
| S2-inherit | Arona 1,170행 / 1.519 h | 13.7 (epoch 14) |
| **R1**(이 런) | **축소 JSUT 1,170행 / 1.508 h** | **13.7 (epoch 14)** |

부분집합은 `scripts/make_subset.py`로 만들었다: `prepared/ja-jsut-v1`을 복사해 **`train.jsonl`만** 교체하고
`validation.jsonl`·`val-real.jsonl`·`symbols.json`·`audio/`는 **바이트 그대로** 두었다(측정 40문장과 학습 중 감시 클립이 완전히 동일해진다).
`prepare`는 다시 돌리지 않았다 — 걸러낸 manifest로 재준비하면 검증 split의 크기·구성과 오디오 파일명이 함께 바뀌어 한 축 조작이 아니게 된다
(`prepare.py:157-189`, `:79-85`). 추출은 **하위 코퍼스 일곱 개 접두 일치로 층화 + 비례·큰-나머지 배분 + `random.Random(20260909)` 한 스트림**으로
못박았다(같은 시드에서도 이 선택들이 실현 시간·음소 집합을 바꾼다). 실측: **1,170행 · 1.508 h · 평균 4.64 s**, 층 비율 편차 최대 **0.034 pp**,
고유 음소 문자 **36 → 33**(빠진 `!`·`?`·`ʷ`가 **측정 40문장에 하나도 없음**). `inflect-adapt audit --strict` **통과**.

무결성은 범용 검사의 임계가 아니라 **엄격 검사**(`exact_match.py --dataset-axis train`)로 잡았고 전부 통과했다: 두 `training-options.json`
**완전 일치**, `train_jsonl`·`dataset_json` 해시 **다름**, `validation_jsonl`·`symbols` 해시 **같음**, base 체크포인트 sha 동일, 계승 리포트 3항,
**R1@500 추론이 C1@500과 40/40 비트 동일**(warm-up 동안 추론 경로가 동결이므로 성립해야 하는 등식), 손실 2,000행 전부 유한.
`--dataset-axis` 추가 시 `all` 축의 심볼 해시 기대값을 잘못 넣어 라운드 D 재검사가 실패했고, **두 축 모두 "같음"이 맞다**(모든 데이터셋이
released 178심볼 인벤토리를 그대로 선언한다)로 고친 뒤 라운드 D 재검사가 통과했다.

계기(서술): 두 arm이 사실상 같은 자리다 — C1@2000 추론 +1.08 dB · 220.3 Hz · 0/40, R1@2000 **+1.07 dB · 217.8 Hz · 0/40**.
같은 텍스트에서 S2-inherit@2000은 +6.00 dB · 280.9 Hz · 10/40이었다. **계기 선호를 청취 결과로 읽지 않는다.**

청취 페이지(봉인, 판정 대기): `listening/small-jsut-20260909/` — JSUT 화자·JSUT 텍스트, **처음 쓰는 문장 0002·0003**,
실물 / 적응 전 / 전체 JSUT / 축소 JSUT + **주질문 트랙(`smalldata`) 복제 두 행** = **10클립**. 사전 규칙: ① 적응 전 − 축소 ≥ 2(두 문장) →
"친숙한 자료라도 줄여 반복하면 큰 저하"(**행 ③이 'C1 유지'일 때만**), ② 두 규모 비교는 **관측만** 적고 자료량에 귀속하지 않음, ③ 적응 전 − 전체가
두 문장 모두 \|이득\| ≤ 1이면 C1 유지, ≥ 2 두 문장이면 두 arm 모두 저하로 규모 축소를 구분 못 함, 한 문장만 ≥ 2 또는 어느 문장이든 ≤ −2면 미결.
**척도 여유**: 기준 항이 낮아 ≥ 2 대비가 산술적으로 불가능한 문장은 **미시험**(미결로 세지 않는다). **미달을 "유지"로, 미결을 "효과 없음"으로 쓰지
않는다.** 이 대조가 함께 움직이는 것은 **자료량·반복 횟수·부분집합 구성** 셋이다 — **기학습 여부는 이 비교에서 고정**이며(전체 JSUT와 축소 JSUT는
둘 다 base가 이미 학습한 자료다) 기학습 대 미학습의 차이는 **축소 JSUT ↔ Arona 대비**에만 남는다(그 대비는 이 라운드에 없고, 둘 다 일본어라 언어도
바뀌지 않는다). 다만 부분집합이 base가 이미 학습한 코퍼스에서 나왔다는 사실은 **일반화 한계**로 남는다 — 저하가 없어도 **"적은 새 자료를 반복해도
무해"로 넓히지 않는다.**

**청취 판정 (2026-09-09)** — 처음 쓰는 문장 0002·0003, 10클립, 축 응답 미기입 0. **catch(축소 arm 복제, 두 행)는 두 문장에서 세 축 모두 일치**
(불일치 0단계)이므로 어떤 분류도 복제에 걸려 있지 않다.

| 조건 | 0002 | 0003 |
|---|:--:|:--:|
| 적응 전 | **3·2·예** | **4·1·예** |
| 전체 JSUT @2000 | 4·1·예 | 4·1·예 |
| 축소 JSUT @2000 | **4·1·예** (복제 4·1·예) | **4·1·예** (복제 4·1·예) |

- **행 ① 미결 — 저하 관측되지 않음**: 이득 **−1, 0**. 두 문장 모두 1등급 이내이고 결함(1)·언어(예)도 적응 전 이상이다. 기록은 "**이 런·이 스케줄·이
  문장군에서는 큰 저하가 관측되지 않았다**"까지 — **"유지"로 쓰지 않는다.** 두 문장 모두 적응 전 ≥ 3이므로 **미시험이 아니다**(시험됐고 미결이다);
  다만 0002는 적응 전이 3이라 "큰 저하"가 성립하려면 축소 arm이 자연성 1까지 떨어져야 했다.
- **행 ② 미결(관측)**: 이득 **0, 0** — 두 규모가 두 문장 모두 **같은 자연성(4)·같은 결함(1)·같은 언어(예)**를 받았다. **"자료량이 원인/무관"으로 적지 않는다.**
- **행 ③ (a) 성립**: 적응 전 − 전체 JSUT = **−1, 0** → "전체 JSUT의 유지가 새 문장 2개에서도 관측됨" → 행 ①·②를 위 문구대로 읽는다.
  0002에서는 **적응 전이 결함 2**로 두 arm보다 나쁘게 읽혀 상단 기준이 아니었다.
- **여전히 열려 있는 것**: 이 대조는 **자료량·반복 횟수·부분집합 구성** 셋을 한 묶음으로 움직였다(**기학습 여부는 고정** — 두 arm 모두 base가 이미
  학습한 자료다). 부분집합이 그 코퍼스에서 나왔다는 것은 **일반화 한계**이므로 이 결과를 **"적은 새 자료를 반복해도 무해"로 넓히지 않는다**.
  기전 확정 금지. 부분집합 추출 1회, 청취자 1인, 문장 2개.
- 계기와 청취가 같은 방향이었으나(두 arm이 +1.08 대 +1.07 dB) 계기를 판정 근거로 올리지 않는다.
- **사전 분기 적용**: 세 조건(행 ③ = (a) · 행 ① 이득이 두 문장 모두 ≤ 0 · 복제 교체로 불변)이 모두 성립 → **Arona의 음성 특성과 계승 posterior
  정합으로 초점을 옮긴다**(후보: 계승 posterior에 Arona warm-up만 더 준 arm, 음역 정규화, 화자 요인 — 설계는 별도 계획). 자료량 축은 **"무관"으로
  닫지 않고** 미분해 항목과 함께 열어 둔다. flow 귀속은 미결 유지.
- *각주 2026-09-12: 이 행은 **서로 다른 두 런의 학습된 가중**을 비교한다. 같은 명목 설정으로 재실행해도 학습된 부분이 달라진다는 것이
  2026-09-12에 확인됐고(W2 대 S2-inherit의 step 500에서 `enc_q` 0/100 동일·max|Δ| 9.4e-03, 판별기 0/111·5.0e-02; 동결 부분은 410/410
  비트 동일), **그 변동의 크기는 측정되지 않았고 원인도 분리되지 않았다.** 판정 라벨은 관측 점수의 산술이라 그대로지만, 이 행의 이득을
  읽을 때 그 변동이 함께 들어 있다는 사실을 적어 둔다. 자세한 것은 `docs/TROUBLESHOOTING.md`.*

### 3.12 라운드 G — 계승 posterior에 Arona warm-up을 1,000 step 더 준 스케줄 (2026-09-10) — **두 행 모두 사전 기준 미달, 가설 미시험**

상세: `inflect-work/runs/W2-JA-WARMUP1500-2026-09-10.md`. 라운드 F가 사전 분기 세 조건을 충족해 **Arona의 음성 특성·계승 posterior 정합**으로
넘어온 첫 arm이다. **3,000 step 런 1개.** 시험하는 것은 **"ADAPT 진입을 늦추는 스케줄"**이며 **"posterior 준비도" 자체가 아니다** — 이 플래그는
**warm-up 중에도 학습하는 판별기**(`training.py:406-407`)의 이력, 판별기 lr(경계에서 약 1% 낮다), 데이터 스트림·RNG·GradScaler 상태를 함께 움직인다.
생성기 lr은 경계에서 명목값으로 재설정되므로 두 arm의 ADAPT 첫 업데이트에서 같고(posterior 1e-4 · linguistic 5e-5), linguistic·decoder는 두 arm
모두 cold start다.

**W1500의 반복이 아니다**: 그 라운드는 **fresh posterior · ko-arona-v1b(`arona_kr`, 다른 성우)** 에 ADAPT+500이었고, 이 arm은
**inherit · ja-arona-v2(`arona`)** 에 ADAPT+1500이다. (warm-up 길이 × posterior 초기화 × 화자) 격자에서 **`inherit × 1500`은 어느 화자로도
실행된 적이 없다.** `max_steps` 3000은 **ADAPT 업데이트 수를 1,500으로 같게** 두기 위한 최소 길이이며(총 step을 맞추면 ADAPT가 500회로 줄어든다),
`decoder_unfreeze_step` 3000과 겹쳐도 **decoder 단계 업데이트는 0회**다(최종 stage `linguistic_adaptation`, 첫 ADAPT step 1501 → 1,500회 확인).

무결성은 엄격 검사로 잡았고 **ok 줄 열한 개**가 전부 통과했다: 바뀐 **옵션 키 집합과 값**이 `posterior_warmup_steps=1500`·`max_steps=3000`과 정확히
일치, 데이터 축이 움직이지 않음(데이터셋 3해시 + `symbols_sha256` 전부 baseline과 같음), base 체크포인트 sha 동일, 계승 리포트 3항,
**W2@500·@1000·@1500 추론이 각각 S2-inherit@500과 40문장·전부 유한·`array_equal` 40/40**, 손실 3,000행 유한. 이번 라운드용으로 `exact_match.py`에
`--options-allow KEY=VALUE`(키만이 아니라 **값**까지)·`--dataset-axis none`·`--steps`/`--baseline-step`(비대칭 step)을 더했고, 렌더 줄이
**문장 수·유한성·`array_equal`을 한 줄에서** 확인한다 — `warmup_gates.py --compare-infer`의 max\|diff\| 출력은 차이가 NaN이면 0으로 남을 수 있어
기준으로 쓰지 않는다.

계기(서술): 두 arm의 **경계 재구성이 거의 같은 자리**다(+7.56 대 +7.70 dB, 잠김 7/40 대 10/40), ADAPT+1500 끝에서도 재구성 +8.46 대 +8.91,
추론 +6.54 대 +7.62다. W2의 추론은 @1500까지 +1.36 dB로 완전히 일정해 동결을 직접 확인한다. 경계 직전 100 step의 손실 창은 mel 0.9881 대 0.9774,
kl 3.2167 대 3.5254이며 **손실 감소를 준비의 근거로 쓰지 않는다**(W2 자신의 400–500 창이 baseline과 사실상 같다).

청취 페이지(봉인, 판정 대기): `listening/warmup2-20260910/` — JA Arona, **처음 쓰는 문장 0006·0019**(전부 기계적인 규칙: 판정 페이지 id 제외 ·
실물 3–6 s · **음소 토큰 ≥ 25** · 인덱스 앞선 둘), 실물 / 경계 재구성 기준·긴 warm-up / ADAPT+1500 추론 기준·긴 warm-up + **`reconbase` 복제 두 행**
= **12클립**. 복제를 기준 재구성에 둔 이유는 그 트랙의 **같은 바이트 파일이 두 라운드에서 1·아니오와 3·예로 갈린 기록**이 있고 척도 여유 게이트의
기준항이기도 해서다. 사전 규칙: **행 A**(재구성 이득 ≥ 2 두 문장) → "진입 시점의 재구성을 개선했다", 미달은 **"사전 조작 성립 기준 미달"**로 적고
**"개선되지 않았다"로 바꾸어 읽지 않는다**; **행 B**(추론 이득 ≥ 2 두 문장) → "동일 ADAPT+1500에서 긴 warm-up의 추론 자연성이 기준보다 두 문장 모두
≥ 2등급 높았다"(적응 전 추론이 페이지에 없으므로 **"저하가 줄었다"로 쓰지 않는다**), 미달은 미결이며 **"줄지 않았다"로 단정하지 않는다**.
네 칸을 모두 사전 고정했고 **A를 B의 필수 조건으로 두지 않는다** — A 미달·B 성립이면 "재구성의 큰 개선은 확인하지 못했지만 추론의 큰 개선은
관측됐다. posterior 준비도에 의한 설명은 확립되지 않았다"로 적는다. 척도 여유: `reconbase` 원본·복제 **모두 ≥ 4 → 행 A 미시험, 한쪽만 → 미결**;
`inferbase` ≥ 4 → 행 B 미시험. 어느 행이든 미시험이면 결과 후 분기를 자동 적용하지 않는다. **"준비도가 레버다/아니다"는 어느 결과에서도 쓰지 않는다.**

**판정 (청취 2026-09-12, 축 미기입 0, 12클립)**

- 처음 쓰는 문장 0006·0019. 실물은 두 문장 모두 **5 · 0 · 예**.
- **행 A**(경계 재구성): `reconbase` **2 · 2 · 아니오**(복제도 2 · 2 · 아니오) → `reconlong` **2 · 2 · 아니오** — 이득 **0, 0** →
  **사전 조작 성립 기준 미달**. 부호로는 "그 점수에서 차이 없음"이며 **"개선되지 않았다"로 바꾸어 읽지 않는다**.
- **행 B**(동일 ADAPT+1500 추론): `inferbase` **1 · 2 · 아니오** → `inferlong` **1 · 2 · 아니오** — 이득 **0, 0** → **미결**.
  **"줄지 않았다"·"효과 없음"으로 단정하지 않고**, 적응 전 추론이 이 페이지에 없으므로 **"저하가 줄었다"도 쓰지 않는다**.
- **catch**: `reconbase` 원본·복제가 두 문장 모두 **세 축 정확히 일치(0단계)** → 어떤 분류도 복제에 걸리지 않는다(그 복제쌍에 한정된 사실).
- **척도 여유**: `reconbase` 2·2, `inferbase` 1·1로 전부 < 4 → **두 행 모두 미시험이 아니다**(≥ 2등급 개선이 척도상 가능했다).
- **결합 해석(사전 고정한 네 칸 중 A 미달 ∧ B 미달)**: **"이 스케줄로 준비된 posterior에서 ADAPT를 시작한다는 가설은 시험하지 못했다."**
  **B 미달은 가설 기각이 아니다.** 사전 규칙이 이 문구의 선례로 W1500을 들지만 **같은 실험으로 묶지 않는다**(fresh · KO `arona_kr` · ADAPT+500).
- 말소리 성립(≥ 2)은 재구성 세 조건 성립 · 추론 두 조건 미성립이며 **1↔2 단일 판정으로 대비하지 않는다**(자연성 2를 "말소리 완전 소실"로도 쓰지 않는다).
  결함은 합성 네 조건 모두 **2**, 언어는 재구성 쌍·추론 쌍 모두 **아니오 → 아니오 = 변화 없음**(`판단 어려움` 0칸)이고 실물만 예.
  강제 선택은 `most_natural` 실물 2/2 · `most_blurred` `inferbase` 2/2이지만 **판정 축이 아니다**(두 추론 트랙의 자연성이 같은 1이다).
- 계기(경계 재구성 +7.56 대 +7.70 dB, 손실 창 0.01 수준)와 청취가 같은 방향이었으나 **계기를 판정 근거로 올리지 않는다**.
  - *각주 2026-09-12: 0.01은 **mel 창에만** 해당한다(0.9881 → 0.9774). **kl 창은 3.2167 → 3.5254로 늘었다.** kl 증가를 실패의 원인으로 읽지는 않되, 두 손실을 뭉쳐 적지 않는다.*
- **사전 분기**: 가설은 **미시험**으로 남는다. 다음 후보는 조작을 성립시키는 다른 손잡이(**더 긴 warm-up · 낮은 posterior lr · 미실행 W1500g 셀**
  = warm-up 중 adv/FM 차단) 중에서 **별도 계획으로** 고른다. **"준비도가 레버다/아니다"·"≤ 1,500 warm-up 기각" 금지**, 기전 확정 금지,
  재구성의 posterior 단독 귀속 금지. 자료량 축은 열린 채, flow 귀속은 미결로 유지한다. 이 결과로 장기 학습·모듈 동결을 선택하지 않는다.
- **이번 처방의 실용적 결과(2026-09-12 보강)**: "가설 미시험"은 **조작이 확인되지 않았다**는 뜻이고, **warm-up을 늘리는 처방 자체는 시험했다.**
  이번 500→1,500 연장은 두 문장에서 재구성·추론의 사전 기준 큰 개선을 만들지 못했고, 최종 추론은 **자연성 1 · 일본어 '아니오'로 사용 가능한
  발화로 회복되지 않았다**. 더 준비된 posterior로 ADAPT를 시작했을 때의 효과는 **미확인**이다. 따라서 **이 처방을 채택할 근거가 없고 같은
  방식의 추가 연장 우선순위를 낮출 근거**가 되지만, **모든 warm-up 길이의 효과를 부정하지는 않는다.** 경계 재구성의 자연성 2는 규약상
  "말소리 성립"이되, 같은 트랙의 **결함 2 · 언어 '아니오' · "기계 음성 같은 ringing"** 과 함께 놓으면 **"준비 완료"로 해석할 근거는 없다.**
- **런 대 런 변동(2026-09-12 확인)**: 이 라운드의 두 행은 **서로 다른 두 런의 학습된 가중**을 비교한다. 같은 명목 설정으로 재실행해도 학습된
  부분이 달라진다(W2 대 S2-inherit의 step 500에서 `enc_q` 0/100 동일·max|Δ| 9.4e-03, 판별기 0/111·5.0e-02; 동결 부분은 410/410 비트 동일).
  **그 변동의 크기는 측정되지 않았고 원인도 분리되지 않았다.** 미달·미결 라벨은 관측 점수의 산술이라 그대로지만, **이득 0을 "변동이 작다"의
  근거로 쓰지 않는다.** 자세한 것은 `docs/TROUBLESHOOTING.md`.

### 3.13 라운드 H — warm-up 동안만 생성기 adv/FM 가중을 0으로 (2026-09-12) — **두 행 모두 사전 기준 미달**

상세: `inflect-work/runs/W3-JA-WARMUPADV-2026-09-12.md`. 라운드 G가 두 행 모두 미달로 끝나 사용자 권고에 따라 **단순 연장보다 손실 구성을
바꾸는 대조**로 옮겼다. **저장소 코드를 바꾼 첫 라운드다.** 기존 `adversarial_gating`은 램프가 `decoder_unfreeze_step`에 고정돼 있어 W2의
일정(unfreeze 3000 · max_steps 3000)에서 켜면 **전 구간이 0**이 된다(K-A 로그가 step 1–3000 전부 0.0으로 보여준다). 그래서
**`posterior_warmup_steps`에 고정된 두 번째 게이트** `warmup_adversarial_gating`을 더했다 — warm-up에서 0, ADAPT에서 1.0, **판별기 정책
무변경**, 기본값 off, 두 플래그는 합성된다(커밋 `0dff25f`).

**기본 경로 불변 게이트를 런보다 먼저 통과**했다: 변경 전후 20 step(CPU · `--no-amp` · seed 1234)의 `metrics.jsonl`이 **20/20 바이트 동일**
(`a5ed6c1a…`), `training-options.json`은 **새 키 하나만** 추가되고 다른 값 변화 0, pytest **397 통과·1 스킵**(370에서 증가), ruff **68 불변**.
플래그를 켠 20 step은 행 1–10이 advW 0.0·adv/FM null·`loss_d` 유한, 행 11부터 1.0으로 사전 예측한 오프바이원과 일치했다.

런 `W3-ja-warmupadv-20260912`는 W2와 **새 키 하나만** 다르다(나머지 36키는 값까지 같다). 엄격 검사 **ok 줄 열한 개** 전부 통과 —
W2 JSON에 없던 키를 `Ellipsis` 센티넬이 "변경됨"으로 잡아 **값 검사가 직렬화까지 증명**하므로 `exact_match.py`는 **고치지 않았다**.
데이터 축 무이동(4해시 동일), base sha 동일, 계승 3항, **W3@500·@1000·@1500 추론이 W2@1500과 40문장·유한·`array_equal` 40/40**,
손실 3,000행 유한. **개입 증명은 렌더 차이가 아니라 로그로** 했다: 행 1–1500 전부 `adversarial_weight 0.0` · adv/FM **null** · `loss_d` 유한,
**행 1501**이 1.0 · 두 항 float · `linguistic_adaptation`. 저장소 코드를 바꾼 뒤 다시 렌더한 **W2의 0036이 봉인 sha와 바이트 일치**
(recon `be17aa6d…`, infer `9f5a96fa…`)해 렌더 회귀도 없다.

**이 라운드의 전제로 기록한 새 사실**: 동일 설정 재실행에서 학습 결과가 변동한다(W2 대 S2-inherit의 step 500에서 `enc_q` 0/100 동일·
max|Δ| 9.4e-03, 판별기 0/111·5.0e-02, 동결 부분은 410/410 비트 동일). **크기는 미측정이고 원인도 분리하지 않았다.** 그래서 성립이 나오면
**"이 개입이 원인"으로 쓰지 않고**, 채택 전에 **W2 반복(첫 확인, 충분조건 아님) → W3 반복(개선 재현)** 두 단계를 별도 계획으로 요구한다.

계기(서술): 경계 재구성이 **거의 같은 자리**(+7.70 대 +8.08 dB, 잠김 10/40 대 12/40), ADAPT+1500 끝의 추론도 +7.62 대 +7.52다.
두 arm의 추론은 @1500까지 +1.36 dB로 완전히 일정해 동결을 직접 확인한다. 손실 창은 **mel과 kl을 각각** 적는다 — 경계 직전 100 step이
W2 mel 0.9774 · kl 3.5254, W3 mel 0.9653 · kl 3.2064이고, **warm-up 구간의 `loss_g`는 두 arm을 비교할 수 없다**(W3는 정의상 두 항이 빠져 있다).
**손실 감소를 준비의 근거로 쓰지 않는다.**

청취 페이지(봉인, 판정 대기): `listening/warmupadv-20260912/` — JA Arona, **처음 쓰는 문장 0020·0022**(라운드 G와 같은 기계적 규칙; 다만
0026이 24토큰으로 한 개 차이로 탈락해 **임계 민감도가 달라졌고**, 선택 결과는 하한 18–25에서 불변이다), 실물 / 경계 재구성 기준·개입 /
ADAPT+1500 추론 기준·개입 + **`reconbase` 복제 두 행** = **12클립**. 사전 규칙: **행 A**(재구성 이득 ≥ 2 두 문장)는 성립해도
**"이 개입이 개선했다"가 아니라 관측형**으로 적고, **행 B**는 적응 전 추론이 없으므로 "저하가 줄었다"로 쓰지 않는다. **A 미달 ∧ B 미달**은
**"어느 행도 두 문장 모두에서 ≥ 2등급 기준을 충족하지 못했다. 문장별 이득은 각각 기록한다"** 로 적는다 — 이득 +2, +1도 두 문장 조건에는
미달이므로 "어느 문장도 ≥ 2에 닿지 않았다"로 쓰면 한 문장의 큰 개선을 지운다. 척도 여유는 **문장 단위**로 보고 우선순위는
**미시험 > 판정 불가 > 성립·미달**이다.

**판정 (청취 2026-09-12, 12클립, 축 미기입 7)**

- 처음 쓰는 문장 0020·0022. 실물은 두 문장 모두 **5 · 0 · 예**.
- **축 미기입 7칸**(앞선 네 라운드는 0): **0022의 언어 축 다섯 칸 전부**와 **0022의 결함 축 두 칸**(`inferbase`·`recongate`).
  자연성은 12트랙 전부 기입돼 **행 A·B는 그대로 판정**된다. **미기입을 `아니오`·`판단 어려움`으로 대신 읽지 않는다** —
  사전 규칙이 미기입을 다루지 않으므로 그 칸은 미기입으로만 적는다.
- **행 A**(경계 재구성): `reconbase` 0020 **2**(복제 **1**) · 0022 **1**(복제 1) → `recongate` 0020 **2** · 0022 **1**.
  이득 **0, 0**(복제 기준 **+1**, 0) → **사전 조작 성립 기준 미달.** "개선되지 않았다"로 바꾸어 읽지 않는다.
- **행 B**(동일 ADAPT+1500 추론): `inferbase` 1 · 1 → `infergate` 1 · 1, 이득 **0, 0** → **미결.**
  "줄지 않았다"·"효과 없음" 금지, 적응 전 추론이 없어 "저하가 줄었다"도 쓰지 않는다.
- **catch가 0020에서 1등급 갈렸다**(원본 2 대 복제 1, **말소리 경계를 넘는 불일치**; 0022는 0단계 일치). 다만 **행 A의 분류는
  어느 쪽으로 계산해도 미달**이라 바뀌지 않으므로 **행 A는 판정 불가가 아니다** — 복제쌍이 갈린 것 자체는 사유가 아니다.
  **말소리 성립의 `reconbase`@0020 칸만** 성립↔미성립이 뒤집혀 **판정 불가**다.
  - *각주 2026-09-12: 0022는 **자연성·결함만 일치(0단계)** 이고 **언어 축은 원본·복제 양쪽 모두 미기입**이었다 — "0단계 일치"를 세 축 일치로 읽지 않는다.*
- **척도 여유**: `reconbase` 2·1·1·1, `inferbase` 1·1로 전부 < 4 → **두 행 모두 미시험도 판정 불가도 아니다**(실제로 시험됐다).
- **결합 해석(사전 고정)**: **"어느 행도 두 문장 모두에서 ≥ 2등급이라는 사전 기준을 충족하지 못했다. 문장별 이득은 각각
  기록한다."** — 이득 +2, +1도 두 문장 조건에는 미달이므로 "어느 문장도 ≥ 2에 닿지 않았다"로 쓰지 않는다. 가설 기각도 쓰지 않는다.
- **사용 가능한 발화로의 회복(따로 기록)**: `infergate`는 두 문장 모두 **자연성 1**, 0020에서 결함 2 · 언어 `아니오`,
  0022에서 결함 2 · 언어 미기입 → **이번 조건에서 사용 가능한 발화로 회복되지 않았다.**
- 결함은 기입된 합성 트랙이 전부 **2**, 언어는 0020 재구성 쌍이 `판단 어려움` 포함으로 **미결**·추론 쌍이 **변화 없음**,
  0022는 전부 미기입이라 읽지 못한다 → **페이지 수준 언어 진술 없음**. 강제 선택은 `most_natural` 실물 2/2이나 판정 축이 아니다.
- 자유 기술은 두 문장 모두 "발성이 거의 안 되고 ringing 아티팩트 기계음"이며, **"발성되는 음소의 캐릭터는 JSUT 화자에 가깝다"**
  는 관측이 붙었다. **목소리 정체성은 이 페이지의 판정 축이 아니므로**(안내문 명시) 기록만 하고 어떤 행의 판정에도 쓰지 않는다.
- 계기는 경계 재구성이 거의 같은 자리(+7.70 대 +8.08 dB)였고 손실 창은 mel 0.9774 대 0.9653 · kl 3.5254 대 3.2064였다.
  **계기를 판정 근거로 올리지 않는다.**
- **이번 개입에서 얻은 것(2026-09-12 보강 — 위 판정 문구는 그대로 둔다)**: 이 조건에서 warm-up 동안 **생성기의** adv/FM
  두 항을 0으로 두었을 때 `recongate`의 자연성은 0020에서 **2**, 0022에서 **1**이었고 행 A는 두 문장 모두 **사전 기준
  미달**이었다(0022의 결함·언어 축은 **미기입**이라 읽지 않는다). 따라서 **이 두 항을 끄는 처방을 채택할 근거는 없다.**
  다만 **판별기는 warm-up 내내 학습했고 ADAPT 진입 시점의 판별기 상태도 함께 달라졌으며**, ADAPT에서는 두 항을 다시
  켰으므로 **이후 추론 실패에서 adv/FM의 역할까지 배제한 것은 아니다.** "남았다"·"유지"·"개선되지 않았다"로 쓰지 않는다.
- **사전 분기**: A·B 모두 미달이고 어느 행도 미시험·판정 불가가 아니므로 다음 후보는 **posterior 학습률 하향**(별도 계획).
  **추가 warm-up 연장의 우선순위는 라운드 G 한 번의 미달 때문에 낮춘 채** 두되 **기각은 아니며**, **라운드 H는 두 arm의 warm-up
  길이가 1500으로 같아 길이 축의 근거로 세지 않는다.** "판별기가 원인"·"adv/FM이 원인/무관" 금지, 기전 확정 금지,
  재실행 변동이 미측정이므로 **이득 0을 "변동이 작다"의 근거로 쓰지 않는다.** flow 귀속 미결·자료량 축 열림 유지.

### 3.14 라운드 I — 계승 레시피를 디코더 해제 너머까지, 예산 상한 10,000 step (2026-09-12) — **청취 대기**

상세: `inflect-work/runs/I1-JA-RECOVER10K-2026-09-12.md`. 라운드 G·H가 모두 두 행 미달로 끝나 사용자 권고에 따라
**작은 설정 변경을 하나씩 시험하는 방식에서 벗어나** 질문을 하나로 모았다: **최종 추론이 실제로 사용 가능한 일본어 발화가 되는가.**
저장된 21런 중 **8런만 `decoder_polish` 행을 남겼고**, 닿지 못한 13런 중 12런은 `max_steps ≤ decoder_unfreeze_step`이었다
(예외는 `decoder_unfreeze_step: null`인 E2). **계승 다섯 런은 전부 그 12런에 속한다** — 계승 축이 아니라 `max_steps`가 짧아서다.
그래서 **`inherit` + `warm-up 1500` + 해제 너머**는 어느 화자로도 실행된 적이 없는 칸이었다.

**이미 실패한 선례를 함께 적는다**: `ja-arona-stage2-20260904`는 같은 화자·해제 3000·8000 step(해제 뒤 5,000)으로 돌았고
**G4 판정이 "쓸 수 없음"** 이었다. 그 런과 이 런은 **다섯 가지가 다르다** — posterior 초기화(fresh 대 inherit), warm-up(500 대 1500),
해제 뒤 길이(5,000 대 7,000), `validation_interval`(500 대 50, D5 이전에는 궤적을 바꾼다), 그리고 **코드 시대**(그 런은 D1 mel 수정
`0727f7d`(2026-09-05) **이전**). **`run-identity.json`은 코드 시대를 기록하지 않는다.** **D1을 G4 실패의 원인으로 읽지 않는다.**
이 라운드는 **유망한 처방이 아니라 공백 채우기**다.

**런이 두 번 끊겼다**(step 6500·8000, 둘 다 체크포인트 기록 순간의 호스트 저메모리; 커널 OOM 아님). 두 번 다 250 경계라 단계
체크포인트가 온전했고 **`metrics.jsonl`에 중복 행이 없다**(10,000행·고유 10,000·버린 행 0). 그러나 **체크포인트는 DataLoader의 난수
상태와 epoch 내 위치를 저장하지 않으므로**(`training_data.py:206-216`) **두 지점에서 데이터 순서가 끊겼다** — **이 런은 중단 없는
10,000 step이 아니며** 행 A·행 C의 두 체크포인트 사이에 그 불연속이 들어간다. 재개는 CLI를 직접 불러 고정 인자를 전부 다시 넘겼고
identity 검사를 통과했다.

무결성은 **ok 줄 열한 개** 전부 통과했다: 옵션 두 키(`max_steps=10000`·`warmup_adversarial_gating=false`)가 값까지 일치,
데이터 축 무이동, base sha 동일, 계승 3항, **I1@500·@1000·@1500 추론이 W2@1500과 40문장·유한·`array_equal` 40/40**, 손실 유한.
단계 진행도 선언대로다 — 첫 ADAPT 행 **1501**, 첫 `decoder_polish` 행 **3001**, 디코더 7,000행, 최종 stage `decoder_polish`,
전 구간 advW 1.0, 해제 뒤 `lr.decoder` 9.32e-06~1.00e-05(그 전은 0). 적응 전 트랙은 `module_restore.py`가 `max|diff| 0.0e+00 ok`를
찍었고 그 0036이 `I1-01500__infer`의 0036과 **바이트 동일**이다.

계기(서술): 해제 뒤 **f0 잠김이 풀린다**(3000에서 12/40 → 6000 3/40 → 8000·10000 **0/40**)이고 f0 중앙값이 278 → 355 Hz로 올라간다.
**콤 초과는 사라지지 않는다**(내내 +6.8~+9.9). 손실 창은 mel·kl을 각각 적는다 — 경계 직전 0.9790/3.4704, @10000 0.6151/1.5301.
epoch는 11 → 21 → **70**. **잠김 해제도 콤 감소도 손실 감소도 "회복"으로 읽지 않는다.**

청취 페이지(봉인, 판정 대기): `listening/recover10k-20260912/` — JA Arona, **처음 쓰는 문장 0023·0024**(같은 기계적 규칙, 토큰 하한
18–34에서 불변; 0023은 말 끊김과 외침을 포함한다고 청취 전에 기록), 실물 / **적응 전 추론** / @3000 / @6000 / @10000 +
**`infer10000` 복제 두 행** = **12클립**. 사전 규칙은 **발화 회복(자연성 ≥ 3 ∧ 언어 '예')과 사용성 기준(그 조건 ∧ 결함 ≤ 1)을
따로** 둔다 — **결함 2가 남으면 사용 가능으로 승격하지 않는다.** 보조 대비는 행 A(@10000 − @3000)·행 B(적응 전 대비)·
행 C(6,000→10,000 구간의 상승, **"아직 오르는 중"으로 확대 금지**)이고, **절대 기준을 먼저 판정하고 발화 회복이 미달일 때만**
행 A·C로 후속을 고른다. catch는 **문장별 원본·복제의 네 조합**에서 분류가 하나라도 다르면 그 항목만 판정 불가다.
**중단 결정(사용자 요구)**: 발화 회복 미달 ∧ 행 A 미달이면 **이 계승 레시피의 warm-up·스케줄 손잡이 순회를 멈춘다** — 기각이 아니라
우선순위를 낮추는 결정이며 다음 축은 사용자가 고른다.

이 페이지부터 **판정 내보내기에 빈 칸 가드**가 걸린다(커밋 `d212a85`): 빈 칸이 실시간 표시되고 첫 클릭이 거절되며, 편집 없이 두 번째로
누를 때만 내려받는다. node로 동작을 확인했다.

### M5 — 한국어 프론트엔드 (구조 검증) — **코드 완료 (2026-08-30)**

- `ko_g2pkk.py`: g2pkk → 발음 한글 → 자모 직접 매핑 (espeak 제외, V4/V6 정정)
- **M1에서 만든 레지스트리에 코드 변경 없이 얹히는지**가 진짜 게이트다.
  얹히지 않으면 F1 설계가 틀린 것이므로 M1로 되돌아간다.

**게이트 G5 — 통과.** 파이프라인 파일(`frontend.py`·`prepare.py`·`audit.py`·`cli.py`·
`exporting.py`) **변경 0줄**. 변경은 `frontends/__init__.py`의 REGISTRY 항목 1개
(설계된 확장점) + `pyproject.toml`의 extra + 검수 스크립트의 한국어 읽기 분기뿐이다.
신규 심볼 0개, 최소대립쌍 13/13 보존.

**남은 것**: 한국어 화자 검수(G1(c)에 해당). 커버리지 스위트 246문장 덤프는 실패 0이며,
실제 전사 표본 검수가 남았다.

### M6 — 한국어 적응 + 배포 패키징

- F5, 한국어 데이터 확보(§5.3), stage 1/2 반복
- 언어별 릴리스 노트(데이터 출처·동의·프론트엔드·알려진 한계)

**게이트 G6**: 클린 환경에서 export 패키지 로드 + ONNX parity + 프론트엔드 의존성
누락 시 **영어 폴백 없이 실패**.

---

## 4. 코드 변경 목록

| ID | 변경 | 위치 | 단계 | 비고 |
|---|---|---|---|---|
| C1 | `frontends/` 레지스트리 패키지 | `frontends/__init__.py` | M1 | ✅ 완료. 훅 계약 불변 |
| ~~C2~~ | ~~`FrontendOptions.mode`에 레지스트리 이름 허용~~ | — | — | **삭제.** 레지스트리가 `mode="custom"`으로 해석하므로 `frontend.py`는 변경 불필요 |
| C3 | `--frontend` choices 확장 + `prepare` 배선 | `cli.py`, `prepare.py` | M1 | ✅ 완료 |
| C4 | `ja_openjtalk.py` | `frontends/ja_openjtalk.py` | M1 | ✅ 완료. pyopenjtalk-plus. 2026-09-04에 `fy`(フュ) 누락을 JSUT 덤프가 잡아 추가 — 음소 인벤토리 전수 테스트로 잠금 |
| C5 | `--require-no-new-symbols` | `audit.py`, `cli.py` | M1 | ✅ 완료 |
| C5b | export의 동봉 훅 자동 해석 | `cli.py` | M1 | ✅ 완료. 없으면 JA 경로가 end-to-end로 닫히지 않는다 |
| C6 | 178 → `>=178 + prefix` 완화 | `modeling.validate_release_compatible_symbols()` | M3 | ✅ 완료. 마이그레이션 테스트 공백도 같이 메움 |
| C7 | `--corpus-role` (다화자 허용) | `prepare_dataset()` · `audit_dataset()` | M3 | **조건부 보류.** §3.0 T3에서만 착수 — stage-1은 단일화자 JSUT로 성립한다. 기본 동작 불변 |
| C8 | F0 진단 추가 | `evaluation._signal_metrics()` | M4 | ✅ 완료(2026-09-04). median·IQR·유성 프레임 비율. fmax 1000 |
| C9 | ASR/CER 플러그인 (JA/KO) | `examples/transcript_evaluator_asr.py` | M4 | ✅ 완료(2026-09-04). `INFLECT_ASR_MODEL_DIR` + `local_files_only`, 자동 다운로드 금지 유지 |
| C10 | 블라인드 A/B 페이지 생성기 | `examples/build_blind_ab_page.py` · `examples/tally_verdict.py` | M4 | ✅ 완료(2026-09-04). 행별 무작위 라벨·봉인 mapping·실물 앵커 강제·catch 행 |
| C11 | `ko_g2pkk.py` | `frontends/ko_g2pkk.py` | M5 | ✅ 완료. 파이프라인 파일 변경 0 — **G5 통과** |
| C12 | 배포 런타임 프론트엔드 패키징 | `_write_deployment_runtime()` | M6 | |
| C13 | 적대항 게이팅 + 램프 + 디코더 lr 워밍업 | `training._adversarial_weight()` · `_decoder_lr_scale()` · `_scaled_decoder_lr()` | M7 | ✅ 완료(2026-09-05). 기본 off. 게이트 구간에도 D는 계속 학습 |
| C14 | recon-only 폴리시 + MR-STFT + proximal | `training._enabled_groups()` · `_multi_resolution_stft_loss()` · `_proximal_loss()` | M7 | ✅ 완료(2026-09-05). recon은 디코더만 학습, D 정지. STFT는 해상도 평균(PWG 관례) |
| C15 | 업샘플러 동결 · posterior 사이드카 · 생성기 EMA | `training._apply_stage()` · `checkpoint.save_posterior_sidecar()` · `exporting.export_checkpoint()` | M7 | ✅ 완료(2026-09-05). 업샘플러 동결은 그룹 분리가 아니라 기울기 마스크 — 옵티마이저 state 형태 불변 |
| C16 | 프레임 격자 스크린 + fp32 mel | `grid_screens.py`(신규) · `evaluation._grid_screens()` · `training._mel_from_spec()` | M7 | ✅ 완료(2026-09-05). 실물 40 대 렌더 40에서 grid·steady-tone이 0/40 대 40/40으로 완전 분리. fp32 mel은 AMP 런의 수치를 바꾸는 의도된 변경 |
| C17 | mel 크기 스펙트럼 공식 통일(D1) | `training_data.magnitude_spectrogram()`(신규) · `training._mel_from_waveform()` | M7 | ✅ 완료(2026-09-05). 같은 파형의 mel L1이 정확히 0. 임시 `--mel-loss-legacy-floor`는 대조 실험 후 제거 예정. **모든 런의 `loss_mel`·`loss_g` 절대값이 바뀌므로 이전 값과 비교 불가** |
| C18 | 경계 resume lr(D2) + 검증 RNG 격리(D5) | `training.train_adaptation()` resume 분기 · `training._validate()` | M7 | ✅ 완료(2026-09-05). 경계 재개가 무중단 런과 1e-18 이내로 일치. 초기 커밋부터 있던 결함 |
| C19 | evaluate 출처 명시(D3) | `evaluation.evaluate_checkpoint()` | M7 | ✅ 완료(2026-09-05). `source.mode`·합성 수·무시된 audio 수·혼합 검사. `ok`의 의미는 의도적으로 불변 |
| C20 | 배포 splitter 숫자 가드(D4) | `exporting._SENTENCE_BOUNDARY` · 신규 `tests/test_export_runtime_split.py` | M6 | ✅ 완료(2026-09-05). **기존 export 15개는 각자 사본을 갖고 있어 재-export 필요.** 공개 HF 패키지는 영향 없음 |
| C21 | 콤 절대 레벨 관측치 추가 | `grid_screens.grid_comb_metrics()` (`grid_tone_level_db`·`off_grid_level_db`) | M7 | ✅ 완료(2026-09-05). **격자 톤 초과는 비율이라 바닥이 다른 두 렌더의 순위를 뒤집는다** — mel A/B에서 40행 중 37행을 거꾸로 매겼다. 검출은 비율, 렌더 비교는 레벨 |
| C22 | 청취 페이지 공통 목표 RMS | `examples/build_blind_ab_page.py` (`page_target_rms_dbfs`·`crest_factor_db`·`LEVEL_FLOOR_DBFS`·`--catch-system`) | M7 | ✅ 완료(2026-09-06). 클립별 레벨링이라 피크 가드가 걸린 클립만 조용해졌다 — 지난 페이지 33트랙 중 9개가 목표 미달, 한 행 2.33 dB 차. 이제 모든 트랙이 도달 가능한 최대값 하나를 페이지 전체에 적용하고(지난 페이지 재생성 시 −27.28 dBFS, 편차 0.0001 dB), 바닥 −30 dBFS를 넘기면 중단한다. **`limited_by`는 시스템 이름을 담으므로 페이지에 박히는 `axes`가 아니라 봉인된 mapping 최상위에 기록한다** |
| C23 | 문장별 CSV에 절대 PSD 파생 열 | `inflect-work/scripts/verdict_mel_ab.py --csv` | M7 | ✅ 완료(2026-09-06). 스크린 세 값이 모두 순수 게인에 불변이라 절대량을 말하지 못한다. `레벨 + rms_dbfs`(원래 출력)와 `레벨 + 고정 기준`(공통 재생 레벨)을 나눠 기록하고 길이·무음 비율을 함께 남긴다. **라이브러리 키는 추가하지 않았다 — 항등식으로 파생된다** |

학습 코어(`training.py`)와 임베딩 마이그레이션(`checkpoint.py`)은 원래 **변경 대상이
아니었다.** 2026-09-05 사용자가 링잉 대응(개선안 b)을 승인하면서 이 제약을 해제했고,
C13–C16이 그 결과다. 새 옵션은 전부 기본 off이고 기본 경로의 손실·스케줄은 20 step
비교에서 마지막 자리까지 동일함을 확인했다. **분할 로직은 여전히 변경 대상이 아니고
실제로 손대지 않았다.**

---

## 5. 부록

### 5.1 일본어

**프론트엔드 파이프라인**
```
원문 → NFKC/공백 정규화 → pyopenjtalk.extract_fullcontext
     → (음소, 악센트구 위치 A:) → IPA 매핑 → 악센트 표기 → phoneme string
```

IPA 매핑(전부 base 178 안): `a i ɯ e o` / `ɴ`(N) `ʔ`(cl) / `k kʲ kʷ ɡ ɡʲ ɡʷ s ɕ z dʑ
t ts tʲ tɕ d dʲ n ɲ h ç ɸ b bʲ p pʲ m mʲ j ɾ ɾʲ w v`.

**장음은 길이 기호가 아니라 모음 반복으로 쓴다**(`koo`, `koː` 아님). 일본어는 모라 박자
언어이고 duration predictor가 심볼 단위로 동작하므로, 모라마다 심볼 하나가 예측 분포를
단봉으로 유지한다. 그래서 `ː`는 사용하지 않는다.

**D1 (결정됨 2026-08-30) — 피치 악센트 표기 = base의 `↑`/`↓`.** 178 인벤토리를 유지해
2단계 체이닝이 안전하고, C6 완화를 기다리지 않아도 된다. 대가는 두 임베딩 행이 영어
학습에서 거의 안 쓰였다는 점 — 실질적으로 신규 행에 가깝고 코퍼스가 가르쳐야 한다.
대안이었던 `ꜜ`(U+A71C)는 C6 선행이 필요해 보류했다.

**D2 (열림) — 악센트구 경계 문자.** 현재 경계는 **공백**이라 어절 공백과 구분되지 않는다
(`pau`는 `,`로 구분된다). base 안에서 `—`가 비어 있어 청취에서 구 분할 문제가 보이면
심볼 수 변경 없이 교체할 수 있다. metadata의 `accent_phrase_boundary` 필드가 이 선택을
기록한다.

**구현 노트 — 모라 경계.** 널리 복사되는 라벨 단위 악센트 규칙은 **자기 자신이 악센트구인
1모라**(예: 조사 `と`)에서 자음과 모음 사이에 경계를 끼워 넣는다. `_MORA_FINAL_PHONES`로
모라 종단에서만 표기하도록 막았다 (`tests/test_ja_frontend.py`가 회귀를 잠근다).

**무성화 모음**(`I`/`U`)은 1차에서 평문 모음으로 접는다. 필요성이 청취로 확인되면 그때
별도 표기를 도입한다 — 추측으로 심볼을 늘리지 않는다.

### 5.2 supertonic-ja-ft에서 반영하는 것

같은 사용자의 일본어 파인튜닝 선행 프로젝트(`~/github/supertonic-ja-ft`, 실험 124건 ·
청취 교훈 81건)에서 **구조가 달라도 유효한 것**만 가져온다.

**반드시 반영**

| 항목 | 출처 | 로드맵 반영 위치 |
|---|---|---|
| 자동 지표는 전부 스크린, 판정자는 청취 | 교훈 1·2·47·72·74·75 | F4, G4 |
| source-file-disjoint split (발화 해시 split은 동일파일 오염 100%였다) | `product_voice_data.md` §4 (NORMATIVE) | M2, G2 |
| 사전 등록 게이트를 데이터 본 뒤 옮기지 않기 | 교훈 5·67 | §3 서두 |
| 수치는 렌더 관례와 함께만 인용 | 교훈 79 | M0 config 기록 |
| anime 코퍼스는 타 코퍼스보다 5.7~16.6 dB 뜨겁고 0 dBFS 초과가 코퍼스 전역 성질 — 행 필터로 못 거른다 | 교훈 70·71 | M2 클리핑 정책 |
| 데이터 권리 게이트 (JSUT/JVS 상업 학습 허가 / つくよみちゃん "다른 캐릭터" 조항 / anime CC0 주장 무효) | `licenses_and_rights.md` decision 9·13 | M2, M6 릴리스 노트 |

**고 F0 여성 타깃에 특히 유효**

- **교훈 14·15** — F0 평균만 맞추는 목적함수는 flat-pitch 퇴화해를 갖는다. 평균 +2.48 st를
  달성했는데 컨투어가 평평해져 "오히려 더 어색"이 나왔다. F4의 F0 진단을 median 단독이
  아니라 **IQR과 함께** 보는 이유다.
- **교훈 73·76** — 540.9 Hz에서 실제 녹음은 깨끗한데 시스템만 갈라졌다. "여성 고음이라
  원래 그렇다"를 미리 차단하는 관측치. G4가 고음역 행 포함을 요구하는 이유다.
- **교훈 57·69** — 최악 tail 행을 직접 청취 페이지에 올린다. arm 귀속(paired)과 절대
  품질(floor)을 **분리해** 사전 등록한다. 공유 결함에 절대 바닥선이 발화한 전례가 있다.

**반영하지 않음**

- ONNX forensic → PyTorch parity → wav-to-latent → adapter 스택 전체. supertonic 전제의
  대부분은 "학습용 체크포인트 미공개"에서 파생됐다. Inflect는 학습 가능한 체크포인트와
  warm-start 경로를 공개한다 — **우회로가 통째로 불필요하다.**
- `jf-surgical-v1~v6` 어휘사전 아크(exp-j#001a~002c). orthography 프론트엔드(모델이
  텍스트를 직접 소비) 제약에서 나온 싸움이며, V7이 그 실패 클래스의 소멸을 보인다.
  다만 **고유명사 오독이라는 새 실패 클래스**가 생기므로 사용자 사전 슬롯은 유지한다(M1).
- `midhigh` 비음 학습 표적 (교훈 81 — 소진됨). 새 학습 목적함수로 제안하지 않는다.
- jp-base v4 / ship candidate `xa080` 등 자산. Supertonic 아키텍처 전용.

### 5.3 한국어

**프론트엔드 파이프라인** (구현됨)
```
원문 → 정규화 → g2pkk (어절 단위) → 발음 한글 → 음절 분해 → 자모 IPA 매핑 → 심볼열
```

**espeak는 체인에서 제외한다.** 후두 대립을 붕괴시키기 때문이다(V4 정정). 한글이
자질문자라, 음운이 이미 적용된 발음 한글을 음소로 바꾸는 것은 기계적 음절 분해다.
의존성은 `g2pkk` 하나(+ wheel로 따라오는 `python-mecab-ko`)로 줄었다.

경음은 `ʼ`(U+02BC), 격음은 `ʰ`(U+02B0) — 둘 다 base 안이라 3중 대립이 심볼을 늘리지 않는다.

**어절 단위로 돌린다.** 문장 전체를 넘기면 g2pkk가 경계를 넘어 연음을 과적용해
`오늘 날씨`→`오늘 랄씨`, `희망을 얘기`→`히망으 럐기`가 된다. 대가는 경계를 넘는 비음화를
놓치는 것(`몇 년`→`멷 년`)인데, 틀린 단어가 아니라 또박또박한 발음이라 안전하다.

**영문·낱자모는 거부한다.** g2pkk가 `IT`를 `읻`으로, `AI`를 `아이`로 읽으면서 라틴
문자를 남기지 않아 출력 검사로는 못 잡는다. 정규화된 **입력**에서 검사한다.
읽기는 `INFLECT_KO_LEXICON`으로 준다.

**알려진 g2pkk 한계**(코드로 고치지 않고 문서화 — 고치려면 수사 읽기를 재구현해야 하고
그건 "추측하지 않는다" 선을 넘는다): `세기`·`층`·`장` 앞 두 자리 이상 수는 자릿수로
읽힌다(`21세기`→`이일세기`). 단위 없는 맨 숫자도 자릿수로 읽힌다. 경음화가 가끔
과소적용된다(`여덟 시`→`여덜 시`).

**일본어보다 쉬운 점**: 어휘 성조가 없어 D1에 해당하는 결정이 없다.

> **검토 필요 (K1-D1)**: ㅐ/ㅔ를 `ɛ`/`e`로 **구분 유지**한다. 현대 서울말에서는 병합됐지만
> 병합은 되돌릴 수 없고 구분 유지는 되돌릴 수 있다. ㅚ/ㅞ는 둘 다 `we`. 청취 판단 항목.

**데이터**: 미확보. HF 캐시의 `Bingsu/KSS_Dataset`은 메타데이터 스텁(12K)이고 오디오는
없다. `fsicoli/common_voice_17_0`(2.3G 캐시)은 다화자라 stage-1 후보다.
**한국어 단일 화자 코퍼스 확보는 M6의 선행 조건이며 사용자 결정 사항이다**(§8 Q3).

---

## 6. CUDA 머신 인계

### 6.1 환경 구축 — 2026-09-04 실행판

이 머신의 저장소는 `/home/ysoya/projects/Inflect`다(작성 당시의 `~/github/...`가 아니다).
Blackwell(sm_120)에서는 **torch를 cu128 인덱스에서 먼저** 깔아야 한다. `pyproject`의 `torch>=2.2`는
sm_120 하한을 강제하지 않으므로, 순서를 바꾸면 기본 PyPI 빌드가 들어와 커널이 없다.

```bash
cd /home/ysoya/projects/Inflect/finetune
python3 -m venv .venv && source .venv/bin/activate && unset LD_LIBRARY_PATH
python -m pip install -U pip
python -m pip install "torch==2.8.0" --index-url https://download.pytorch.org/whl/cu128
python -m pip install -e ".[onnx,dev,ja,ko]"
pytest                                    # 111 passed, 1 skipped
INFLECT_TEST_BASE_MODEL=micro pytest -k inventory -p no:warnings    # 21 passed
```

**주의 (실제로 겪은 것)**
- `~/.bashrc`가 `LD_LIBRARY_PATH=/usr/local/cuda/lib64`를 설정한다. 시스템 CUDA lib가 wheel 동봉
  cuDNN/cuBLAS를 가릴 수 있으므로 **torch 셸에서는 `unset LD_LIBRARY_PATH`**.
- 이 머신에는 Python 3.12.3만 있고 `python3-venv`·`ensurepip`가 정상이라 작성 당시의 ensurepip
  우회는 필요 없었다. 3.12는 오히려 유리하다 — `onnxruntime` 1.29는 cp310 wheel이 없다.
- 본가 `pyopenjtalk` 대신 **`pyopenjtalk-plus`**(V5) 는 그대로 유효하다.
- `espeak-ng` CLI는 이 머신에 없지만 `espeakng-loader` wheel이 라이브러리를 제공하므로 설치하지
  않았다. JA/KO 프론트엔드는 espeak을 쓰지 않는다.
- g2pkk는 python-mecab-ko를 **첫 임포트에서 런타임 설치**한다. `[ko]` 엑스트라가 이제 분석기를
  명시하므로 그 부작용에 의존하지 않는다.

### 6.2 데이터셋 마운트

```bash
wsl.exe -d Ubuntu-24.04 -u root -- bash -lc "mkdir -p /mnt/m && mount -t drvfs M: /mnt/m"
```

경로 목록은 `/home/ysoya/projects/supertonic-ja-ft/configs/paths.example.yaml`,
환경 절차는 같은 저장소 `docs/environment.md`. (작성 당시 표기한 `~/github/...`는 이 머신에 없다.)

`/mnt/m`은 9p로 **≈53 files/s · ≈25 MB/s**다. 많은 작은 파일에서 병목이 되므로 학습·prepare는
로컬 복제본에서만 한다. 2026-09-04 복제·검증한 것:

| 데이터셋 | 로컬 경로 | 검증 |
|---|---|---|
| arona/plana JA (processed) | `inflect-work/data/ja` | `sha256sum -c` 2019/2019 |
| arona KR | `inflect-work/data/ko/FINETUNE_Arona_KR` | 3,401파일 · 48 kHz/1ch/24-bit 전수 · 전사 1:1 · 4.825 h |
| JSUT (`repeat500` 제외) | `inflect-work/data/jsut` | 8 하위셋 7,196발화 |

### 6.3 기준선 재현

M0에서 아래를 실행해 이 문서의 표가 그 머신에서도 참인지 확인한다. V4·V6는
2026-08-30에 정정됐다(espeak `ko` 제외) — 아래 한국어 스니펫은 정정 후 기준이다.

```python
# 신규 심볼 0개 확인 (V1, V2)
import sys, unicodedata
sys.path.insert(0, "finetune")
from inflect_finetune.symbols import BASE_SYMBOLS
base = set(BASE_SYMBOLS)
print(len(BASE_SYMBOLS))                      # 178
print(sorted(set("aiɯeoɴʔkɡsɕzdʑtɕɸçɲɾʲʷː") - base))   # []  (JA)
print(sorted(set("ɐʌɯɫŋʰʼqtɕ") - base))                # []  (KO)
```

```python
# 일본어 G2P (V5, V7)
import pyopenjtalk
print(pyopenjtalk.g2p("彼女は2026年8月30日に来ます。"))
print(pyopenjtalk.g2p("抗うつ剤の対策について、痛み止め薬を飲みました。", kana=True))
```

```python
# 한국어 (V6 정정판: espeak 없이 자모 직접 매핑)
from g2pkk import G2p
print(G2p()("국물 좀 드세요."))   # 궁물 좀 드세요.
```

프론트엔드 자체 확인은 테스트가 대신한다 — `pytest -k "ko_frontend or ja_frontend"`.

### 6.4 인계 상태와 첫 작업 순서

브랜치 `feat/multilingual-frontend-registry`. 프론트엔드 스택과 마이그레이션 경로는
끝났고, 남은 GPU-불필요 작업(C7·C8–C10)은 착수하지 않았다 — 학습 대기 시간에 넣을 수
있도록 남겨둔 것이다.

```bash
git fetch origin && git checkout feat/multilingual-frontend-registry
cd finetune
python -m pip install -e ".[onnx,dev,ja,ko]"
pytest                       # 111 passed, 1 skipped (opt-in 실물 테스트)
```

| 완료 | 내용 |
|---|---|
| M1 | 프론트엔드 레지스트리 + `ja-openjtalk` (신규 심볼 0, 피치 악센트 `↑`/`↓`) |
| M5 | `ko-g2pkk` (신규 심볼 0, 후두 대립 13/13 보존). **G5 통과** — 파이프라인 파일 0줄 |
| C6 | 확장 인벤토리 베이스 허용 + 미검증이던 임베딩 마이그레이션 테스트 |

| 상태 (2026-09-04) | 내용 |
|---|---|
| ✅ **M0/G0** | 통과. §3 M0과 `inflect-work/env/G0.md` |
| ✅ **체이닝 실증** | `INFLECT_TEST_BASE_MODEL=micro pytest -k inventory` → 21 passed (실물 Micro) |
| 🔁 **G1(c)** | 사용자 결정으로 **사람 카나 검수를 자동 스크린으로 대체**한다(아래) |
| ⏳ M2 | 매니페스트·prepare·audit 진행 중 |
| ⏳ C10 → C8 → C9 | GPU 불필요. JA-A/KO-A 학습 시간에 끼워 넣는다 |
| ⏸ C7 `--corpus-role` | **조건부 보류.** §3.0 T3에서만 착수한다 — 그때까지 stage-1은 단일화자 JSUT로 충분하다 |

**G1(c)의 사용자 결정 (2026-09-04)**: 사람 카나 검수 대신 **자동 스크린**을 게이트로 쓴다.
`reading_source == xlsx_reading_exact`인 1,140행에 대해 프론트엔드 덤프의 `reading` 열과 데이터셋
`reading_text`를 카타카나 통일·구두점 제거 후 정규화 편집거리로 비교하고, 분포와 상위 행을 기록한다.
거리 ≥ 0.5 행은 전사 불일치로 보고 매니페스트에서 제외한다. **이것은 로드맵 G1(c)의 예외이며,
"판정자는 청취"라는 원칙을 이 항목에서만 완화한 것이다** — 고유명사 오독(R3)의 실측 상한이 사라지므로
G4 라운드에서 발음 결함이 나오면 여기를 먼저 의심한다.

**첫 작업 순서** (1·2는 완료)

1. ~~G0~~ ✅ 2. ~~체이닝 실증~~ ✅
3. **G1 덤프** — 전 행(JA 1,366 / KO 3,401)에 `examples/frontend_review_dump.py`를 돌려 FAILED를 0으로
   만든다. `prepare`는 한 행만 실패해도 전체를 롤백하므로 이 덤프가 선행 게이트다. 덤프가 잡지 못하는
   **조용한 통과**(`☆ ～ 〜 ~`처럼 JA 구두점 맵에 없어 그대로 Open JTalk에 넘어가는 문자)는 매니페스트
   빌더에서 처리한다.
4. **M2** — 매니페스트(`group_id` = source file), 클리핑 정책, stage-1/stage-2 분리.
5. C8–C10은 GPU를 쓰지 않으므로 학습 대기 시간에 끼워 넣을 수 있다.

**인계받는 사람이 먼저 알아야 할 것**

- `export`에는 **`--package-template micro`가 필수**다. 학습 체크포인트는 base model을
  저장하지 않으므로(run identity 해시에 들어가기 때문) 익스포터가 추론할 수 없고,
  `--verify` 기본값이 true라 없으면 실패한다.
- **다화자 데이터셋은 아직 준비할 수 없다** (C7 미완). stage-1도 단일 화자여야 한다.
- **`--min/--max-duration-seconds`는 필터가 아니라 단언이다.** 범위 밖 행이 하나라도 매니페스트에
  있으면 `inspect_wav`가 던지고 `prepare`가 스테이징을 지우며 전체 실패한다. 길이·텍스트 필터는
  **매니페스트를 만들 때** 끝내고, 이 플래그는 같은 값을 단언하는 용도로만 쓴다.
- **`evaluate`는 행에 `audio`가 있으면 디스크 오디오를 읽고 모델을 로드하지 않는다.**
  `validation.jsonl`을 그대로 넘기면 실물 앵커 지표가 나오고(그것이 "같은 채널" 앵커의 정확한
  용도다), 렌더를 원하면 `audio`·`phonemes`를 제거한 사본을 넘겨야 한다. 후보 체크포인트를
  `--checkpoint`로 바꿔가며 `validation.jsonl`을 넘기면 전 후보가 같은 실물 수치를 낸다.
- **`max_steps`는 run identity에 들어간다**(`_public_options`가 제외하는 것은
  `base_model/prepared_dir/output_dir/preset/resume`뿐). 스텝 수를 바꾼 `--resume`은 거부되므로
  학습 연장은 export → `--base` 체이닝으로 새 run을 만드는 것뿐이다.
- `metrics.jsonl`에는 **타임스탬프도 검증 loss도 없다.** 벽시계는 로그로 재고, 체크포인트 선택은
  `evaluate` + 청취로 한다.
- **알려진 G2P 한계는 `docs/LANGUAGES.md`에 언어별로 정리돼 있다** — 예를 들어
  한국어는 `세기`·`층`·`장` 앞 두 자리 수를 자릿수로 읽는다. 새 오독을 만나면
  프론트엔드를 고치기 전에 먼저 거기를 본다.
- 자동 지표는 전부 스크린이고 판정자는 청취다(§5.2). 이 저장소의 결함 대부분은
  테스트가 아니라 **검수 덤프를 눈으로 훑다가** 나왔다.

---

## 7. 리스크 레지스터

| ID | 리스크 | 영향 | 완화 |
|---|---|---|---|
| R1 | 영어 남성 → 일본어 고 F0 여성 동시 이동이 3.96M/9.36M 용량을 초과 | 치명 | **가설이므로 직행 1런으로 측정한다**(§3.0). 되돌아갈 때 stage-1은 단일화자 JSUT → 그마저 실패면 C7 다화자. Micro 우선, Nano는 Micro 검증 후 |
| ~~R2~~ | ~~신규 심볼 발생 시 체이닝 붕괴~~ | — | **해소.** C5(탐지, M1) + C6(완화) 완료 |
| R3 | pyopenjtalk 고유명사 오독 | 중간→**높음** | 사전 슬롯은 구현됨(`INFLECT_JA_LEXICON`). 그러나 G1(c) 사람 검수가 자동 스크린으로 대체돼(§6.4) **오독률 실측치가 없다**. G4에서 발음 결함이 나오면 여기를 먼저 본다 |
| ~~R4~~ | ~~`mel_fmax`가 여성 고음을 자르는 값~~ | — | **해소(2026-09-04).** `mel_fmax = 12000` = 24 kHz Nyquist. 학습 전 결정 없음 |
| R5 | 청취 판정자가 1인(단일 리스너, 작은 N) | 중간 | supertonic이 동일 한계를 안고 갔다. 라운드 내 대비만 비교하고 MOS로 부르지 않는다 |
| ~~R6~~ | ~~anime 계열 클리핑이 고 F0에서 균열로 증폭~~ | — | **해당 없음.** anime 코퍼스를 쓰지 않는다. arona JA는 loudnorm −23·`clip_rows 0`, arona KR은 진클리핑 0 |
| ~~R7~~ | ~~한국어 단일 화자 데이터 미확보~~ | — | **해소.** §8 Q3 = `FINETUNE_Arona_KR` 3,401클립/4.825 h(검증 완료) |
| ~~R8~~ | ~~F1 설계가 한국어에서 안 맞음~~ | — | **해소.** G5 통과 — `ko_g2pkk.py` 추가 외 파이프라인 파일 0줄 |
| **R9** | 짧은 클립·비어휘 발성이 duration predictor를 훼손 | 중간 | JA <1 s 39클립·KO <1 s 5클립은 길이 하한으로, 구두점만 남는 행은 텍스트 하한으로 매니페스트에서 제외 |
| **R10** | JA 전사 품질 — `C_AronaChan` 170클립은 스크립트 없는 ASR 전사, 소스 표본 severe 3.3% | 중간 | 사용자 결정으로 **포함 + `text_source=asr_only` 태그**. 자동 스크린 상위 tail을 제외하고, JA-A 통과 후 제외 ablation 1회 |
| **R11** | 세션/소스 누출 — 한 소스에서 최대 26세그먼트, 배포 `splits/*`는 텍스트 20개·소스 14개가 교차 | 높음 | `group_id = source_relative_path` + 툴킷 group-aware split. 배포 split 파일 미사용 |
| **R12** | KO arona는 **JP arona와 다른 성우** | 치명(오용 시) | 트랙·데이터셋·`speaker` 값(`arona` vs `arona_kr`) 완전 분리. 체이닝·혼합 금지 |
| **R13** | KO `Chatbot_` 1,299클립(38%)이 다른 파이프라인 — `Arona_` 접두 없음, >16 kHz 에너지 8배, 같은 NAS에 2023-04 arona RVC 체크포인트 존재 | 높음 | prepare 전 **블라인드 스팟체크**(Chatbot 20 + 타 서브코퍼스 20, 봉인 mapping). 불합격 시 제외본으로 run 1 |
| **R14** | 단일 세션 코퍼스 암기 | 중간 | JA-A 8,000 step(≈53 epoch), KO-A 10,000 step. 프리셋 20k(≈133 epoch)는 쓰지 않는다. OOD 텍스트를 모든 청취 게이트에 넣는다 |
| **R15** | 학습 연장이 불가 — `max_steps`가 run identity에 포함 | 낮음 | 스텝 예산을 사전 선언하고, 연장은 export → `--base` 체이닝 새 run으로 |
| **R16** | **디코더 업샘플 격자 톤(링잉).** 신선한 `enc_q`/mean-only `flow`가 동결 디코더를 역산하며 z가 릴리스 분포를 벗어나고(채널평균 RMS 0.74 → 1.4–1.5), anti-imaging 없는 HiFi-GAN 디코더가 그 z를 93.75/750/6000 Hz 격자 톤으로 냄; 신선한 D의 해제 충격이 정적 톤을 응집; 체이닝은 이를 두 번 겪음 | **치명 — 2026-09-04 G4 두 라운드 모두 사용자 판정 "쓸 수 없음"** | 진단 `inflect-work/evals/diag/RINGING-DIAGNOSIS.md`. 2026-09-05 개선안 (b) 구현(C13–C16) 후 KO 2 arm 10,000 step 실행. **처방 두 개 모두 기각, 원인 가설도 기각.** 게이팅은 종점 콤을 바꾸지 못했고(8.15 → 8.27 dB) 드리프트는 오히려 늘렸다(1.190 → 1.528). recon 폴리시는 크게 악화시켰다(11.53 dB, 추적 음고 중앙값이 93.76 Hz로 붕괴, 40 중 31행 격자 잠김). **드리프트는 콤을 지배하지 않는다** — 1.190과 1.528이 같은 콤을 낸다. 남은 산출물은 계측이다(실물 0/40 · 렌더 40/40). 상세 §3.1 |
| **R17** | **배포된 Micro 디코더는 alias-free가 꺼져 있다.** `config.json`에 `decoder_alias_free` 없음(→ False), `model.pth`에 Snake/필터 텐서 없음 — 표준 HiFi-GAN V1. README·ARCHITECTURE의 "alias-reduced decoder"와 불일치 | 중간 | 저장소 소유자 보고. 켜려면 재학습 필요(장기) |

---

## 8. 사용자 결정 필요

| # | 질문 | 관련 |
|---|---|---|
| ~~Q1~~ | ~~일본어 stage-2 목표 화자~~ | **결정(2026-09-03): `Ja_Voice_AroPla_processed/wavs/arona`** — 1,366클립/110.1분, 48 kHz mono 16-bit, loudnorm −23/TP −1.5, `clip_rows 0`. Plana는 혼입 금지 |
| ~~Q2~~ | ~~배포 계획~~ | **결정(2026-09-03): 배포 없음, 사설 연구.** 따라서 No.7 비상업 조항·つくよみちゃん "다른 캐릭터" 조항·anime CC0 의심은 **학습 입력 사용을 막지 않는다**(권리 기록 decision 13 "매니페스트·캐시·학습 입력 사용 승인, 외부 배포 금지"). 대신 산출물 외부 공개 금지가 유지되고, 매니페스트는 `license`/`distribution_gate`를 보존해 배포 가능 부분집합을 사후 재구성할 수 있게 한다 |
| ~~Q3~~ | ~~한국어 단일 화자 코퍼스~~ | **결정(2026-09-03): `ko_AronaPlana_voice/FINETUNE_Arona_KR`** ("arona kr") — 3,401클립/4.825 h, 48 kHz mono 24-bit, 전사와 1:1. **JP arona와 다른 성우이므로 별개 화자 트랙**(R12). 권리 기록이 어디에도 없어 한 줄 문구가 필요하다(비차단) |
| ~~Q4~~ | ~~D1 — 일본어 피치 악센트 표기~~ | **결정됨: `↑`/`↓`** (§5.1 D1). C6 완료로 `ꜜ`도 가능해졌지만 재개방하지 않음 |
| Q5 | D2 — 일본어 악센트구 경계를 공백 유지 vs base의 `—`로 분리 (§5.1 D2) | 청취 후 |
| Q6 | K1-D1 — 한국어 ㅐ/ㅔ 구분 유지 vs 병합 (§5.3) | 청취 후 |

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-30 | 최초 작성. V1~V10 실측 기준선 확립. |
| 2026-08-30 | M1 코드 완료. C2 삭제(레지스트리가 custom으로 해석), C5b 추가, D1 결정(`↑`/`↓`), D2 등재, 장음 정책 명시. G1은 화자 검수 대기. |
| 2026-08-30 | M5 코드 완료, **G5 통과**(파이프라인 파일 0줄). V4·V6 정정 — espeak `ko`가 후두 대립을 붕괴시켜 체인에서 제외하고 자모 직접 매핑으로 대체. K1-D1 등재. |
| 2026-08-30 | C6 완료. V9 해소. 인벤토리 검증을 준비 데이터셋·런타임이 공유하고, 미검증이던 임베딩 마이그레이션 경로에 테스트를 넣었다. 버려지는 base 심볼을 `compatibility-report.json`에 보고. |
| 2026-08-30 | 인계 리뷰. **문서의 export 예시 5개가 그대로는 실패하던 것을 수정** (`--package-template` 필수). §6.4를 인계 상태 기준으로 재작성. R2·R8 해소, Q4 결정 반영, Q5·Q6 등재. |
| 2026-09-04 | **M0/G0 통과** (RTX 5090 / sm_120 / torch 2.8.0+cu128 / pytest 111·1 / 실물 체이닝 21 passed). `mel_fmax 12000` 실측 → R4 해소. JA·KO·JSUT 로컬 복제·검증. Q1·Q2·Q3 결정 반영, R6·R7 해소, R9–R15 등재. **JA 경로를 하이브리드로 확정**하고 T1–T3 전환 규칙·직행 G4 baseline 정의를 §3.0에 고정. G1(c)를 자동 스크린으로 대체(예외 명시, R3 상향). C7을 조건부 보류로 이동. §6.1·§6.2·§6.4를 이 머신 기준으로 재작성. **G0을 따라가다 드러난 공개 툴킷 결함 2건 수정** — 문서가 지시한 `finetune/.venv`가 `test_public_safety`를 깨뜨린 것, `pyproject`가 릴리스 런타임의 `unidecode`/`num2words`를 선언하지 않아 깨끗한 설치에서 `train --base micro`가 죽던 것. |
| 2026-09-04 | M2 통과. `ja-arona-v2`(A_CO026 제외)·`ja-jsut-v1`·`ko-arona-v1b` 준비. **KO 클리핑 정책 발동** — 소스가 아니라 우리 리샘플이 331행(9.89%)의 상단을 잘라내고 있었고, 선언대로 균일 −3 dB 후 재준비. **툴킷 결함 2건 추가 수정**(WAVEX 거부, 출력단 클리핑 미보고). C8·C9·C10 완료, 청취 파이프라인 실물 예행 검증. G3/stage-2/G4 실행 스크립트와 선언 규칙 고정. `fy` 수정이 사전 준비 데이터셋의 export를 무효화하는 것을 확인 — **프론트엔드 수정은 prepare 앞에 온다**. |
| 2026-09-05 | **G4 두 라운드 미통과(링잉).** 진단 완료: 디코더 업샘플 격자 톤(93.75/750/6000 Hz 배수), 1차 원인 z 드리프트 + 신선한 D 충격 + 체이닝, anti-imaging 없는 디코더가 enabler. R16·R17 등재. E1(디코더 조기 해제) 기각. 처방은 학습 코어 변경이라 사용자 결정 대기. |
| 2026-09-05 | **외부 감사 확정 결함 5건 수정 — C17–C20.** mel 양쪽 바닥값 통일(같은 파형 L1이 `ln(100)` → 0), 경계 resume lr, 검증 RNG 격리, evaluate 출처 명시, 배포 splitter 숫자 가드. **전부 초기 커밋부터 있던 결함.** D1은 실패 런 전부에 영향을 주므로 K-A/K-B 결론을 "깨진 목적함수 아래의 측정"으로 강등했고, `docs/TRAINING.md`·`docs/TROUBLESHOOTING.md`의 인과 서술 5건(flow 경로 오류, 재구성 손실 민감도, 드리프트 추적, 경로 불일치, F0로 적응 성공 판단)을 정정했다. pytest 330 → 364. 1,500 step mel 대조 실험 진행 중. |
| 2026-09-05 | **KO 2 arm 판정 — 처방 기각, 계측 존속.** K-A(게이팅)는 대조군과 구별되지 않았고(grid 8.27 대 8.15 dB), K-B(recon 폴리시)는 더 나빠졌다(11.53 dB, f0 중앙값 93.76 Hz). 잠재 드리프트가 콤을 지배하지 않음을 확인 — 진단의 인과 사슬 기각. `docs/TRAINING.md`·`docs/TROUBLESHOOTING.md`의 권장 문구를 측정 결과로 교체했다. 다음 후보는 z 스케일이 아니라 z의 프레임 간 구조, 그리고 디코더의 anti-imaging 부재 자체. |
| 2026-09-05 | **개선안 (b) 구현 — C13–C16 완료.** 사용자 승인으로 학습 코어 제약 해제. 손잡이 9개 전부 기본 off, 기본 경로 20 step 비교에서 레거시 열 불일치 0. pytest 155 → 330, ruff 74 → 68(신규 지적 0). 스크린 임계는 계획값에서 실측으로 이동(G 2.0 → 4.0 dB, fold 3.0 → 6.0 dB, steady-tone 기본 off → on) — 근거는 `inflect-work/runs/REMEDY-B-VERIFICATION.md`. **아직 링잉이 사라진다는 증거는 없다**: 600 step 스모크에서 게이팅을 켜고도 콤이 step 200에 나타났고(사이드카 G −0.24 → +7.18 dB), 다만 잠재 드리프트는 대조군보다 낮았다(z_dc_rms 1.035 대 1.19–1.42). 판정은 KO 3 arm 10,000 step이 한다. |
| 2026-09-06 | **mel A/B 청취 판정 = 사용성 탈락, 계기 3건이 3답.** 두 arm 모두 품질 1 8/8, 언어 "아니오" 8/8, 자유기술 "발음 템포에 ringing 기계음만". 절대 평균 PSD로 재측정한 결과 **"부분 개선 −3.16 dB"는 철회** — 절대 on-grid는 +0.99 dB(9/40)이고 전체 전력이 +3.97 dB 커진 것이며, matched 렌더가 더 짧고(40/40) 무음이 적다(38/40). 등급을 "개선 불확실"로 재분류. 과장 3건 정정("귀로 구분 불가", "디코더 사실상 면제", 지표 미명시한 "디코더 학습이 악화")과 오기 1건(K-A 사이드카 → M-legacy). **mel 수정(D1)은 유지.** C22·C23 완료. §3.4 경로 진단(재구성 대 추론) 착수 — 새 장기 학습은 진단·청취 후 별도 승인. |
| 2026-09-06 | **경로 진단 청취 판정 — 콤과 말소리는 다른 문제다.** 기존 체크포인트로 재구성(실물→enc_q→dec)과 추론(음소→enc_p/flow→dec)을 갈랐다. 재현 먼저 통과(진단 추론이 배포 `model.infer`와 비트 동일, control 재구성이 2×2 셀을 0.0000 dB 재현). **control@10k는 콤 초과 +8.1로도 말소리·한국어로 판정됐고, matched@1500은 같은 +7.7~+8.6으로 말소리가 아니다 — 초과 크기는 말소리 성립을 예측하지 않는다.** **step 500 추론은 콤 없는 말소리(품질 2·결함 0)였고 1,500 step에서 그것을 잃었다.** 1,500 arm만 f0가 93.75 Hz에 잠긴다(34/40·39/40). control 재구성이 말소리로 판정되며 **재구성 경로의 양성 대조가 생겨** 디코더 구조 가설이 약해졌다. 사전 선언 분기 4번. 확인 페이지 2문장 대기, 새 장기 학습은 그 뒤 별도 승인. |
| 2026-09-06 | **W1500 짧은 탐색 런 — warm-up 도중 posterior가 스스로 콤에 잠긴다.** posterior warm-up 500→1500 한 옵션, 2,000 step, 10분 47초. 무결성 전부 통과(동결 경로 렌더 max|diff| 0.000e+00). 손실 창은 개선(mel 0.877 대 1.083)인데 f0 잠김은 **step 1000에 재구성 39/40 잠김** — linguistic·decoder 전부 동결 구간이다. ADAPT 진입 상태가 matched(0/40)와 뒤바뀌었고 ADAPT+500 추론은 양쪽 40/40이나 콤 초과는 W가 5.1 dB 낮다. 계기 둘이 다른 답을 내므로 판정은 청취(`warmup-20260906`, 3문장 × 5시스템)가 하며 문장별 ⓐⓑⓒ 대비로 보존/실패/미결을 가른다. 기전 확정·다른 개선책 배제 없음. |
| 2026-09-06 | **W1500 청취 판정 = 실패(막지 못함; 재해석: 관측한 초반 구간의 말소리 미성립), 준비도 조작 불성립.** 3문장 × {진입 직전 재구성, W 추론, 비교군 추론} 전부 자연성 1. 사전 선언 규칙 그대로: G1 실패("가설 미시험"), ⓑ=X∧ⓒ=X 3/3("시험한 스케줄은 이 실행에서 막지 못했다"). matched@1000 infer 첫 청취 = 말소리 아님. **fresh posterior는 linguistic 단계 없이 posterior-only 학습만으로 1,000 step 안에 콤에 잠긴다** — 후속 설계의 입력. 어떤 개선책도 제외하지 않음. 다음 런은 별도 계획. |
| 2026-09-06 | **JSUT를 기준점으로 되돌리고 posterior 계승을 대조.** 짧은 런 3개(≈32분). **JSUT는 20k 전 구간에서 콤에 잠기지 않는다**(잠김 0–5/40, 초과 +1~+4 dB) — 같은 step 1000에서 KO 런은 39–40/40. stage 2(fresh posterior)는 중간 양상(1000에서 13–16/40 → 7000 회복). 현행 코드 JSUT 재현은 750–1250 과도 구간을 지나 2,000에서 원본과 거의 같아지고 한 번도 잠기지 않는다. **posterior 계승 arm이 계기에서 일관되게 낮다** (진입 직전 재구성 초과 −4.5 dB, ADAPT+500 추론 잠김 19→9/40). base 바이트 동일·sidecar max|Δ| 0·옵션 diff 한 키·동결 경로 0.000e+00 전부 확인. 두 페이지(화자별) 청취 대기. 기전 확정·개선책 배제 없음. |
| 2026-09-06 | **JSUT 기준점 확립 · 계승 처방은 관측 구간의 미성립을 막지 못함(초기화 기여 가능성 잔존).** 페이지 1: JSUT@20000 추론·재구성 3/3 자연성 4·결함 1(규약상 기준 품질). **JSUT@1000은 3/3 자연성 1** → "1,000은 JSUT도 말소리 아님", Context 전제 철회. 계기 미잠김(0/40, 205 Hz)인데 말소리 아님 → **미잠김 ≠ 말소리** 두 번째 반례. C(현행 코드 replay@1000)는 원본도 X라 판단 불가, `most_blurred` 3/3은 replay. 페이지 2: 재구성@500·추론@1000·@2000 세 행 모두 X/X ≥ 2/3 → "posterior 계승만으로 관측 구간의 말소리 미성립을 막지 못함"(처방 실패 ≠ 원인 기각); 0036에서만 계승 arm 자연성 2(효과 전무 아님). 계기 차이(−4.5 dB, 19→9/40)가 청취로 이어지지 않음. 기전 확정·개선책 배제 없음. |
| 2026-09-07 | **말소리 형태 JSUT 2k(언어 인식 4k) · 적응 직전(JSUT 목소리) 추론 출력이 관측 구간에서 저하.** 새 학습 없음. 페이지 A: 문장별 지속 성립 N=2,000(2k recon은 3/3), 품질은 2→5로 계속 상승(성립 ≠ 완성), 해제 전 이미 말소리(디코더 인과 미확정), **같은 업데이트 수의 KO 런은 미성립**(epoch 3 대 14·시작 가중치·데이터·음역 동시 상이 → 학습 길이 포함 축 미확정). 페이지 B: **동결 JSUT 사전은 Arona 텍스트를 자연성 3~4로 읽고 2k 적응 뒤 같은 문장이 1이 된다(2/3)** → 관측 구간에서 기존 추론 출력(JSUT 목소리) 저하; 계승 posterior의 재구성 실패는 **warm-up 이전부터**(바닥 점수라 추가 악화 미판정, 결합 시험); 0029 계승 > fresh는 단일 문장 관측. catch 복제쌍 불일치 A 0·B 1; 라운드 간 재청취 공통 9파일 중 3개 경계 넘어 변경. 원본@2000이 말소리로 확인돼 replay@2000 비교가 다음 라운드에 유효. 청취 부담 축소 방침 등재. |
| 2026-09-08 | **모듈 복원 — 원본 텍스트에서도 저하 확인, 모듈 귀속 미결.** 새 학습 없음. 적응된 체크포인트에서 추론 모듈만 적응 전으로 되돌려 렌더(무결성: 무복원 == @2000 추론 0, 전체 복원 == 적응 전 추론 0, 변형마다 텐서 `torch.equal`). 페이지 1(JSUT 텍스트): 적응 전 5·1·예 → 적응 후 2·2·아니오(−3, −3) → **원본 텍스트에서도 큰 자연성 저하**(언어도 실제 악화). flow 복원 +1·+1, enc_p+dp 0·+1 → **둘 다 미결이나 flow는 부분 개선 신호**(0025 언어 아니오→예), 복원 후보 모두 결함 2 잔존 → "flow 동결"로 넘어갈 근거 부족. 페이지 2(Arona 텍스트, 11클립) 판정 대기. 청취 페이지 음원 출처 검증 기준을 '원본 sha 불변 + 동일 변환 경로 바이트 일치'로 정정(PCM16 절단으로 절대 오차 1 LSB는 정상). |
| 2026-09-08 | **모듈 복원 페이지 2 — 목표 텍스트에서도 저하 확인, flow 복원은 catch 불일치로 미결.** 새 학습·새 렌더 없음. Arona 텍스트 0036·0040: 적응 전 4·1·예 / 5·1·예 → 적응 후 **1·2·아니오** 두 문장(이득 −3(복제 −2), −4) → **목표 텍스트에서도 관측 구간의 큰 자연성 저하**, 언어도 두 문장 모두 실제 악화. **이 페이지 catch(적응 후 복제)는 불일치**(원본 1 vs 복제 2, 말소리 경계) → 그 복제쌍의 불일치이며 라운드 잡음 바닥 아님. `flow←500` 이득 **+2(복제 +1), +2** — 원본 점수로는 두 문장 모두 기준 도달이나 복제 점수로 분류가 바뀌어 **미결**(페이지 1 +1·+1보다 큰 부분 개선 신호, 언어 두 문장 아니오→예; 합산 금지). `encp+dp←500` **+1(복제 0), +1** 미결. 복원 후보 모두 결함 2 잔존·자유기술은 적응 전 대비 어조 변화·ringing 증가. 사전 분기상 "큰 회복 확인" 불성립 → 모듈 귀속 미결 유지, **JSUT 동일 데이터 재시작 대조**를 별도 계획으로. flow 재확인 소형 페이지는 사후 탐색 후보(별도 승인). |
| 2026-09-08 | **동일 데이터 재시작 대조 — 청취 대기.** 승인된 2,000 step 런 1개. S2-inherit과 `--dataset` 한 개만 다르게(JSUT 데이터로) 다시 적응. 엄격 검사 전부 통과(옵션 JSON 모든 키·값 일치, prepared_dataset sha 3/3 일치·baseline과 3/3 상이, base sha 동일, 계승 3항, **@500 추론 40/40 비트 동일**, 손실 2,000행 유한) — 범용 검사의 `<1e-5`·허용키 PASS는 기준으로 쓰지 않고 `scripts/exact_match.py`를 따로 만들었다. 계기: 이 런의 추론이 2,000 step 내내 +1 dB 대·220 Hz·0~1/40, 같은 문장에서 S2-inherit@2000은 +6.00 dB·280.9 Hz·10/40 — **계기이며 말소리·품질이 아니다**. 페이지 `restart-jsut-20260908`(9클립, 처음 쓰는 문장 0012·0031, catch는 주질문 트랙 복제) 판정 대기. 언어 축 순서를 처음 못박음(`예`→`아니오`만 악화, `예`→`판단 어려움`은 미결). |
| 2026-09-09 | **동일 데이터 재시작 대조 판정 — 이 조건에서는 저하가 관측되지 않았다.** 새 문장 0012·0031, 9클립, 축 미기입 0. 적응 전 4·1·예 / 5·1·예 → **JSUT 데이터 적응 후 5·1·예(복제 4)·4·1·예**(이득 −1(복제 0)·+1, **두 문장 1등급 이내 → 행 ① 미결, 저하 관측 안 됨**; 결함 1 유지, 일본어 예 유지) → 같은 라운드의 **Arona 데이터 적응 후는 1·2·아니오 두 문장**(적응 전 대비 +3·+4 → 행 ③ 페이지 1 손상 확인 재확인, 언어도 실제 악화). 행 ② 두 조합 비교 성립(+4(복제 +3)·+3)이나 **관측만 적고 데이터 변경에 귀속하지 않는다**(데이터량·화자·음역·정렬·계승 posterior 정합이 한 묶음). **"재시작은 무해하다"·"데이터가 원인/무관"·기전 확정 금지.** catch는 1등급 갈렸으나 분류 불변(세 페이지 연속 1등급 이내 불일치). 계기와 청취가 이번에는 같은 방향이었으나 계기를 판정 근거로 올리지 않는다. 모듈 귀속 미결·flow 우선 후보 유지. |
| 2026-09-09 | **라운드 E — flow 복원 재확인 페이지 봉인, 청취 대기.** 학습·새 체크포인트 없음. 페이지 2의 flow 미결이 `adapted2k` 복제쌍 1 대 2에 걸린 것이므로 같은 렌더 조건에서 문장만 바꿔(**0002·0003**, 계기 항 없는 선택 규칙으로 청취 전 고정) 한 번 더 듣는다. 렌더 무결성 두 줄 `max|diff| 0`, 함께 렌더한 0036이 봉인본과 바이트 동일, 봉인 sha 7개 불변, 페이지 8클립·편차 0.0001 dB·복제쌍 2개 바이트 동일·이름 누출 0·안내문 sha 페이지 2와 동일. 복제는 **주비교 당사자이자 1↔2에서 여러 번 갈린 `adapted2k`**에 두 행 모두. 적응 전 트랙이 없으므로 **"적응 전 대비 저하"·"원래 품질 회복"은 이 라운드에서 쓰지 않는다**. `flowback`은 단일 평가라 반복 변동 미확인 — catch가 보는 안정성은 `adapted2k` 교체에 한정. **사용자 결정: 재확인은 한 번; 다시 미결이면 반복 청취 대신 데이터 전환 조건 분해로; 이번 결과만으로 장기 학습·모듈 동결을 선택하지 않는다.** |
| 2026-09-09 | **라운드 E 판정 — flow 복원 다시 미결(+1, +2), flow 축 반복 청취 종료.** 새 문장 0002·0003, 8클립, 학습 없음. 적응 후 **1·2·아니오 두 문장**(복제도 동일) → `flow←500` **2·2·판단 어려움** / **3·2·예**. **catch가 두 문장 모두 세 축 정확히 일치(불일치 0단계)** 하므로 이번 미결은 catch 탓이 아니라 0002의 실제 이득이 +1이기 때문이다. `adapted2k`가 1이라 미시험도 아니다. `flowback`도 결함 2·2 잔존, 언어는 0003만 아니오→예 회복(0002는 판단 어려움 → 미결)이라 페이지 수준 언어 회복은 쓰지 않는다. 페이지 2(+2(복제 +1)·+2)와 **합산하지 않는다** — 네 문장 부호는 모두 양수지만 복제 유지 조건에서 두 문장 모두 ≥ 2를 넘긴 페이지는 없다. `flowback`은 단일 평가라 반복 변동 미확인. **사전 분기대로 flow 축을 접고 데이터 전환 조건 분해로 이동**; 모듈 귀속 미결 유지, flow 갱신 제한 학습 대조·장기 학습·모듈 동결은 선택하지 않는다. |
| 2026-09-09 | **라운드 F — 축소 JSUT 재시작 페이지 봉인, 청취 대기.** flow 축 반복 청취를 끝내고 데이터 전환 조건 분해로 이동한 첫 대조. 승인된 2,000 step 런 1개. `prepared/ja-jsut-v1`을 복사해 `train.jsonl`만 층화·시드 고정 추출 **1,170행 / 1.508 h**(Arona 1,170행 / 1.519 h와 규모·시간·**통과 수 13.7** 일치)로 바꾸고 `validation.jsonl`·`symbols.json`·`audio/`는 바이트 그대로 — 측정 40문장과 감시 클립이 동일. `audit --strict` 통과. 엄격 검사(`exact_match.py --dataset-axis train`) 전부 통과: 옵션 완전 일치, train·dataset 해시만 다름, validation·symbols 해시 같음, base sha 동일, 계승 3항, **R1@500 추론이 C1@500과 40/40 비트 동일**, 손실 2,000행 유한. 계기는 두 arm이 사실상 동일(+1.08 대 +1.07 dB, 잠김 0/40)이고 같은 텍스트의 Arona arm은 +6.00 dB·10/40 — **계기를 판정으로 읽지 않는다**. 페이지 `small-jsut-20260909`(10클립, 처음 쓰는 문장 0002·0003, 복제는 주질문 트랙 두 행). 사전 규칙에 **척도 여유(미시험)**·**행 ③ 상호 조건**·**미달을 '유지'로 쓰지 않음**을 못박았고, 부분집합이 base의 기학습 코퍼스에서 나온 것은 **일반화 한계**이므로 저하가 없어도 '적은 새 자료 반복은 무해'로 넓히지 않는다(**기학습 여부는 두 arm 모두 고정** — 2026-09-10 정정). |
| 2026-09-09 | **라운드 F 판정 — 축소 JSUT에서도 큰 저하가 관측되지 않았다.** 처음 쓰는 문장 0002·0003, 10클립. 적응 전 3·2·예 / 4·1·예 → **축소 JSUT 4·1·예 / 4·1·예**(이득 −1, 0 → 행 ① 미결, 저하 관측 안 됨; 결함 1·언어 예로 적응 전 이상), **전체 JSUT도 4·1·예 / 4·1·예**(적응 전 대비 −1, 0 → 행 ③ (a) 유지 재확인). 행 ② 두 규모 비교는 **이득 0, 0** — 같은 자연성·결함·언어. **catch가 두 문장에서 세 축 모두 일치(0단계)** 하므로 어떤 분류도 복제에 걸리지 않는다. **"유지"·"자료량 무관"으로 쓰지 않고**, 부분집합이 base의 기학습 코퍼스에서 나왔다는 일반화 한계 때문에 **"적은 새 자료 반복은 무해"로도 넓히지 않는다**(미분해 항목은 **반복 횟수·부분집합 구성** — 기학습 여부는 두 arm 모두 고정, 2026-09-10 정정). 계기도 두 arm이 같은 자리(+1.08 대 +1.07 dB)였으나 판정 근거로 올리지 않는다. **사전 분기 세 조건 충족 → Arona 음성 특성·계승 posterior 정합으로 초점 이동**(별도 계획). |
| 2026-09-10 | **라운드 G — 계승 posterior에 Arona warm-up 1,000 step 추가, 페이지 봉인·청취 대기.** 승인된 3,000 step 런 1개(ADAPT 업데이트를 baseline과 같은 1,500회로 맞추기 위한 길이; decoder 단계 진입 0회 확인). **inherit × warm-up 1500** 셀은 어느 화자로도 처음이다 — W1500은 fresh·KO `arona_kr`(다른 성우)·ADAPT+500이었으므로 같은 실험의 반복이 아니다. 시험 대상은 **스케줄**이며 준비도 자체가 아니다(warm-up 중에도 판별기가 학습하고 lr·데이터 스트림·RNG 상태가 함께 움직인다). 엄격 검사 **ok 줄 열한 개** 전부 통과: 옵션 키 집합과 **값**(1500·3000), 데이터 축 무이동(4해시 전부 동일), base sha, 계승 3항, **W2@500·@1000·@1500 추론이 S2-inherit@500과 40문장·유한·`array_equal` 40/40**, 손실 3,000행 유한. `exact_match.py`에 `--options-allow KEY=VALUE`·`--dataset-axis none`·`--steps`/`--baseline-step`을 추가(값 검증·비대칭 step·유한성 포함) — `warmup_gates`의 max|diff| 출력은 NaN에서 0으로 남을 수 있어 기준으로 쓰지 않는다. 계기는 두 arm의 경계 재구성이 거의 같고(+7.56 대 +7.70 dB) 손실 창도 0.01 수준 차이 — **손실을 준비의 근거로 쓰지 않는다**(**2026-09-12 정정: 0.01은 mel 창에 한정한다 — kl 창은 3.2167 → 3.5254로 늘었다**). 페이지 `warmup2-20260910`(12클립, 처음 쓰는 문장 0006·0019, 복제는 기준 재구성 두 행). 사전 규칙에 **미달 ≠ 개선 없음**, **행 B는 '저하가 줄었다'로 쓰지 않음**, **A 미달·B 성립 칸 별도 보존**, 척도 여유(원본·복제 모두 ≥ 4 → 미시험, 한쪽만 → 미결)를 못박았다. |
| 2026-09-12 | **라운드 G 판정 — 두 행 모두 사전 기준 미달, 가설은 미시험으로 남는다.** 처음 쓰는 문장 0006·0019, 12클립, 축 미기입 0. 실물 5·0·예 두 문장. **행 A**: 경계 재구성 `reconbase` 2·2·아니오(복제 동일) → `reconlong` 2·2·아니오, 이득 **0, 0** → **사전 조작 성립 기준 미달**("개선되지 않았다"로 바꾸어 읽지 않는다). **행 B**: 동일 ADAPT+1500 추론 `inferbase` 1·2·아니오 → `inferlong` 1·2·아니오, 이득 **0, 0** → **미결**("줄지 않았다"·"효과 없음" 금지, 적응 전 추론이 페이지에 없어 "저하가 줄었다"도 쓰지 않는다). **catch가 두 문장에서 세 축 정확히 일치(0단계)** 하므로 어떤 분류도 복제에 걸리지 않고, `reconbase` 2·`inferbase` 1로 **두 행 모두 미시험이 아니다**(≥ 2등급 개선이 척도상 가능했다). 사전 고정한 결합 해석대로 **"이 스케줄로 준비된 posterior에서 ADAPT를 시작한다는 가설은 시험하지 못했다"** — B 미달은 기각이 아니며, W1500은 문구의 선례일 뿐 **같은 실험으로 묶지 않는다**(fresh·KO `arona_kr`·ADAPT+500). 말소리 성립은 재구성 세 조건 성립·추론 두 조건 미성립이나 **1↔2 단일 판정으로 대비하지 않는다**. 결함은 합성 네 조건 모두 2, 언어는 두 쌍 모두 아니오→아니오 **변화 없음**, 강제 선택 `most_blurred`가 두 문장 모두 `inferbase`였으나 판정 축이 아니다(두 추론 트랙 자연성이 같은 1). 계기도 같은 방향(+7.56 대 +7.70 dB)이었으나 판정 근거로 올리지 않는다. **다음 후보는 조작을 성립시키는 다른 손잡이(더 긴 warm-up·낮은 posterior lr·미실행 W1500g 셀)에서 별도 계획으로 고른다** — "준비도가 레버다/아니다"·"≤ 1,500 warm-up 기각" 금지, 기전 확정 금지, 자료량 축 열림·flow 귀속 미결 유지, 장기 학습·모듈 동결 선택 없음. |
| 2026-09-12 | **라운드 G 기록 정정·보강 — 손실 창은 mel 한정, "미시험" 옆에 처방의 실용적 결과.** 학습 없음. (1) "손실 창 0.01 수준"을 **mel에 한정**했다 — mel 0.9881 → 0.9774(Δ 0.011)이지만 **kl은 3.2167 → 3.5254(Δ +0.309)** 다. kl 증가를 실패의 원인으로 읽지는 않되 두 손실을 뭉쳐 적지 않는다. 판정 진술은 **날짜 각주**로, 변경 이력 행은 **행 안 정정**으로, `TROUBLESHOOTING.md`의 서술 문장은 본문 수정으로 고쳤다("the listening agreed"가 혼합된 계기 판독에 붙지 않도록 문장 전체를 다시 썼다). (2) "가설 미시험" 옆에 **이번 처방의 실용적 결과**를 함께 적었다: **warm-up 연장 처방 자체는 시험했고**, 두 문장에서 사전 기준 큰 개선을 만들지 못했으며, 최종 추론은 **자연성 1 · 일본어 '아니오'로 사용 가능한 발화로 회복되지 않았다**. 더 준비된 posterior의 효과는 미확인. 따라서 **이 처방을 채택할 근거가 없고 같은 방식의 추가 연장 우선순위를 낮출 근거**지만 **모든 warm-up 길이의 효과를 부정하지 않는다**. 경계 재구성의 자연성 2는 규약상 말소리 성립이되 **결함 2·언어 '아니오'·ringing 자유기술과 함께 놓으면 "준비 완료"로 해석할 근거는 없다**. |
| 2026-09-12 | **동일 설정 재실행에서 학습 결과가 변동한다 — 새 사실, 원인은 분리하지 않았다.** 학습 없음(기존 체크포인트 비교). W2와 S2-inherit은 step 0–499에서 옵션·시드·데이터가 같은데 **step 500 체크포인트에서 `enc_q` 0/100 동일**(max|Δ| 9.3980e-03), **판별기 0/111 동일**(5.0316e-02)이고 **동결된 나머지 410개는 410/410 비트 동일**이다. 직접 뒷받침되는 것은 "같은 명목 설정에서도 학습된 가중이 달라졌다"까지이며 **GPU 커널·AMP 등 원인은 분리하지 않았고 그 실험도 추가하지 않는다**. 렌더 쪽은 별개로 안정적이다(같은 체크포인트·고정 시드 재렌더가 바이트 일치). **함의**: 두 런의 학습된 가중을 비교하는 모든 대조에 이 변동이 함께 들어 있고 **크기는 미측정**이다. 런 대 런 대조에서 성립은 이미 한 번 기록돼 있으므로(**라운드 D 행 2, +4(복제 +3)/+3**) 이 유보는 앞으로의 성립뿐 아니라 그 판정에도 걸린다. 라운드 F 행 2(0, 0)·라운드 G 두 행(0, 0)의 미결·미달 라벨은 관측 점수의 산술이라 그대로지만 **이득 0을 "변동이 작다"의 근거로 쓰지 않고**, |이득| ≤ 1을 성립 조건으로 쓰는 유지형 판정(라운드 F 행 3)은 오히려 **약화**된다. **반복 한 쌍을 변동의 상한이나 검증된 "잡음 바닥"으로 취급하지 않는다.** 각주는 라운드 D·F·G에만 붙였다. |
| 2026-09-12 | **라운드 H — warm-up 동안만 생성기 adv/FM 가중을 0으로, 페이지 봉인·청취 대기.** 승인된 저장소 코드 변경 1개 + 3,000 step 런 1개. 기존 `adversarial_gating`은 `decoder_unfreeze_step`에 고정돼 이 일정에서 **전 구간**을 막으므로(K-A 로그 step 1–3000 전부 0.0) **`posterior_warmup_steps`에 고정된 두 번째 게이트** `warmup_adversarial_gating`을 추가했다 — warm-up 0 · ADAPT 1.0 · **판별기 정책 무변경** · 기본값 off · 두 플래그 합성. **런보다 먼저 기본 경로 불변 게이트 통과**: 변경 전후 20 step(CPU·`--no-amp`)의 `metrics.jsonl` **20/20 바이트 동일**, 옵션은 **새 키 하나만** 추가, pytest 370 → **397 통과·1 스킵**, ruff **68 불변**. 엄격 검사 **ok 줄 열한 개** 전부 통과하고 `exact_match.py`는 **고치지 않았다**(없는 키를 센티넬이 잡아 값 검사가 직렬화까지 증명한다). **개입 증명은 렌더가 아니라 로그로** 했다 — 행 1–1500 advW 0.0·adv/FM null·`loss_d` 유한, 행 1501부터 1.0. 코드 변경 뒤 재렌더한 **W2의 0036이 봉인 sha와 바이트 일치**. 계기는 경계 재구성이 거의 같은 자리(+7.70 대 +8.08 dB)이고 손실 창은 **mel·kl을 각각** 적었다(경계 직전 W2 0.9774/3.5254, W3 0.9653/3.2064; warm-up의 `loss_g`는 두 항이 빠져 있어 비교 불가). 페이지 `warmupadv-20260912`(12클립, 처음 쓰는 문장 0020·0022, 복제는 기준 재구성 두 행). 사전 규칙에 **행 A 성립도 관측형으로**, **두 행 미달은 '문장별 이득을 각각 기록'**, **척도 여유는 문장 단위**, **성립 시 W2 반복 → W3 반복 두 단계**를 못박았다. |
| 2026-09-12 | **라운드 H 판정 — 두 행 모두 사전 기준 미달, catch가 한 문장에서 1등급 갈림, 축 미기입 7.** 처음 쓰는 문장 0020·0022, 12클립. 실물 5·0·예 두 문장. **행 A**: `reconbase` 2(복제 1)·1(복제 1) → `recongate` 2·1, 이득 **0, 0**(복제 기준 +1, 0) → **사전 조작 성립 기준 미달**. **행 B**: `inferbase` 1·1 → `infergate` 1·1, 이득 **0, 0** → **미결**. **catch가 0020에서 원본 2 대 복제 1로 말소리 경계를 넘어 갈렸으나 행 A의 분류는 어느 쪽으로 계산해도 미달**이라 판정 불가가 아니고, **말소리 성립의 그 한 칸만** 판정 불가다. 척도 여유는 네 기준값 모두 < 4라 두 행 다 시험됐다. 사전 고정 문구대로 **"어느 행도 두 문장 모두에서 ≥ 2등급 기준을 충족하지 못했다. 문장별 이득은 각각 기록한다"** 로 적는다. `infergate`는 두 문장 모두 자연성 1·결함 2로 **사용 가능한 발화로 회복되지 않았다**(0022의 언어 축은 미기입). **축 미기입 7칸**(0022의 언어 다섯·결함 둘)은 앞선 네 라운드의 0과 달라 그 축을 그 문장에서 읽지 못하며, **미기입을 `아니오`·`판단 어려움`으로 대신 읽지 않는다**. 자유 기술은 두 문장 모두 ringing 기계음이고 "발성되는 음소의 캐릭터가 JSUT 화자에 가깝다"는 관측이 붙었으나 **정체성은 판정 축이 아니어서 기록만** 한다. 계기도 거의 같은 자리(+7.70 대 +8.08 dB)였으나 판정 근거로 올리지 않는다. **다음 후보는 posterior 학습률 하향**(별도 계획); 추가 warm-up 연장은 **라운드 G 한 번의 미달**로 우선순위만 낮추고 **라운드 H는 두 arm의 warm-up 길이가 같아 길이 축의 근거로 세지 않는다**. |
| 2026-09-12 | **라운드 H 기록 정정 두 곳 + 얻은 것 명시.** 학습 없음, 재청취 없음. (1) "0022 세 축 일치"는 **언어 축이 원본·복제 양쪽 모두 미기입**이었으므로 성립하지 않는다 — 일치한 것은 자연성과 결함뿐이다(런 문서·§3.13 양쪽에 **날짜 각주**). (2) 복제 기준의 **+1을 "부분 개선 신호"로 읽지 않는다** — 후보 `recongate`는 원본·복제 어느 쪽으로도 **2로 그대로**이고 +1은 **같은 바이트 음원인 기준 트랙의 점수가 2에서 1로 내려가서만** 생긴 값이다(날짜 각주, `TROUBLESHOOTING.md`는 서술 단락이라 본문에 한 절 추가). 정정 방법은 2026-09-12에 못박은 규칙 그대로 **판정 진술 = 날짜 각주**다. 함께 적은 것: 이 조건에서 warm-up 동안 **생성기의** adv/FM 두 항을 0으로 두었을 때 `recongate`는 2·1이었고 행 A는 두 문장 미달이었으므로 **이 두 항을 끄는 처방을 채택할 근거는 없다**. 다만 **판별기는 내내 학습했고 진입 시점 상태도 달라졌으며** ADAPT에서 두 항을 다시 켰으므로 **이후 추론 실패에서 adv/FM의 역할까지 배제한 것은 아니다**. |
| 2026-09-12 | **라운드 I — 계승 레시피를 디코더 해제 너머까지, 10,000 step 봉인·청취 대기.** 승인된 10,000 step 런 1개 + 페이지 빌더 코드 변경 1개. **계승 다섯 런이 전부 디코더 학습 전에 끝났던 공백**을 채운다(닿지 못한 13런 중 12런이 `max_steps ≤ unfreeze`, 예외는 E2). 질문을 **"최종 추론이 사용 가능한 일본어 발화가 되는가"** 하나로 모았다. **같은 화자에서 해제를 지난 런은 이미 실패했고**(`ja-arona-stage2`, 해제 뒤 5,000 step, G4 "쓸 수 없음") 그 런과 **다섯 가지가 다르다**(초기화·warm-up·길이·`validation_interval`·**코드 시대**, 그 런은 D1 mel 수정 이전) — **identity 파일은 코드 시대를 기록하지 않는다**. **런이 두 번 끊겼다**(6500·8000, 체크포인트 기록 순간의 호스트 저메모리): 중복 행은 0이지만 **로더 상태가 저장되지 않아 두 지점에서 데이터 순서가 끊겼고, 이 런은 중단 없는 10,000 step이 아니다**. 엄격 검사 **ok 줄 열한 개** 통과(옵션 두 키 값까지, 데이터 축 무이동, 동결 경로 40/40 × 3), 단계 진행은 첫 ADAPT 1501·첫 `decoder_polish` **3001**·디코더 7,000행·최종 `decoder_polish`. 계기는 **해제 뒤 f0 잠김이 풀리고**(12/40 → 0/40) f0가 278 → 355 Hz로 오르지만 **콤은 +6.8~+9.9로 남는다** — **잠김 해제도 콤 감소도 회복으로 읽지 않는다**. 페이지 `recover10k-20260912`(12클립, 처음 쓰는 문장 0023·0024, 복제는 마지막 추론 두 행). 사전 규칙에 **발화 회복과 사용성 기준(결함 ≤ 1)을 분리**, **절대 기준 우선 판정**, **catch 네 조합**, **행 C는 구간 상승으로만**, 그리고 **중단 결정**을 못박았다. 이 페이지부터 판정 내보내기에 **빈 칸 가드**가 걸린다. |
| 2026-09-06 | **저장된 체크포인트 궤적으로 붕괴·회복 조건을 분리.** 5런 × 모든 체크포인트 × 2경로 × 40문장 f0 잠김. 붕괴는 모든 런에서 디코더 동결 상태의 linguistic 첫 500 step 안(K-A만 1000에서 미잠김이나 같은 차단 조건의 K-B는 40/40이라 미재현). 회복은 control·K-A(디코더 학습 + adv ON)만 4000에서 즉시, K-B(adv OFF)는 7,000 step 디코더 학습 뒤에도 잠김, E2(디코더 동결)는 끝까지 잠김 — **회복 = 학습 가능한 디코더 × adversarial ON.** 콤 초과는 내내 +6~+12. 두 문장 확인 라운드 분기 재현(3/3). 미청취 결정 지점 5개 페이지(`path-unlock-20260906`, T1–T5 사전 선언) 대기. JA E1(500 해제) 궤적 측정 중. |
